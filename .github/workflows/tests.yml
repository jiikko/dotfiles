---
name: Tests

'on':
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

jobs:
  dotfiles-tests:
    runs-on: ubuntu-slim
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5

      - name: Install runtime dependencies (Neovim, tmux, zsh, bats)
        run: |
          sudo apt-get update
          sudo apt-get install -y neovim tmux zsh make bats

      - name: Run dotfiles test suite
        run: make test-runtime
