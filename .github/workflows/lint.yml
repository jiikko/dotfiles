---
name: Lint

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:  # GitHub UI からの手動再実行 (flake 切り分け用)

permissions:
  contents: read

# git init の hint 抑制 (init.defaultBranch)。env で注入する理由は _go-project.yml 参照。
env:
  GIT_CONFIG_COUNT: "1"
  GIT_CONFIG_KEY_0: init.defaultBranch
  GIT_CONFIG_VALUE_0: main

# 同一 ref の連続 push では古い run を打ち切る (旧コミットの結果に価値が無いため)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dotfiles-lint:
    runs-on: ubuntu-slim
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7

      - name: Install lint dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          # ruby は test-ruby-syntax (Brewfile/_pryrc の ruby -c と _gemrc の YAML パース) 用
          sudo apt-get install -y --no-install-recommends zsh shellcheck yamllint jq ruby

      # actionlint は apt に無いため公式リリースバイナリを取得する (test-actionlint が使う。
      # shellcheck が PATH にあると run: ブロックのシェル検査も有効になる)
      - name: Install actionlint
        run: |
          # gh api は一時的に HTML エラーページを返すことがある (実例: run 29542275908 の
          # "invalid character '<'")。version 解決は再試行し、空の ver のまま壊れた URL を
          # 組み立てるのを防ぐ。curl の -f は HTTP エラー時に 404 ページを tarball として
          # 保存して後段の tar が「not in gzip format」で死ぬ分かりにくい失敗を防ぐ。
          for i in 1 2 3; do
            ver=$(gh api repos/rhysd/actionlint/releases/latest --jq '.tag_name' | sed 's/^v//') \
              && [ -n "$ver" ] && break
            echo "actionlint version 解決に失敗 (attempt $i)。再試行..." >&2
            sleep 5
          done
          [ -n "$ver" ] || { echo "::error::actionlint の最新バージョン解決に失敗 (gh api が JSON を返さない)"; exit 1; }
          curl -fsSL --retry 3 --retry-all-errors -o "$RUNNER_TEMP/actionlint.tar.gz" \
            "https://github.com/rhysd/actionlint/releases/download/v${ver}/actionlint_${ver}_linux_amd64.tar.gz"
          mkdir -p "$RUNNER_TEMP/actionlint"
          tar xzf "$RUNNER_TEMP/actionlint.tar.gz" -C "$RUNNER_TEMP/actionlint" actionlint
          echo "$RUNNER_TEMP/actionlint" >> "$GITHUB_PATH"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Run lint suite
        run: make test-lint

# Go プロジェクト (src/*) の lint / test は、ここではなく再利用 workflow _go-project.yml を
# 呼ぶ paths filter 付きの caller (src_<project>.yml) で回す。全 push で走る lint.yml に置くと
# 非接触の push でも毎回実行されるため分離した (2026-07-17)。新しい Go プロジェクトを src/ に
# 足したら src_<project>.yml (caller) を 1 本作ること (root Makefile の GO_PROJECT_DIRS コメント参照)。
