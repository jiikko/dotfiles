# 474 (retro): pro-con で PM をしながら PG を 4 本並べ、もろい作りの監査を回した夜 (2026-09-25)

起票日: 2026-09-25

## 概要

retro 448 の後の作業。PM (人間の代わりの Claude) として C-004〜C-015 を回し (446・447・444・451 を取り込み、437・445・457・458・438 を PG が作業中)、
pro-con のもろい作りの監査 (460。issue 457〜466) と、ユーザーの要望の起票 (452〜456・467〜473) をした。うまくいった話は書かない。

## どこで踏んだか

1. **並べる前に「同じ判断を変える組」を見積もらなかった**。C-007 (437: PM を終了で止める対象に入れる) と C-010 (457: 記録に無い session を止める対象に入れる) は
   どちらも「終了のときに何を止めるか」を変えると分かっていたのに、PG に「review の前に rebase」とだけ頼んで並べた。ユーザーに指摘された (468)
2. **既にある欄の意味を、見出しで読んで使い間違えた**。PG への指示を `card add --request` (人間の原文の欄) に書き続けた。受け手 (`dispatcher.Prompt`) を読むと
   `--prompt` が最初から PG への指示の欄だった。画面の見出し「PM に渡した指示」も中身と食い違っていて、読み違いを助けた
3. 重い処理を直列にしたつもりが、pro-con の中だけだった (外の session の make test・PG の自前の変異の検証が並び、ロードアベレージ 25 / 14 コア。471)

## 次に効きそうな改善 (切り出し先の提案。実行はユーザーの判断を待つ)

1. **書き込みの作業を並べる前に、各作業が変える「判断・不変条件」を 1 行ずつ書き、同じものを変える組は順番に出す**。worktree を分けるのはファイルの衝突を防ぐだけで、
   意味の衝突 (合わせた結果が片方のテストでしか守られない) は防がない
   → 切り出し先: `_claude/rules/parallel-write-agents-need-worktree-isolation.md` の「ルール」へ追記
2. **既にある欄・引数・口を使う前に、その値を受け取る側のコードで意味を確かめる**。見出し・名前・usage の文面は受け手とずれていることがある
   → 切り出し先: `_claude/rules/survey-receiver-guards-before-passing-new-values.md` の「ルール」へ追記 (新しい値を通す前に受け手を洗う、と同じ形の「既にある口」版)

## 却下 (既存のルールで足りる / 局所)

- 17 秒で rc=0 の make test を、何が走ったかの出力で確かめた: `verify-execution-not-just-exit-code.md` どおりに機能した
- 見張りのスクリプトが自分のカードの追加でも起きる: その場の道具の局所的な改善 (提案しない)
- 3 (重い処理の直列化): 仕組みの issue (471) に切り出した。規範としては 1 と同じ形 (並べる前に共有する資源を数える) なので 1 に含める

## 決着 (2026-09-26、カード C-076)

ユーザーの決定: 「codex (gpt-6-astra) に聞いて、意義があると判断されたら追記を許可する」。判定と扱い:

- 1 (並べる前に変える判断・不変条件を書き、同じものを変える組は順番に) → **採用**。`_claude/rules/parallel-write-agents-need-worktree-isolation.md` のルールに足した
- 2 (既にある欄を使う前に受け側で意味を確かめる) → **採用**。`_claude/rules/survey-receiver-guards-before-passing-new-values.md` の発動点とルールに足した
