# 197 retro: ratelimit ダッシュボードの未消費 5h カード (2026-09-03)

起票日: 2026-09-03

## このセッションでやったこと

1. codex rateLimits の `resetsAt: null` (未消費の枠) を捨てていたパーサを直し、`Window.Unused` で
   「無い」と「まだ使っていない」を型で区別。1 行 / 表 / ダッシュボードの 3 経路で「未消費」と出す
2. `tools/dial-preview -unused` を足し、3 サイズで見た目を確認 (2 行目の説明文は幅で候補を選ぶ形に)
3. 新規テスト 3 本を変異 3 本で red 確認。read-only の敵対レビュー 1 体: P2 1 件 (未消費の 1 行目だけ `fitLine` を通しておらず狭幅で語の途中で千切れる。再現あり → 採用し、同型の窓幅不明の行も揃えた)。MergeLastGood / キャッシュ往復 / glogx 本体側の消費経路は壊せず。未確認リスク: 表の「時間」列に全角語だけの行が混ざる見え方 (issue 196 で目視)

## 反省・気づき (切り出し先を提案。実行はユーザーの判断待ち)

### 1. 「API が返さない」の出所を 2 つ (Claude `/usage` / codex app-server) 持っていて、片方しか確定できなかった

codex 側は JSON スキーマとテストのフィクスチャから `resetsAt: null` を確定できたが、Claude 側の
`/usage` の文言は 200MB の minify バイナリを grep しても取れず (BSD grep は 2 分で timeout、
`strings` 経由は断片だけ)、**未確認のまま**にした。ユーザーの環境で現象が出た瞬間の生出力を
残してもらう方が確実 (issue 196 に依頼)。

**切り出し先案**: 却下 (ルール化するほど一般性が無い)。issue 196 で回収する。

### 2. zsh で `echo ====X` が glob 展開されて「not found」になった

区切り線を `echo ====SNAP` と書き、zsh の `=command` 展開で失敗して 1 往復無駄にした。
引用符で囲めば済む。

**切り出し先案**: 却下 (自分の手癖の問題。次から引用符)。

## 残課題

- issue 196 (human) の確認結果を待つ。Claude 側の文言が分かれば、それに応じてパーサを足す
