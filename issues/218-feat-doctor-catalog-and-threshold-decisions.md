# 218 feat: doctor カタログと閾値の未決事項 (issue 148 の 8 章から切り出し)

起票日: 2026-09-03
出典: [issues/148](done/148-feat-glogx-doctor-disk-diagnosis.md) の 8 章 (未決事項) と 7 章の未チェック 2 件
重要度: **P3** (どれも実装の残りではなく、**ユーザーの判断**か**実測の機会待ち**)

## なぜ切り出したか

148 は段階 ①②③④ を実装し切って done にした。8 章に残っていたのは「実装すべきこと」ではなく
**「どう決めるか」**と**「実機で測らないと分からないこと」**で、実装の issue にぶら下げたまま
done にすると判断ごと埋もれる。ここへ移す。

**着手の単位は 1 項目ずつ**。まとめてやる作業ではない (どれも独立している)。

## 判断待ち (ユーザーが決める)

- ~~doctor の起動キー~~ → `D` に決定 (3 章「起動導線」)
- **保存結果が実態とずれる問題** (codex P3-3、未解決)。
  外部で掃除した場合 (`brew cleanup` を手動実行、Xcode が自分で DerivedData を消した等)、
  保存結果は古い数字を持ったままで、**トーストが「もう無いゴミ」を宣伝する**。
  起動時に走査しない以上、検知手段が無い。現状の緩和は `(N 日前の診断)` の添え字のみ。
  → doctor を開いた時点で必ず再スキャンするので実害は「トーストが一度空振りする」程度、
  という整理でよいか要判断
- **`speech-model-cache` を将来カタログに載せるか** (今回は保留)。
  載せるには `lsof` 判定 + 削除後に音声機能が壊れないことの実測が要る
- ~~トーストから doctor を直接開けるか~~ → 文言で誘導するだけに決定 (3 章「起動導線」)
- 閾値 10GB は妥当か (今回は 41GB 溜まっていた。10GB だと頻繁すぎる可能性)
- 再通知の抑止期間 3 日は妥当か
- 同時走査数の上限 4・エントリごとの timeout 60 秒は妥当か (実測して決める)
- `~/Downloads/*.crdownload` を対象に含めるか (今回 749MB あった。ユーザーデータ領域なので慎重)
- **`xcode-devicesupport` のグロブを `iOS` 以外へ広げるか** (2026-09-01 レビュー)。
  Xcode は watchOS / tvOS の実機を繋ぐと同名の DeviceSupport を作るはずだが、
  **この機には `iOS DeviceSupport` しか存在せず未実測**。
  カタログの登録条件 (実測してから登録) に従うなら、実機を繋ぐまで確認できない。
  グロブだけ広げて空振りさせるか、実測できるまで iOS 限定に戻すか要判断
- **小物の同型キャッシュを載せるか** (`electron-builder` 82MB / `typescript` 31MB /
  `deno` 27MB)。構造は Tier 1 と同じだが、1 件あたりが小さく**行が増えるだけ**になる。
  「合計 N MB のツールキャッシュ」と 1 行にまとめる案もある
- **`~/.Trash` (2026-09-01 実測 2.3GB) を扱うか。** 「ユーザーが既に消したもの」なので
  提案する価値はあるが、**空にすると復元不可**。ツールから消すのではなく
  「ゴミ箱に 2.3GB あります」と**表示するだけ** (`deleteVia: propose`) が妥当か要判断。
  🚨 `risk: confirm` の削除先がゴミ箱なので、**ゴミ箱を空にする機能と衝突する**
  (同じ画面で「ゴミ箱へ移す」と「ゴミ箱を空にする」を並べない)
- ~~**`go-modcache` を `rm` でなく `go clean -modcache` にするか**~~ → **決着済み**
  (カタログは `cli:go clean -modcache`。失敗は「走査できず」に倒れる)。以下は当時の検討:
  module cache は read-only で作られるため `rm -rf` には強制が要る一方、
  `go clean` は **go 本体が必要**で、goenv の版が壊れていると実行できない。
  `cli:` を主経路にしつつ、失敗時に「走査できず」に倒す設計でよいか要判断
- `~/src/**` のビルド成果物・`node_modules` を扱うか
  (プロジェクト単位の判断が要るので別 issue が妥当)
- 既存の `cli_health.go` を doctor 画面に統合するか (今回は**しない**方針。
  統合するなら別 issue で、起動時トーストの挙動を変えないこと)
- **サービス診断を起動時トーストの対象にするか** (今回は**しない**方針)。
  ディスクは「合計 GB」という自然な閾値があるが、サービスは件数しかない。
  「壊れた登録 1 件」で毎回トーストを出すと、**消すまで永久に鳴り続ける**
  (ディスクの `lastNotifiedAt` と違い、放置してよいケースがある)。
  → doctor を開いたときだけ出す、で足りるか要判断
- **`/Library/LaunchDaemons` の読み取りに権限が要るか** (要実測)。
  root 所有だが `0644` が通例なので読めるはず。読めない場合は「診断できず」に倒す
- **`sfltool dumpbtm` を使うか**。2026-09-01 の実測では**一般ユーザーで出力が取れた**が、
  出力形式は非公開で macOS バージョン間の安定性が不明。補助情報に留めるか、そもそも外すか
- **BTM の残骸を「独立した検出項目」に昇格させるか** (4 章の実測を受けて)。
  plist が消えていても検出できる強い判定だが、**`legacy agent` の消し方を提示できない**
  可能性がある。「検出できるが直せない」項目を出すべきかの判断が要る
- **`launchctl print` の出力パースの安定性**。`penalty box` は `properties = ` 行に出るが、
  これも非公開形式。**取れなければ「penalty box 情報なし」で続行**する設計にはしてある
- サービス診断の結果もキャッシュするか (ディスクと違い走査が軽いので、
  doctor を開くたびに実行でよい可能性が高い)
- **プロセス側の診断を別 issue として起こすか** (`5` 章の結論は「本 issue では扱わない」)。
  起こすなら前提は「外部に正本が無いので**自分で記録を作る**」ことで、
  glogx が起動したプロセスを追跡する常設の仕組みが要る。
  **`5` 章の実測 (629 件の誤検出 / `pane_pid` の限界) をそのまま前提として引き継ぐこと**
- ~~`cacheBaseDir()` が `package main` の unexported で、別パッケージから呼べない~~
  → **解決 (2026-09-02)**。`doctor/cachedir` パッケージへ切り出し、glogx 側は
  `cacheBaseDir()` を `cachedir.Base()` の 1 行 alias にした。受け入れ条件の文言も直した


## 7 章の未チェック 2 件 (④ の範囲外だったもの)

- [ ] **`sfltool dumpbtm` のパース失敗が BTM 行を出さないだけで済む** — 段階 1 で
      **BTM の判定自体を実装していない**ので、条件が成立していない。BTM を載せるなら
      同時にこの受け入れ条件を満たす
- [x] **glogx の起動時間が悪化しない** → **実測して決着 (2026-09-03。issue 206 の (b))**。
      doctor 導入前 (`b2f8ede1`) と HEAD でバイナリを建て、隔離 tmux で「起動〜初回描画」を
      10 回ずつ測った: **中央値 327.8ms → 329.5ms**。差 +1.7ms は群内のばらつき (14〜19ms) より
      小さく、`loadDoctorDiskCache` 単体の 26.5µs は測定の分解能の 1/60 なので壁時計では
      原理的に見えない。分布と外れ値の扱いは
      [`issues/done/206`](done/206-perf-doctor-unmeasured-leak-and-startup-items.md) が正本。
      ⚠️ Bench 経路には**乗せない**と判断済み (`tests/glogx/bench_glogx.sh` の冒頭が
      「バイナリ起動の壁時計は CI では flake 枠」と既に決めており、実測の外れ値 1.9 倍がそれを裏づけた)

## 叩き台 (2026-09-03 の実測つき推奨。○×を付けてもらう)

実測は 2026-09-03 22:5x、この機 (dotfiles の開発機)。`diskdoctor` の合計は **84.3GB**
(148 起票時の 41GB から倍増している)。内訳の上位は npm-cache 21.1 / go-build 20.1 /
xcode-deriveddata 17.6 / simulator-runtimes 16.2 / go-modcache 5.8 GB。

### A. 実測したら既に決着していた / 前提が消えていた (5 件)

| 項目 | 実測 | 推奨 |
|---|---|---|
| `xcode-devicesupport` の glob を iOS 以外へ広げるか | **既に広い**。`catalog.go:58` は `~/Library/Developer/Xcode/* DeviceSupport/*`。なお**この機には DeviceSupport が 1 つも無い** (実機を繋いでいない) | **現状維持で決着**。issue の記述が古い。空振りのコストは 0 件時の glob 1 回だけ |
| `/Library/LaunchDaemons` の読み取りに権限が要るか | **要らない**。dir は `drwxr-xr-x root:wheel`、中身は `-rw-r--r--`、5 件読めた。`svc/scan.go:29` が既に対象にしている | **決着**。「読めない場合は診断できずに倒す」実装はそのまま保険として残す |
| `sfltool dumpbtm` は一般ユーザーで取れるか | **取れる**。rc=0 / **stdout 775 行 / stderr 0 行** (stream 分離で実測) | 取得可否は決着。ただし**採用するかは C 節へ** (形式が非公開という論点は消えていない) |
| `~/.Trash` を扱うか | **今は 0 件 / 0 バイト** (2026-09-01 の 2.3GB は既に空にされている) | **却下**。下記 |
| `~/Downloads/*.crdownload` を対象に含めるか | **今は 0 件** (2026-09-01 の 749MB は消えている) | **却下**。下記 |

`~/.Trash` の却下理由: ①**空にすると復元不可**で、ツールの他項目 (`deleteVia: trash`) の
避難先そのもの。同じ画面に「ゴミ箱へ移す」と「ゴミ箱を空にする」が並ぶのは危険
②実測 0 件で、ユーザーは既に自分で空にする習慣がある ③Finder が標準機能で持っている。
**再開の trigger**: ゴミ箱が数 GB 溜まったまま放置されているのを見たとき。

`*.crdownload` の却下理由: **ユーザーデータ領域**で、中断した DL の再開情報を持つ。
実測 0 件。カタログの登録条件「消したまま戻らないことを実測」を満たせない
(消すと DL のやり直しが要る = 戻らない)。**再開の trigger**: 同上。

### B. 実測から妥当と言える (現状維持。4 件)

| 項目 | 現状 | 実測の根拠 | 推奨 |
|---|---|---|---|
| 同時走査数の上限 | 4 (`scan.go:76`) | `du` の並行度実測 (163 体 4): 1 並列 16.0s / 2 並列 9.1s / **4 並列 4.6s** / 8 並列 4.7s。**4 で頭打ち** | **現状維持で決着** |
| エントリごとの timeout | 60 秒 (`scan.go:79`) | 最大の `du` (63GB / 596k ファイル) が 4 並列で 4.6s。60 秒は 13 倍の余裕 | **現状維持で決着** |
| サービス診断の結果をキャッシュするか | しない | svc の走査は launchctl の呼び出しだけで軽い (ディスクの `du` と桁が違う) | **キャッシュしないで決着** |
| 保存結果が実態とずれる問題 (codex P3-3) | 緩和は `(N 日前の診断)` の添え字のみ | doctor を開いた時点で必ず再スキャンする。ずれの実害は**トーストが一度空振りする**ことだけ | **現状の整理で決着**。起動時に走査しない設計 (148 の骨格) を変える方が高い |

### C. 判断が割れうる (ユーザーに聞く。5 件)

ここは実測だけでは決まらない。**推奨は付けるが、要相談**。

| 項目 | 論点 | 推奨 |
|---|---|---|
| **閾値 10GB は妥当か** | 現在 84.3GB。掃除しても npm/go/Xcode は数日で GB 単位に戻る。10GB だと**掃除直後を除いてほぼ常時鳴る** | **20GB へ引き上げ**。上位 4 項目がどれか 1 つ育った状態を拾う水準。ただし「鳴ってほしい頻度」は好みなので要相談 |
| **再通知の抑止 3 日は妥当か** | 閾値と対で効く。閾値が低いままだと 3 日おきに鳴り続ける | **閾値を上げるなら 3 日のままでよい**。閾値を据え置くなら 7 日へ |
| **小物の同型キャッシュを載せるか** | 実測: `typescript` **216MB** (起票時 31MB から 7 倍)、`electron-builder` と `deno` は**消滅**。`ms-playwright` 477MB は既に載っている | **`typescript` だけ単独で載せる**。「合計 N MB」1 行にまとめる案は、消滅しているものを含む器を作ることになるので却下 |
| **サービス診断を起動時トーストの対象にするか** | 件数しか無く、`lastNotifiedAt` の抑止と噛み合わない。「壊れた登録 1 件」で**消すまで永久に鳴る** | **対象外のまま決着**。doctor を開いたときだけ出す |
| **BTM の残骸を独立した検出項目に昇格させるか** | 取得は可能 (A 節)。だが**形式が非公開**で macOS 版間の安定性が不明。かつ `legacy agent` の**消し方を提示できない**可能性がある | **昇格させない**。「検出できるが直せない」項目は行を増やすだけ。`launchctl print` の `penalty box` と同じ扱い (取れなければ情報なしで続行) の補助情報に留める |

### D. 別 issue が妥当 (3 件)

| 項目 | 理由 |
|---|---|
| `speech-model-cache` をカタログに載せるか | **この機に存在しない** (`com.apple.SpeechRecognitionCore` / `SpeechModelCache` とも無し)。`lsof` 判定 + 削除後に音声機能が壊れない実測が要る。**実測できないので起票もできない**。trigger: 実機に現れたとき |
| `~/src/**` のビルド成果物・`node_modules` | プロジェクト単位の判断が要る。カタログ (allowlist) の枠組みに合わない |
| プロセス側の診断 | 148 の 5 章が「外部に正本が無いので自分で記録を作る」= 常設の追跡機構が要ると結論。実測 (629 件の誤検出 / `pane_pid` の限界) を前提として引き継ぐ |
| `cli_health.go` を doctor 画面に統合するか | 148 の時点で「しない」方針。統合するなら起動時トーストの挙動を変えないことが条件 |

## 受け入れ条件 (この issue 自体の)

- [ ] 各項目に**決着**が付いている (採用 / 却下 / pending へ移動のいずれか)
- [ ] 却下したものは**理由つき**で残っている (次の audit が同じ提案を再生成しないため)
- [ ] カタログへ足したものは、`~/.claude/rules/` の規律どおり
      **実測してから登録**されている (「消したまま戻らないこと」を確かめる)
