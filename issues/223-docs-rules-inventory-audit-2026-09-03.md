# 223 docs: `_claude/rules/` の統合・削減の監査記録 (却下理由と未検証を残す)

起票日: 2026-09-03
種別: docs (記録。単独で着手する作業は無い)
出典: workflow `audit-claude-rules` (15 体。分析 6 クラスタ → 反証 8 → 統合) と、
その結論に対する敵対的レビュー (opus)
関連: `CLAUDE.md`「`_claude/` を触るとき」/ `_claude/CLAUDE.md`「Issue管理」の retro 項 /
`issues/done/165-docs-prompt-audit-2026-09-02.md`

## なぜこの記録が要るか

**この監査自体が、2 日前に却下された提案を再生成した。** `pending-issue-rationale-in-code.md` の
コード例 14 行を rationale へ移す案は `issues/done/165:329-333` で既に「本文に移すべき実例が無く、
空の rationale は置かない」と決まっていたのに、workflow の 4 軸がそれを踏まずに再提案した
(反証が先行決定を見つけて主部を却下した)。却下理由を残さないと同じことが起きる。

## 採った結論: 個別の統合ではなく方針 2 つ (D)

行数の内訳ではなく**流入速度**で決めた。git の実測:

| 日 | 本数 | 行数 |
|---|---|---|
| 2026-07-01 | 13 | 626 |
| 2026-08-01 | 18 | 850 |
| 2026-09-01 | 34 | 2035 |
| 2026-09-03 | 37 | 2480 |

**65 日で 4 倍、直近 3 日で +445 行**。監査が出した削減案は合計約 150 行 (反証後の実数は 142〜146 行)
= **約 1 日ぶんの流入**でしかなく、個別に適用しても同じ週に元の行数へ戻る。よって:

1. **書式の方針** (`CLAUDE.md`「`_claude/` を触るとき」): `## やること / やらないこと` と
   `## 関連` はルール節に無いものだけを置く。実測で**この 2 節が 691 行 = 全体の 27%**
   (`やること` 系 36 ファイル 465 行 / `関連` 35 ファイル 226 行)。**節を指す参照は 0 件**。
   触ったファイルで順次適用する (一括改変はしない)
2. **流入のゲート** (`_claude/CLAUDE.md`「Issue管理」の retro 項): retro の切り出し先は
   「既存ルールへの追記」を既定にし、新規ルールは**発動点が既存のどれとも違うと言えるときだけ**。
   実例・実測は最初から rationale へ書く (後から移す形は失敗している)

## 却下した主張 (次の監査が再生成しないため)

| 却下された主張 | 理由 |
|---|---|
| 「削減 43 行」を統合の採用根拠に数える | 行数は根拠にならない (`verify-design-intent-before-refactor.md`)。`claude-md-layer-prompt` の統合根拠は「判定の正本が 2 ファイルに割れている」ことだけ |
| `pending-issue-rationale-in-code` のコード例 14 行を rationale へ移す | **`issues/done/165:329-333` で既に却下**。コード例は規範の適用見本で、`issues/done/123` / `128` の照合に使われている。空の rationale は置かない |
| ✓/✗ 節の間引きは「軸 4 (重複した再掲)」に該当する | 軸 4 は文書間の重複。同一ファイル内の要約は規約上は規範なので、rationale への退避対象ではない (採用根拠は「冗長」であって「規約違反」ではない) |
| ✓/✗ で残すべきは 2 件 | 実照合で **6 件**。過少 (`shell-numeric` の最弱部テストと一括書き換え禁止 / `no-osascript` のスクショ成果物と label マッチ禁止 / `verify-interactive` の静的 pin / `no-mixed-width` の「桁は合っていると説明し返さない」) |
| `issue-creation-codex-review` の本文は skill の再掲 | **起動機構については真だが、「聞きたいこと」4 項目は `codex-review/SKILL.md` に 0 件**で本ルールが唯一の出典 |
| `mutation-verify-new-tests` の実例は rationale に既にある (8 か所の大半) | 実際は **3 件のみ**。残り 5 件は書き足しが必須 (trap / テーブル駆動 / AA 字形表 / Cmd の副作用 / 削除パネル) |
| `move-report-conclusions-to-issues` の表とコード例は取り残し | **意図して残された** (`c7531627` / 165 の M3 が処理済み)。表 1・2 行目は偽陽性抑止の規範で、落とすと正当な `tmp/` 参照を直しに走る |
| `claude-md-layer-prompt` と `claude-md-maintenance` の正本分裂は「誤判断を生む実害」 | **両ファイルとも毎セッション全文読まれる**ので、片方だけ読む状況が構造上存在しない。コストは 57 行の重複であって誤判断ではない |

## 未検証 (残っているもの)

- **反証にかけたのは 8 候補中 7 件**。落ちたのは `commit-with-pathspec` の shrink
  (「`pathspec は cwd 相対` 節の実測ログを rationale へ寄せ、検出手順を hook 前提に圧縮する」)。
  構造化出力のリトライ上限で失敗した。未反証
- **28 候補のうち何件が 165 で既に「やらない」と決まっていたかの全数**。1 件しか確認していない
- **`_claude/rules/*.md` の実トークン数**。199,613 bytes / 98,450 文字 (CJK 50,127) から
  **5〜7 万トークン級**と推定しただけで、tokenizer は回していない

## 実害として残っている 1 件 (行数と無関係)

`rules-rationale/mutation-verify-new-tests.md` の「ルール本文から移した実例」節 (22 行目以下) が
**文頭の切れた断片集**になっている (`:26` 主文なし / `:31` `**` が閉じだけ / `:43` 親項目なしの
インデント / `:49-52` 「開いた窓は 2 つ」の項目 1 が欠落 / `:89-92` 末尾が `(関連:` で切れている)。
過去の移送 (`c7531627`) がこの形で失敗した跡。**本文から rationale へ実例を移す作業をするなら、
先にここを直さないと同じ断片が増える** (方針 2 が「最初から rationale へ書く」を要求するのは
この失敗が理由)。

## 受け入れ条件

- [x] 方針 2 つを明文化した (2026-09-03)
- [ ] 断片化した `rules-rationale/mutation-verify-new-tests.md:22-92` を直す (着手は任意。
      実例の移送をするときの前提条件)
