# 131 retro: issue 103 の作り直しと BSD/GNU 方言の構造化 (2026-08-28)

起票日: 2026-08-28

対象コミット: `5af141f` (103 の作り直し) / `062dc26` `3b15045` `4877257` (CI 赤の追随) /
`c1383b9` (方言ゲートの新設)。依頼は「101 / 124 / 111 / 109 / 103 を順に修正、issue ごとに
敵対的レビュー」で、その最後の 103 から始まった。

## やったこと

103 の敵対的レビューが「最初の修正では原症状が閉じていない」と実証 → 作り直し → push →
CI (Linux) が 2 度赤 → BSD/GNU 方言の事故を 3 件潰す → 再発を止める静的ゲートを新設。

---

## 1. 自分のテストが production の形状を一度も試していなかった

103 のレースハーネスは毎回 `mkdir "$dir"` で**取り残しを事前に作って**いた。この形状では
両プロセスが奪取経路に入るので、奪取権 lock で直列化されている経路しか検査しない。
production の呼び出し元 3 本はどれも **lock dir が存在しない状態から始まる**ので、片方が素の
`mkdir` で入り、奪取権 lock を持たないまま owner を記録する — そこが原症状の窓だった。

「30 回で 1 度も二重取得を観測しない」を「窓が閉じた」と読んだが、実際は
**ハーネスが窓を作れていなかった**。レビューの指示に「窓が閉じたからか、ハーネスが作れて
いないからか」を入れていたのに、自分では区別できていなかった。

- 素の窓は実測 4.6ms しかなく、production 形状でも二重取得は 2/30 でしか出ない
  (確率的にしか落とせない)。`tt_proc_starttime` を 0.4s 遅らせて窓を広げたら
  **ガード無し 15/15 / 有り 0/15** の決定論的な A-B が取れた

**切り出し先の提案**: `_claude/rules/mutation-verify-new-tests.md` の「よくある『守っていない
テスト』の形」に 1 項目。「**フィクスチャが production の初期状態と違う**と、その経路を
構造的に一度も踏まない。並行・レースのテストでは『呼び出し元がどの状態から始まるか』を
実装側で確認してから形状を決める」。加えて「窓がミリ秒なら**人為的に広げて決定論にする**
(確率的な green は変異検証の当たりも不安定にする)」。

## 2. 同じファイルの 100 行上にある helper を探さずに重複を作った

mtime 取得のために `tt_mtime_epoch` を新設したが、**同じ `guards.sh` に既に `tt_mtime_of` が
あり、正しい順序 (GNU 優先) と 2026-07 の実測注記まで書いてあった**。しかも私が書いた方は
順序が逆で、Linux で壊れる側だった。CI が赤になって初めて気づいた。

`claude-md-maintenance.md` は「共通 helper を**新設・抽出する**ときは実装前にその領域の
CLAUDE.md を読む」と言っているが、今回は CLAUDE.md ではなく**同じファイルの中**にあった。

**切り出し先の提案**: `claude-md-maintenance.md` の該当トリガー行に半行足す。
「共通 helper を足す前に、**まず同じファイル/同じ lib 内を helper 名と用途で grep する**
(規約の参照より先に、実物の重複を見る)」。新規ルールは作らない (既存の一項の拡張で足りる)。

## 3. BSD/GNU 方言を 1 セッションで 3 件踏んだ → 静的ゲートで構造化した

`stat -f %m` ×2 (guards.sh / schedkeys の prune) と `date -r <epoch>` ×1。**3 件とも正しい形と
注記が repo 内に既にあった**。運用では止まらないと判断して `scripts/check_platform_dialect.sh`
を新設し `make test-lint` に配線した (`c1383b9`)。

計測しないと間違える点が 1 つあった: 密着形 `stat -f%z` は GNU で `%` が invalid option になり
**stdout が空**なので、フォールバックが正しく効く。危険なのは**空白区切りだけ**。
「`stat -f` は全部だめ」にすると動いている既存 4 箇所を壊すところだった。

**切り出し先の提案**: **却下** (対応済み)。ゲートとテスト、`tests/CLAUDE.md` の新節で閉じている。
ルール文書には書かない (`comment-no-restate-enforced.md`: 実装が強制していることを再掲しない)。

## 4. 検証ハーネスを zsh で回して、実装のバグと誤診しかけた

`tt_lock_sweep_stale` の `.steal` 回収が「効いていない」と読んだが、原因は
**Bash ツールのシェルが zsh** (ユーザーの profile で初期化される) で、zsh の glob が
ディレクトリに `/` を付けたため `$d.steal` が別パスになっていたこと。lib は冒頭に
「前提: 呼び出し元は bash」と書いてある。`bash -c` で測り直したら正しく動いていた。

`| head` 越しに `$?` を読んで GNU `date -r` の rc を誤読しかけた件も同じ日にあった
(こちらは `verify-execution-not-just-exit-code.md` が既に扱っている既知の形)。

**切り出し先の提案**: `tests/CLAUDE.md` に 1 行。「**bash 前提の lib を手検証するときは
`bash -c` で回す**。このリポジトリの対話シェルは zsh で、glob・配列・単語分割が違う
(実例: zsh の MARK_DIRS で `*.lock` が `foo.lock/` になり、`$d.steal` が別パスを指した)」。
ルール化はしない (dotfiles 固有の環境事情なので、領域の入口に置くのが適切)。

## 5. 敵対的レビューが 2 度、私の「直った」を覆した

103 では「修正が原症状を閉じていない」を実測つきで示し、その修正過程では**既存テスト**が
私の入れた退行 (mkdir 不能を rc=2 でなく rc=1 に化けさせ、装置不在を無音にする) を捕まえた。
指摘は 5 件とも実コードで裏を取ってから採用し、示せなかった 1 件 (奪取権 lock の TTL 奪取に
よる多重取得) は「未確認リスク」として issue に残した。

**切り出し先の提案**: **却下** (記録のみ)。CLAUDE.md「レビュー方針」の
「『指摘なし』は『正しい』ではない」がそのまま効いた実例。

---

---

# 後半 (同じセッション): CI の macOS 移行 (issue 133)

対象コミット: `f3875bb` → `7b3eb61` → `93b47ee` → `c246fa9`。「Linux はサポート対象外」という
ユーザー判断を受けて、tests.yml と lint.yml を macOS runner へ移した。

## 6. 「緑になった」を完了と読みかけた — 敵対的レビューが 2 本の安全機構テストの消失を発見

移行後の CI が green になった時点で、私は完了として報告する寸前だった。敵対的レビューが
**ubuntu run と macOS run の全行 A-B** を取り、`tests/claude/test_deny_bare_tmux_kill.sh` が
`timeout(1)` 不在で**丸ごと skip し、60 件の assert が消えたのに `[ok]` として集計されていた**
ことを見つけた。守っている対象は本番 tmux サーバ誤 kill の再発防止ゲートで、失った中には
「入力長で hook が timeout に殺されて deny が消える」回帰テストも含まれていた。

自分でも 1 本 (`test_fork_scratch.sh` の Darwin skip) は見つけていたが、**探し方が「skip しそうな
ガードを grep する」で、レビューの「両 run の出力を全行突き合わせる」より弱かった**。

**切り出し先の提案**: `_claude/rules/verify-execution-not-just-exit-code.md` に 1 項目。
「**実行環境を変えたら、変更前後の出力を全行 diff する**。件数 (テストファイル数・assert 数) が
同じでも中身が入れ替わっていることがある。skip はファイル単位では `[ok]` に見えるので、
緑の比較では検出できない」。今回は「96 ファイル / 96 ファイル」で同数だったのに
`✓` 行が 68 件消えていた。

## 7. 自分の A-B 実験が汚染され、2 回測って初めて気づいた

`_zshrc` の stdout 汚染を直したあと、修正前後を A-B したら**両方とも「汚染あり」**という
矛盾した結果が出た。原因は `$ZDOTDIR` をケース間で使い回したこと。ケースごとに新規
ディレクトリを作って測り直したら、修正後 stdout 0 行 / 変異版 2 行と決定的になった。

危なかったのは、**最初の 1 回だけ測っていたら「修正が効いていない」と誤診して、効いている
修正を作り替えていた**こと。矛盾に気づけたのは、その前に単発で測った結果 (stdout 1 行) と
食い違ったから。

**切り出し先の提案**: `mutation-verify-new-tests.md` の「復元の作法」の隣に 1 項目。
「**A-B のケース間で状態を共有しない** (HOME / ZDOTDIR / 一時ディレクトリはケースごとに
新規作成する)。前のケースが残した状態が次のケースに効くと、A と B が同じ結果を返して
『変異が検知できない』と誤診する」。

## 8. `| head` 越しに exit code を読んで、既知の罠を再発させかけた

GNU `date -r <epoch>` の終了コードを `gdate ... | head -2` の後の `$?` で読み、rc=0 と誤読した。
`verify-execution-not-just-exit-code.md` が名指ししている形そのもの。stdout/stderr/exit code を
分けて測り直したら rc=1 で、フォールバックが正しく発火することが分かった。

**切り出し先の提案**: **却下** (既存ルールで足りる)。ルールは存在し、内容も正確で、今回は
測り直して自力で気づけた。増やすべきはルールではなく、測るときの手癖。

## 9. 番号を 1 回の走査で決め、以降インクリメントして衝突を作った

132 を起票する直前に走査して最大 131 を得たあと、**133 は再走査せずに書いた**。その間に
並行セッションが 133 を取っており衝突した。並行セッションの retro (137) と対になる観測で、
先方の「write 時の検査では遅すぎる」という結論を、この事実で反証できた
(番号が頭の中で決まっていても、**実体化するのは write の瞬間**で、その時点で相手のファイルは
既にコミット済みだった)。

**切り出し先の提案**: **却下** (137 側で扱う)。事前衝突検出は先方が issue 化を検討中で、
二重に起票しない。

## 10. 並行セッションとの「反証」のやり取りが、双方の結論を訂正させた

追認ではなく反証を投げ合った結果、先方の結論が 1 回覆り (write 時では遅すぎる → 遅くない)、
数字が 2 件訂正され、こちらの数字も 1 件 (検出時間 ~3 時間 → 1:17) 訂正された。
帰属の特定は双方とも失敗し、「**schedkeys 群を自分の作業だと名乗るセッションが 1 つも無い**」
という事実だけが残った (共有ツリーで誰が何をしたか事後に特定できない)。

**切り出し先の提案**: **却下** (記録のみ)。CLAUDE.md「レビュー方針」の「主張は証拠ではない」が
セッション間でも効いた実例。

---

## 残課題

- [ ] 項目 6 → `verify-execution-not-just-exit-code.md` へ追記 (環境を変えたら出力を全行 diff)
- [ ] 項目 7 → `mutation-verify-new-tests.md` へ追記 (A-B のケース間で状態を共有しない)
- [ ] 項目 8・9・10 は却下 (理由は各項に記載)
- [ ] 項目 1 → `mutation-verify-new-tests.md` へ追記
- [ ] 項目 2 → `claude-md-maintenance.md` のトリガー行へ半行追記
- [ ] 項目 4 → `tests/CLAUDE.md` へ 1 行追記
- [ ] 項目 3・5 は却下 (理由は各項に記載)
