# 247 retro: doctor の UX 監査 P3 (issue 242) を片付けたセッション (2026-09-04)

起票日: 2026-09-04
対象セッション: issue 242 の P3 5 件の実装 + 敵対的レビュー対応 + 245 / 246 の起票
成果: commit 5 本（P3 の実装 / レビュー対応 / issues / 改番）、issue 2 本起票、242 を done へ

## 反省

### 1. issue 番号を採番してから push するまでに時間を空け、衝突した

`244` を書き上げてから push しようとしたら、その間に別セッションが同じ `244` を push していた
（`aa9014ce`）。改番（244 → 246）と参照の直し、commit message の食い違いの説明で 1 commit 余計に使った。

- **規範は既にある**（`_claude/rules/worktree-per-session.md` の「issue 番号は採番したら即 push する」）。
  守れなかった理由は「本文を書き上げてから push したい」という気持ちで、**空ファイルで番号だけ
  押さえる**のが正しかった
- 🚨 **番号だけ先に押さえられなかった構造的な理由もある**: push はブランチ単位なので、
  実装 commit が未検証（make test 前）の状態では issue だけを飛ばせない。今回は
  「実装を先に commit → issue を採番 → push」の順にしたため、採番から push まで
  make test 1 回ぶん（約 7 分）空いた
- **切り出し先の提案**: `worktree-per-session.md` の該当項に「本文は後でよい。空ファイルで
  番号だけ押さえて push する」「実装が未検証で push できない時間帯は、採番も遅らせる（先に
  採番して抱えると衝突する）」の 2 行を足す。**新規ルールは立てない**（発動点は既存項と同じ）

### 2. 変異を perl で当てて壊し、「全テスト red」を変異の検知と誤読しかけた

絵文字を含む置換を `perl -0pi -e '…\x{1F6AB}…'` で当てたところ、`Wide character in print` の
警告とともにファイルが壊れ、**変異と無関係なテストまで全部 red** になった。
`sed` で当て直したら、狙ったテスト 2 本だけが red になった。

- **規範は既にある**（`mutation-verify-new-tests.md` の手順 1.6「変異の diff を目視し、意図した
  箇所だけが変わっていることを確認する」）。今回は**出力の tail だけ見ていて先頭の警告を見落とした**
- 🚨 この形の怖さは「**red は出る**」こと。ビルドは通る（手順 1.5 では捕まらない）ので、
  「変異を検知した」と読んで先へ進める
- **切り出し先の提案**: `rules-rationale/mutation-verify-new-tests.md` に実例として追記。
  ルール本文は既に手順 1.6 を持っているので**本文は変えない**

### 3. worktree の未コミット変更で rebase が 2 回止まった

`git rebase origin/master` が `cannot rebase: You have unstaged changes` で止まり、
先に commit する順序に組み替えた。`git stash` は禁止なので選択肢は「commit してから rebase」しかない。

- **提案なし**（規範どおりに動いた結果で、コストも小さい）。ただし
  「レビュー中はファイルを書き換えない」と「rebase したい」が競合するので、
  **レビューを投げる前に rebase を済ませる**のが順序として正しかった

## うまくいったこと（残す価値のある形）

### 4. 着手前に並行セッション全員へ一斉に問い合わせたら、重複作業を着手前に止められた

`git worktree list` に 4 つの worktree があり持ち主が分からなかったので、`ListAgents` で
見えた dotfiles 系 4 セッション全部に「触る関数」を列挙して送った。結果:

- あるセッションが**既に push 済みの 240 / 241 をこれから実装しようとしていた**ことが分かり、
  着手前に止められた（そのセッションは裏取りの上で自分の worktree を破棄した）
- 別のセッションが `src/doctor/**` に閉じてくれ、`src/glogx/**` を私が持つ分担が即決まった
- 「commit の author では持ち主を特定できない（全セッション同じ author）」ことも共有された

**切り出し先の提案**: `parallel-write-agents-need-worktree-isolation.md` に
「**着手前に、触る予定の関数を列挙して並行セッション全員へ送る**（worktree の持ち主は
git state からは分からない）」を 1 行足す。既存項の「他人の worktree を判断する前に
`ListAgents` で見る」は**事後**の話なので、**事前**の申告はそこに並べる形になる。

### 5. 敵対的レビューが「自分の修正が改悪だった」を見つけた

P3-1 で結末語を `doctorOutcomeWord` に寄せたが、レビューが
「下見の `Skipped` は blocked 由来なので、一覧の `🚫 対象外` と食い違う。しかも直下に出る理由が
『いまは対象外です』で、1 行の中で語と理由が矛盾する」と指摘。実コードで裏を取ったら**そのとおり**で、
`Skipped` は一覧と同じ語へ戻した（`Failed` だけ分ける形が正解だった）。

- 元の issue（242 の P3-1）の指摘は「**Failed** を『対象外』と呼ぶのは規律から外れている」であって、
  Skipped は問題ではなかった。**指摘の範囲を超えて直したのが改悪の入口**だった
- 変異検証は全部 red で通っていた（自分が想定した不変条件しか試していないため）

**提案なし**（`adversarial-review-own-safeguards.md` の「変異を全部 red にした後でも敵対レビューは
想定の外側の指摘を出す」がそのまま当たった事例。ルールは既にある）

### 6. issue 本文の「（機能上は効く）」を鵜呑みにせず実測した

242 の P3-3 には「下見中も中断は機能上は効く」と書かれていたが、裏付けが無かった。
使い捨てのテストで実測したところ**効くことは確認できた**が、その先の結末が壊れていた
（理由が空の尻切れ + 「消せるものがありません」表示）。これが 246 の起票につながった。

**提案なし**（CLAUDE.md「issue の記述を鵜呑みにしない」がそのまま効いた）

## 残課題

- [ ] 項目 1 の 2 行を `worktree-per-session.md` へ追記するか（ユーザー判断待ち）
- [ ] 項目 2 の実例を `rules-rationale/mutation-verify-new-tests.md` へ追記するか（ユーザー判断待ち）
- [ ] 項目 4 の 1 行を `parallel-write-agents-need-worktree-isolation.md` へ追記するか（ユーザー判断待ち）
