# 440 (research): pro-con を Claude が使って pro-con を開発する (dogfooding) — 手触りと改善案

起票日: 2026-09-25

親: [415](415-design-claude-pm-worker-orchestration.md)

## 概要

Claude (対話の session) が PM の役をして、pro-con で PG を起動し、pro-con 自体の開発を回す。使ってみた手触りと改善案をここに書く
(2026-09-25 にユーザーが依頼: 「pro-con を Claude 自身が立ち上げて使った場合、手触りとか改善案とかを別の issue に書いて」)。

## やり方

- PM の役は Claude が CLI (`pro-con card add / plan / answer / close`) で行う (PM を pro-con が起こす仕組みはまだ無い = 437)
- 1 枚目のカードは 428 (attach 中の指示をカードに残す) の予定。`--limit 1`。PG は `claude --bg -w pc-c-NNN` の worktree で作業し、
  Claude がレビューして cherry-pick → make test → push する
- PG が pro-con のコードを書いている間、Claude は pro-con のコードを触らない (PM に徹する)

## 1 回目 (2026-09-25 16:40〜17:25。カード C-002 = issue 428。手で起動した dispatcher `--limit 1`)

経過: 依頼 → 分解 → PG 起動 (約 3 秒) → PG が約 14 分作業 → `pro-con card run C-002 -- make test` (約 6 分、rc=0) → 結果つきで再開 →
レビュー待ち → Claude (PM) が diff を読み、master へ cherry-pick → make test → push (84cfc4e3) → カードを完了。
PG は変異 13 本・敵対的レビュー 1 周を自分で回し、残り (実機の E2E 未実施・入れ替わりの穴・改行が詰まる) を 428 に正直に書いた。

### 手触り

- ✓ 依頼を置いてから記録に出るまで 1 秒未満 (socket で即時)。分解してから PG の起動まで約 3 秒
- ✓ テストの係が効いた: PG は make test を自分で回さず頼み、結果つきで再開された (PG の turn が長時間ブロックしない)
- ✓ PG は規律どおり自分のブランチまで push し、master には push しなかった
- 🚨 **終了で止まったものを「止まっていない」と数え続けた** (実バグ)。作業を終えた session は `claude stop` 後も state が `done` のまま pid 無し。
  → 01dbb3b0 で直した (止まった = pid 無し かつ working でない)。直した版の `--stop` は 1 秒で ok
- ✗ PM の CLI にカードを読む口が無い: `card add` は受付の箱の依頼 ID だけを返し、カード ID (C-002) が分からない。状態・PG の出力・質問は
  cards.json を python で読んだ。状態の変化も 5 秒ごとに読んで待った → [442](../441/done/442-feat-pro-con-card-list-show-wait.md)
- ✗ **PM がレビューで差し戻す口が無い**: 操作は add / plan / ask / answer / review / close / run / guide だけで、レビュー待ちのカードに
  直してほしい点を渡して PG を再開する操作が無い。今回は取り込めたが、直してほしい点 (下の「履歴の順」) を PG に返せなかった
- ✗ 完了にしたカードの PG の session が止まらない (state done・pid ありの idle のまま残る)。終了のときには止まるが、それまでプロセスが残る
- ✗ 朝に試した「ping」のカード (C-001) が PM 不在のまま「依頼」の列に残っている (437 の穴の実例)
- △ PG の再開後の session の名前が AI の付けた題 (`test success log`) になり、`claude agents` でどのカードの PG か分からない
- △ 手で起動した dispatcher は SIGTERM から抜けるまで 59 秒かかった (止める処理をもう 1 回試してから抜ける。確かめる段の約 30 秒)
- △ 状態の置き場に改名前の `daemon.lock` が残っている (害は無いが、どちらが今の lock か紛らわしい)
- △ `claude agents` の state に 425 の表に無い `done` が出た (作業を終えた session)

### 改善案

- [x] レビューの差し戻し: `pro-con card rework <カード> "<直してほしい点>"` (レビュー待ち → 作業中へ戻し、同じ PG を回答つきで再開する) → 446
- [x] カードを完了・却下したら、その PG の session を止める (dispatcher の close の適用で) → 447
- [x] カードを読む口 → 442。外の Claude から覗く口 → epic 441
- [x] PG の session の名前を再開の後も `pc-c-NNN` にそろえる (`--resume` で名前を付け直せるか要実測) → 488
- [x] レビューで見つけた小さな点: attach の指示は打った時刻のまま履歴の末尾に足すので、履歴が時刻の順に並ばなくなりうる (428 の実装) → 489
- [ ] stop の直後に `--resume` すると、`claude stop` が rc=0 を返した後でも「まだ実行中」と見てコピーを起動し、`note: session <id> is already running in the background, so this started a copy as <new>. …` を `backgrounded · …` の行の前に出した (488 の実測で 2 回とも。Claude Code 2.1.282)。🚨 **未確認**: 実測は stdout と stderr をまとめて取ったので、`note:` がどちらに出たかは分からない。stdout なら `launcher.go` の `parseBackgrounded` (stdout の 1 行目だけを読む) が「出力を読めない」で失敗するのにコピーは立つ。本物の PG の再開は通っているので stderr の見込みが高い。確かめるなら 2 つの流れを分けて取る (未起票)

## 2 回目 (2026-09-25 17:45〜18:40。カード C-003 = issue 436。ユーザーが見本 C・1000 ms を選んだ)

経過: PG が約 9 分で本体とテスト → テストの係 (make test) → 再開 → 自分の敵対的レビューで「画面が低いと下への揺れで画面が 1 行増える」を
見つけて直す → 2 回目のテストの係 → レビュー待ち → Claude が e2e モードで揺れを撮って見本と比べ、cherry-pick → make test → push (88d22852)。

- ✓ PG は見本のコード (tmp の python) を仕様として読み、揺れの式と丸め (偶数丸め) まで揃えた
- △ 見張り: 再開の途中でカードが一瞬「分解済み」に戻るので、「作業中でなくなったら終わり」で待つと早く抜けた (終わりの列を名指しで待つ)。
  7d の `card wait` (442) が入ったので 3 回目からはそれで待つ
- △ make test が無関係の検査 (tests/codex_fanout.bats の時間切れ) で 1 回落ちた (単独では 3 回とも緑)。7d に直しを頼んだ
- ユーザーが `--view` (見ているだけの画面) を求めた → epic 441 の最小の形として入れた (c6e558b2)。ユーザーが dogfooding を横から見られる

## 3 回目 (2026-09-25 18:45〜。カード C-004 = issue 446 = レビューの差し戻し)

- ✓ `card add` がカード ID を返し (7d の 442)、`card show` で状態と依頼の原文が読めた (1 回目に python で cards.json を読んだ所が解消)
- ✓ PG が実装 → 変異 5 本 → テストの係に make test を頼む → 結果で再開 → review まで 10 分で自走した。PM がしたのは依頼・レビュー・取り込みだけ
- ✓ PM のレビュー: diff を読み、テストの係の記録 (runs/C-004-*.log、FAIL 0 行) を見て取り込んだ (cf67cf7e)。cherry-pick の衝突は 446 の本文だけ
  (PM の書き戻しと PG の進捗が同じ位置に入った。コードは衝突なし)
- ✓ dispatcher を止めて `--limit 2` で起こし直したら、作業中だった C-005 は続きから再開し (新しい session)、C-006 は新しく起動した。
  止める → 再開を本物の PG で通したのはこれが初めて
- △ **テストの結果を待っている間も「作業中」と出る**。PG の turn は終わっていて、動いているのはテストの係だけ。ユーザーが「C-004 を作業中なの?」と聞いた。
  「テスト待ち」のように、PG が止まっていて何を待っているかが列か見出しで分かるとよい (改善案。未起票)
- △ **分解の中身がカードにしか残らない形で頼んだ**。pm-guide の役目 3 は「issue に分けてからキューに積む」で、カードは issue を指すだけのはずだが、
  PM (私) は実装の注意 (並行セッションとぶつかる所・テストの作法) を `card add --request` に書いた。`--request` は人間が書いたままの原文の欄で、
  PM の指示を入れると原文と区別できない。直し方の候補: 分解と注意は issue 本文へ書く / PM から PG への指示は `--prompt` 欄に分ける (ユーザーの質問で気づいた。未決)
- △ 枠 (`--limit`) を変えるのに dispatcher を止めて起こし直すしかない (止めると作業中の PG も止まる)。起動中に変えられる口があるとよい (改善案。未起票)

## 4 回目 (2026-09-25 18:54〜。C-005 = 447 / C-006 = 444 を 2 本並べる)

- ✓ 2 本が並んで動いた。C-005 は 9 分でレビュー待ちに来た (実装 → 変異 5 本 → テストの係に make test → 再開 → review)
- ✓ **レビューで差し戻した (446 の rework を本物で初めて使った)**: 閉じた次の Tick で止まったのを確かめると、実際は pro-con が止めたのに
  履歴が「既に止まっていた」になる所を、`card rework` で戻した。同じ会話の PG がすぐ再開した
- △ **PG の報告 (出力の末尾) が途中から英語になった**。C-004 までは日本語で、C-005 の make test の前後から英語。issue の本文は日本語のまま。
  PG への指示に「報告は日本語」が無い (PM の CLAUDE.md の言語設定は PG に効いていないか、途中で外れる)。差し戻しの文に「日本語で」を足した。
  PG の起動の指示に言語を入れる案 (未起票)
- ✓ C-005 (447) の取り込みの後、閉じたら PG が止まるのを本物で確かめた (C-006 を閉じて c938b1a7 が pid 無しに。447)
- ✓ C-006 (444) は差し戻して 447・443 の上へ rebase させた (notes の型が変わる同士)。PG が衝突を解いて close.go も出来事に揃えた。取り込んだ
- 訂正 (3 回目の「分解の中身がカードにしか残らない」): **`card add --prompt` が最初から PG への指示の欄だった** (`dispatcher.Prompt` は Prompt が有れば
  「指示:」として渡し、無ければ依頼の原文を渡す)。PM (私) が `--request` に指示を書いていただけ。C-007 (437) / C-008 (451) から
  `--request` = ユーザーの原文・`--prompt` = PM の指示 に分けた。usage の `--prompt <PM に渡した指示>` という書き方が紛らわしい (直す候補。未起票)
- PG の報告が英語になる件の仮説: PG は `claude --bg … --setting-sources project,local` で起動し、`"language": "日本語"` を置いた
  ユーザーの settings.json を読まない。**未確認** (確かめるなら、同じ起動で言語の設定だけを変えた A-B)。今は指示に「報告は日本語」と書いて回している
  - **A-B の結果 (2026-09-25 dotfiles-7d。Claude Code 2.1.282・haiku・隔離した cwd・`--settings '{"disableAllHooks":true}'` で user の hook を切る)**:
    仮説は `-p` では**確かめられなかった** (user の settings.json を読んでも英語になる)。英語の依頼「Summarize in one sentence what a git worktree is.」への返答:

    | 読む層 | 言語の設定 | 返答 (2〜3 回) |
    |---|---|---|
    | `--setting-sources project,local` (PG と同じ) | 無し | 英語 × 3 |
    | `--setting-sources user,project,local` (user の `"language": "日本語"` を読む) | user の settings.json | 英語 × 3 (hook を切らない版も英語 × 2) |
    | 既定 (`--setting-sources` を付けない) | user の settings.json | 英語 × 2 |
    | `--setting-sources project,local --settings '{"language":"日本語"}'` | `--settings` で言語だけ | **日本語 × 3** |

    日本語の依頼には、どの条件でも日本語で返した。→ `-p` では user の settings.json の `language` は応答の言語に効いていない (効いたのは `--settings` で渡したときだけ)。
    🚨 **`--bg` は測れていない**: 隔離した cwd では「Workspace not trusted」で起動を拒む (信頼を与えるには対話で `claude` を開いて承認し、`~/.claude.json` に書く必要があり、
    そこまではしなかった)。PG は `--bg` なので、`--bg` では振る舞いが違う可能性は残る。確かめるなら、信頼済みの repo の worktree (PG と同じ置き場) で `--bg` の A-B を取る
  - 直し方の候補 (案と理由まで。dispatcher.Prompt の変更は C-007 の後):
    1. **`--settings` で言語だけ渡す** (`claude --bg … --setting-sources project,local --settings '{"language":"日本語"}'`): `-p` で効くことを確かめた唯一の形。
       ユーザーの settings.json の他の設定 (hook・許可) は読まないまま、言語だけを足せる。言語の値は config (または user の settings.json の `language`) から読んで渡すと、日本語の決め打ちにならない
    2. **起動の指示 (dispatcher.Prompt) に「報告・質問は日本語で」を入れる**: 今の差し戻しの文でやっている形。確実だが、指示の文が長くなり、言語を変えるにはコードを変える
    → 1 を既定にし、1 が `--bg` で効かないと分かったら 2 を足す (--bg の A-B を先に取る)
- 4 回目で PM (私) がしたこと: 依頼 3 枚 (437 / 451 は PG へ、452 / 453 は起票のみ)・レビュー 2 枚・差し戻し 2 回・取り込み 2 回・dispatcher の入れ替え 1 回

## 5 回目 (2026-09-25 20:21〜。PG 4 本)

- △ **PM (私) が衝突を見積もらずに 4 本を並べた**。C-007 (437) と C-010 (457) はどちらも「終了のときに何を止めるか」を変えるのに順番を付けず、PG に「review の前に rebase」とだけ頼んだ。
  ユーザーの指摘で気づいた。順番を dispatcher に伝える口も無い → 468
- ✓ PM 用の使い捨ての見張り (カードの状態の変化・`git merge-tree` で master と PG どうしの取り込みの衝突・15 分ごと) で、C-007 × C-008 (dispatcher.go・README) を
  レビューに来る前に見つけた。取り込みは来た順に 1 本ずつ (C-008 → C-007 は差し戻して待ち)
- ✓ PG を 4 本に増やした (ユーザーの依頼。利用枠は 5 時間 52% / 週 24%。dispatcher の自動の絞り (80% で 1 本) に任せる)。枠を変えるのに dispatcher を止めて起こし直した
  (C-007 のテストの係の make test が切れて頼み直しになった。456)
- △ 重い処理が直列になっていない: 同じ時刻に repo 全体の make test が 2 本 (テストの係と 7d の session)、PG 4 本の変異の検証が並び、ロードアベレージ 25 / 14 コア → 471
- △ C-009 は 23 分作業中だったが、画面には「裏で走っています」の 1 文だけ。PM が手で調べると変異の検証 (13 分経過) と敵対的レビューを待っていた → 473 (今走っているもの) / 467 (ログ) / 469 (進捗)
- △ C-011 の make test が 17 秒で rc=0。PG が `src/pro-con` で頼んだので pro-con だけのテストだった (取り込むときに repo 全体で回す)。どこで走らせたかが出ない → 469
- ✓ 監査もカードで PG に出した (C-014: リソースリーク。コードは触らず issue だけ書く)
- 分担を決めた (ユーザーの選択): 437 が入ったら、依頼の分解 (と PG の質問への回答) は自動の PM、レビュー・差し戻し・閉じる・master への取り込みは人間の代わりの Claude

## 6 回目 (2026-09-25 21:00〜)

- ✓ **利用枠の絞り (stage 5) が本物で効いた**: 5 時間の枠が 80% に達すると、dispatcher が同時に動かす PG を 1 本に絞り、分解済みの 4〜5 枚を起動・再開しなかった
  (出来事に hold が残る)。21:39 のリセットで 0% に戻ると自分で 4 本に戻した。ユーザーの評価「ratelimit に合わせて消費を抑えるムーブはいいね」→ 475 (見張りの係)
- ✓ 見た目の直し (472・436 の連打・枠のちらつき) は、ユーザーの依頼で PM (人間の代わりの Claude) が自分で実装した (C-015 は削除で取り下げた = 451 の実機の確かめ)
- ✓ 自動の PM が本物で初めて動き、C-001 を 9 秒で閉じた (437)
- △ C-012 (438) は、先に入れた C-011・C-013 と dispatcher.go でぶつかったので、取り込みを止めて差し戻した (rebase は PG に任せる)
- △ **`pro-con card close` は箱に置いた時点で rc=0 を返し、適用で弾かれても分からない**。C-016 (issue を付けずに積んだ) の close が「紐づく issue も終わり方も無い」で
  除けられたのに、PM (私) は rc=0 を見て「閉じた」と報告した。`card add` のように適用を待って結果を返すか、`card wait` を組み合わせる運用にする (改善案。未起票)
- ✓ 監査もカードで回せた: C-014 (リソースリーク) が母集合の数え方・一時の module での実験・全数勘定まで PG だけで出した (477〜479)

## 2026-09-25 23:53 の macOS のクラッシュからの復旧 (session dotfiles-4c)

- macOS がカーネルパニックで落ちた (原因は tmux のクライアントがカーネルのメモリを漏らすこと。global の issue 500 に起こした)。PG 4 本・PM・dispatcher が全部落ちた
- 復旧で踏んだこと: `--view` の画面は dispatcher を起こさず、カードは「作業中」のまま見えるだけだった / クラッシュ前の session は `failed`・pid 無しで残り、
  `--stop` が止め直しを続けて抜けなかった (482) / 手で付けた `--limit 4` が残っていなかった / 取り込みの係が消え、レビューの列に 8 枚溜まって後ろの 5 枚も止まった (487)
- 直したもの: 482 (failed を止まった形に) / 483 (起動時に自動で復旧) / 487 (取り込みの係を dispatcher が起こす) / C-029 の `config set limit` (上限が再起動で残る)
