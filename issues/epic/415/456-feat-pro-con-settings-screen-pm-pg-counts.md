# 456 (feat): 設定画面 — PM の数と PG の枠を人間が画面から変える (スケーリングの判断は PM、上限は人間)

起票日: 2026-09-25

親: [415](415-design-claude-pm-worker-orchestration.md)

## 概要

ユーザーの依頼 (2026-09-25): 「スケーリングの判断は pm に任せるはずだけど、人間から pm の数を変更できるような設定画面も作ってね」。
今は PM の数を持つ仕組みが無く (437 が 1 つで始める)、PG の枠は dispatcher の起動引数 `--limit` だけで、変えるには dispatcher を止めて
起こし直すしかない (止めると作業中の PG も止まる。440 の 3 回目で踏んだ)。

## 期待する動作

- 画面に設定画面を足し、人間が **PM の数** と **PG の枠 (同時に動かす PG の上限)** を変えられる。動いている dispatcher を止めずに、次の Tick から効く
- 役割の分け方: **どこまで使うか (スケーリング) は PM が決め、人間が決めるのは上限**。PM は上限の中で、キューの長さや利用枠を見て使う数を決める
  (今は dispatcher が `--limit` と利用枠の cap で決めている。PM に判断させる口は 437 の後で設計する)
- 利用枠による絞り (80% で 1 本 / 95% で 0 本。stage 5) は人間の上限より優先する (上限を上げても枠を超えて起動しない)
- 変えた値は状態の置き場に残し、dispatcher を起こし直しても続く。`--limit` を渡したときとの優先を決める

## 足す: 役ごとのプロセスの一覧 (2026-09-25 のユーザーの依頼)

「起動中の role ごとに claude-code や dispatcher などのプロセス一覧も欲しいね。設定画面にあるといいかな？」

- 設定画面の中を「見る所」と「変える所」に分ける。見る所 = **役ごとのプロセスの一覧**: dispatcher / PM / PG / テストの係 (実行中のコマンド) / 要約の係 (haiku) / 画面 (presence の印)。
  それぞれ pid・起動からの経過・状態 (busy / idle / 止まっている)・担当のカード・今のコマンドを出す
- 今あるのは `s` の PG の一覧だけ (担当カード・状態・実行中のコマンド・経過・pid)。それを役ごとに広げる形でもよい
- 🚨 出すのは pro-con が起動したものだけ (README の `s` の行の「pro-con の外の session は名前も本数も出さない」を守る。`no-unauthorized-branch-switch.md` の系)
- 見る所は読むだけ: `--view` でも出す。集めるのは dispatcher か読む口の側 (画面が数秒ごとに ps を重く回さない)。473 (今走っているコマンド) と同じ出どころを使う

## 対応方針 (候補)

- 書き手は dispatcher だけ (426 の決定 1): 画面は受付の箱に「設定を変える」依頼を置き、dispatcher が状態の置き場 (dispatcher-state.json の隣) に書いて使う
- 画面のキーと設定画面の形は glogx の UI の決まり (`docs/glogx-ui-guide.md`) に合わせ、見本で候補を並べてから本体へ
- CLI (`pro-con config …` など) でも同じことができると、PM と外の Claude からも使える

## 関連

- 437 (PM を起こす。PM の数はここで 1 つから始める) / 415 の論点 6 (PM の数と役割) / 455 (枠に何を数えるか) / 440 (3 回目: 枠を変えるのに止めるしかない)

## 進捗: 画面ではない部分 (C-029。2026-09-26)

- `pro-con config set limit <n> | set pm 1 | unset <名前> | show`: 受付の箱に kind=config を置き、dispatcher が Apply で状態の置き場の `settings.json` に書いて次の Tick から使う (起動し直しても続く)
- 上限の優先: 利用枠の絞り > 設定 > `--limit` > 既定 2 (README に記載)。dispatcher-state.json に `limit_from` (設定 / 起動の引数) を足した。settings.json が壊れていたら `--limit` で動き、理由を様子の Why に出す
- PM の数は 1 だけ受ける (0 と 2 以上は除ける)。1 しか受けないので dispatcher はまだ読まない (415 の論点 6 の後で配線する)
- `pro-con ps [--json]`: dispatcher (lock の pid) / PM・PG (起動の記録) / テストの係 (実行の印) / 画面 (relay の印)。`ps` を 1 回読むだけで、状態の置き場に書かない・dispatcher の lock も presence の印も触らない。busy / idle と要約の係 (haiku) はまだ出さない
- 残り: 設定画面そのもの (見た目。取り込みの係)

## 足す: このアプリが使っているディスクの使用量と内訳 (2026-09-26 のユーザーの依頼)

「設定画面にこのアプリで使っているディスクの使用量と内訳を書いて欲しい」。設定画面の「見る所」に、合計と内訳を出す。

2026-09-26 13:50 の実測 (`du -sh`):

| 置き場 | 大きさ | 中身 |
|---|---|---|
| PG・役の worktree (`<repo>/.claude/worktrees/pc-*`) | 1.5GB | 48 個・1 個あたり 32〜38MB。カードを閉じても消さない (447) ので増え続ける |
| pro-con が起動した session の transcript (`~/.claude/projects/*worktrees-pc-*`) | 408MB | 51 個 |
| 状態の置き場 (`~/.local/state/pro-con`) | 5.2MB | うちテストの係のログ (`live/runs/`) 4.5MB・87 本、`cards.json` 236KB、`events.jsonl` 260KB |
| pro-con のバイナリ (`src/pro-con/pro-con`) | 13MB | |

- 数える対象は「pro-con が作った物」だけ (起動の記録・カードの worktree の名前 `pc-*`・状態の置き場)。repo の本体や、pro-con の外の session の transcript は数えない
- 🚨 **集めるのは重い** (worktree 48 個の `du` で数秒)。画面が描くたびに走らせない: 設定画面を開いたときに裏で 1 回測り、測った時刻を添えて出す (手で測り直すキーを持つ)。
  集める口は `pro-con ps` と同じく読むだけにして、`pro-con du [--json]` のような CLI でも出す (外の Claude・PM が読める)
- 表示は置き場ごとの大きさと件数、合計、多い順。worktree はカードごとの内訳 (完了したカードの worktree がどれだけあるか) を引き出しで見られると、片付けの判断に使える
- 片付け (完了したカードの worktree を消す) はこの issue の範囲外 (破壊的な操作なので別の issue で決める)。ここは見るだけ
