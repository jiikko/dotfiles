# 499 (ux): 完了のレーンをスクロールできるようにし、スクロールバーを出す

起票日: 2026-09-26

親: [415](415-design-claude-pm-worker-orchestration.md)

## 概要

ユーザーの依頼 (2026-09-26、カード C-059): 「完了レーンでスクロールできるようにして。スクロールバーも出してね」。

## 今の形 (2026-09-26 に読んだ)

- レーンは先頭から入るだけの枚数を描き、入らない分は最後の 1 枚の代わりに「… 他 N 枚」と出して打ち切る
  (`src/pro-con/ui/view.go` の列を描く関数と `columnCells`: `items[:min(limit, len(items))]`)。先頭を動かす (オフセット) 仕組みが無い
- 選択は列の全部のカードを動ける (`ui/model.go` の `moveRow`: `min(row+delta, len(cs)-1)`)。**見えている枚数より下へ選択を進めると、選択中のカードが画面に出ない**
- スクロールバーの部品は既にある: `src/tuikit/layout/scrollbar.go` の `Scrollbar(rows, width, total, offset, colored)`。詳細の引き出し (`ui/drawer.go`) が使っている
- 完了のカードは書庫へ移るまで (478) と、完了から 1 週間で自動で消えるまで (497 / C-057) は完了のレーンに溜まる

## 期待する動作

- 完了のレーンで、選択を下へ進めるとレーンの中身が追ってスクロールし、選択中のカードが常に見えていること
- レーンに入らない枚数があるときは、レーンの右端にスクロールバーを出す (`tuikit/layout` の `Scrollbar` を使う。見た目は引き出しと揃える)。
  入り切るときは出さない
- 「… 他 N 枚」の行をどうするか (スクロールバーで置き換えるか、残すか) は決める

## 決めること

1. 完了のレーンだけか、全部のレーンにするか (列を描く関数は全レーンで共通。依頼は完了のレーンだけを名指ししている)。
   全部にするなら、移動中のカードの枠 (`m.moves`) と揺れ (`bump.go`) がスクロールした位置で正しく描かれるかを確かめる
2. スクロールの単位 (1 枚ずつか、半ページか) と、ホイール (マウス) も受けるか
3. 見た目が今の引き出しのスクロールバーと変わるなら、本体に入れる前に ./tmp のサンプルレンダラで見本を出して人に選んでもらう (decide-layout-in-sample-renderer-first)

## 関連ファイル

- `src/pro-con/ui/view.go` (列を描く関数・`columnCells`) / `src/pro-con/ui/model.go` (`moveRow` / `stepRow` / `moveByMotion`)
- `src/tuikit/layout/scrollbar.go` / `src/pro-con/ui/drawer.go` (スクロールバーを使っている前例)

## 関連

- 478 (書庫) / 497 (完了から 1 週間で消す) / 436 (端でぶつかったら揺れる)

## 実装 (2026-09-26、C-059)

決めたこと (見た目は引き出しのスクロールバーと同じ部品・同じ字形なので、サンプルは出さずに入れた):

1. **全部のレーン**に効かせた。列を描く関数は共通で、完了のレーンだけの特例にするとその分岐が要る。実際に溢れるのはほぼ完了のレーン
2. 単位は **カード 1 枚**。選択が見える範囲を出たぶんだけ先頭を動かす (半ページ・先頭・末尾の移動も同じ追従で見える所へ寄る)。ホイールは受けない (今の画面はマウスを受けていない)
3. 「… 他 N 枚」の行は**スクロールバーで置き換えた** (残りの量と位置はバーが見せる)

形:

- `src/pro-con/ui/lanescroll.go`: レーンごとの先頭 `m.tops`。`followSelection` を `Update` の出口 (枠の `trackCursor` より先) で毎回呼ぶので、キー・カードの移動・画面の大きさの変化を同じ経路で拾う。読むときは `laneTop` が末尾を越えないよう寄せる。タブの切り替え (`resetSlots`) で捨てる
- 溢れるレーンはカードの幅を `layout.ScrollbarWidth` ぶん狭め、右端に `layout.Scrollbar` (行で数える: 1 枚 `perCardLines` 行)。選択の枠もカードの幅まで狭め、バーは枠の外に残す
- 枠と移動中のカードの位置 (`slotXY`) はスクロールした先頭を引いた、見えている位置。見える範囲の外の枠は出さない
- テスト: `ui/lanescroll_test.go` (末尾まで進めても選択と枠が見える / 先頭へ戻せる / バーは溢れるときだけ・末尾で thumb が下端)。追従を外す・バーを外す変異でそれぞれ落ちることを確かめた
