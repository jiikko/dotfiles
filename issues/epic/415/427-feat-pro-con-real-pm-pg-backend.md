# 427 (feat): pro-con の本物の PM / PG の backend (段階 3〜5)

> 🚨 **担当中: dotfiles-5c**（2026-09-24〜）

起票日: 2026-09-24

> 🚨 2026-09-25 に `pro-con daemon` を役割どおり **`pro-con dispatcher`** (割り振り係) に改名した (コード・コマンド・画面・状態のファイル名)。
> この issue の以前の記録の「daemon」は dispatcher のこと。`pro-con daemon` は別名として残してある

親: [415](415-design-claude-pm-worker-orchestration.md) の「段階」3〜5

## 概要

模擬 (`src/pro-con/fake`) で UI とつなぎ込みを確かめた機能を、本物の Claude Code の session で動かす。
[425](done/425-research-claude-bg-remaining-measurements.md) の実測と [426](done/426-design-pro-con-open-decisions.md) の決定は済んだ (2026-09-24)。

## 範囲 (段階ごとに分けて入れる)

- [x] 段階 3: 受付 PM 1 つ + カードとキュー + dispatcher (上限固定・手動起動)。質問への回答 UI と watchdog もここで入れる (2026-09-25 に閉じた。下の「段階 3 の小分け」と「敵対的レビュー」)
- [x] 終了の扱い (2026-09-25 にユーザーが依頼。「全部止めて再開できる形に」を選んだ): 画面を終了すると daemon と pro-con が起動した PG を止める
  (`pro-con daemon --stop` と同じ)。作業中のカードは分解済みへ戻して続きから再開する文を持たせ、質問待ちは列のまま。動いている PG があれば確認を挟む。
  止めるのは daemon だけ (画面は止める印を置いて daemon が止め終えるのを待つ。daemon が居なければ自分で daemon の役を取る)。
  止めた印 (card.Stopped) があるカードは、次の再開で自動の再開を待たずに (止めずに) 再開する。変異 10 本が red
  - [x] 本物の claude で通した (2026-09-25 09:56、PG 1 本): 作業中の PG (sleep 150 中) で隔離した tmux の画面に q → 確認のダイアログ
    (「動いている PG があります (作業中 1 本・質問待ち 0 本) PG と daemon を止めて終了しますか?」) → y →「daemon と PG を止めています…」→
    daemon が 1 秒で止め終えて抜け、画面も閉じた。PG の session は stopped、カードは分解済み + 続きから再開の文 + Stopped。
    daemon を起動し直すと 1 秒で再開し (新しい session)、PG は続きをやってレビューへ進んだ。後片付け: session 2 本を claude rm、状態の置き場を退避
  - [x] 敵対的レビュー 1 周目 (Opus、a7bb683f) の全数: P1 1 / P2 5 / P3 2 = 8 件。対応 7 / 記録のみ 1:
    - **P1** 起動・再開の直後でまだ記録に無い PG を止め漏らし、止めたと報告する → 止める前に記録への登録を済ませ、一覧に出るまで待って止める
    - P2 daemon が止め損ねて / SIGTERM で抜けても成功と報告する → daemon は止めた結果を stop-result に書き、頼んだ側はそれを読む
    - P2 落ちて自動の再開を待っている PG (pid 0) を止め漏らし、次の起動で二重に立ちうる → 戻るのを待って止める (約 46 秒)。戻らなければ列を変えず、止めきれなかったと返す
    - P2 止める直前に PG が置いた質問・完了の依頼を失う → 止める前に箱を適用する
    - P2 結果の分からない起動・再開の印を消して二重に再開しうる → 立っていれば取り込んで止める。取り込めなければ印を残す
    - P2 daemon が居ないとき画面が自分で止めていて、ctrl+c で閉じると途中で切れる → 止める処理は常に別プロセス (`pro-con daemon --stop`) で走らせる
    - P3 時間切れの後に止める印が残り、後で daemon が止めに入る → 時間切れで印を取り下げる
    - 記録のみ P3 daemon の起動時に古い印を捨てる処理と、ちょうどその瞬間に置かれた印が重なると捨てられる (窓はロックから数行。--stop は 60 秒待って時間切れを返す)
    変異 7 本が red (箱を適用しない / 登録しない / pid 0 を待たない / 結果の分からない起動・再開を見ない / 戻らないのを止めた扱い / 結果を読まない / 印を取り下げない)
  - [x] 敵対的レビュー 2 周目 (Opus、26d9bb9a) の全数: P2 1 / P3 4 = 5 件。対応 4 / 却下 1:
    - P2 待ちの途中で自動の再開により新しい pid で戻った PG を「既に止まっていた」として止めず、成功と報告する →
      止めるときは、同じ session id の bg session を pid が違っても自分の PG として止める
      (同じ session id で pid だけ違うのは自動の再開。外の shell の --resume は別の session id を立てる = 3f)。警告は全部の周で残す
    - P3 待ちの判定の時刻が Shutdown の冒頭で固定 → 周ごとに取り直す
    - P3 時間の予算が合わない (Shutdown の待ち約 46 秒 + 一覧の取り直しが --stop の 60 秒を超えうる) → --stop 120 秒・画面 150 秒
    - P3 daemon が居ないときに自分で止めた --stop が stop-result を書かない → 書く
    - 却下 P3 一覧から消えた PG (DeadSince が 0) を止まったと読む: 3f の実測では落ちた PG は pid 無しで一覧に残り、一覧から消えるのは止まった / 消された session
    変異 3 本が red (新しい pid で戻ったのを止めない / 時刻を取り直さない / 別の session id も止める)
- [x] 段階 4: リソースの直列化 (PG が 2 体以上になると要る)。2026-09-25 に閉じた (本物の claude で通し、敵対的レビューで P1 / P2 が出なくなった)。2026-09-25 にユーザーが方針に合意 (426 の決定 5 の形):
  - [x] PG は make test 等を自分で走らせず `pro-con card run <カード> -- <コマンド>` で頼んで turn を終える (store の run: 作業中のカードだけ、結果を返す前の 2 本目は除ける)
  - [x] daemon (テストの係) が PG の worktree で 1 本ずつ順に実行する (裏の goroutine。Tick は止めない)。待っているカードには順番 (WaitResource) を出す。
    ログは状態の置き場の runs/。実行はプロセスグループごと止められる (取り消し・1 時間の時間切れ)
  - [x] 終わったら結果 (rc・所要・成功 / 要約・ログの末尾・ログのパス) を持たせて分解済みへ戻し、dispatch が同じ session を再開する。失敗のときだけ haiku が要約する
    (要約できなければログの末尾だけ)。daemon が実行の途中で止まっていたら、次の daemon は「結果が無い」を渡して再開する
  - [x] テストの係を待っている間は watchdog が停滞にしない。終了で止めるときは実行中の 1 本を取り消し、頼みも取り下げる (再開した PG が続きから頼み直す)
  - [x] PG への最初の指示に規律を足した。変異 9 本が red (直列でない / cwd を渡さない / 成功も要約 / 要約を渡さない / 途中で止まった実行を知らせない /
    停滞にする / 終了で取り消さない / 2 本目を受ける / -- の位置を見ない)
  - [x] 本物の claude で通した (2026-09-25 10:28、PG 1 本): PG が `card run` で `false` を頼む → daemon が worktree で実行 (rc=1、haiku の要約つき) →
    再開した PG が `echo ok` を頼む → rc=0 → 再開した PG が review、まで約 36 秒。見つかったこと: 出力の無い失敗のログを haiku に渡すと、
    要約の代わりに「ログを貼って」と返ってきて、それが PG に届いた → 出力が無ければ要約させず「出力なし」と渡す (変異 1 本が red)
  - [x] 敵対的レビュー (Opus、ef75c033 + d5e8227e) の全数: P1 1 / P2 2 / P3 3 = 6 件。全部対応した:
    - **P1** カードが作業中の列を離れても (質問 / 落ちて止めた) テストの係への頼みが残り、結果を捨て、回答で再開した PG を「結果が無い」で止めて再開し直す
      (レビューで再現) → 作業中の列を離れる遷移では必ず頼みを取り下げる (card.DropRun に寄せ、store の遷移と stopCrashing と Shutdown から呼ぶ)。
      そのカードの実行中の 1 本は取り消す
    - P2 daemon が Tick のエラー / SIGTERM で抜けると実行中のコマンドが孤児で残る (最小の再現で 20 回中 2 回) → どの出口でも CancelRun。
      実行が終わった後もプロセスグループの残りを SIGKILL で止める
    - P2 `card run` の文字列は daemon の権限で permission の外で走り、別のカードの名前でも頼める → 頼んだシェルの cwd を載せ、
      そのカードの PG の worktree からの頼みだけを実行する。信頼の前提 (PG に `pro-con card` を許す = その worktree で任意のコマンドを許す) を README とコードに書いた
    - P3 cancelRun の待ち 10 秒の後に SIGTERM を無視する子が残る → 上の SIGKILL で止める / ログを丸ごと読む → 末尾 64KB だけ読む /
      終了のテストが取り消しの有無を見ていない → 偽の実行が取り消しを見たかを確かめる
    変異 5 本が red (store の遷移で取り下げない / 列を離れても実行を取り消さない / 頼んだ場所を照らさない / 落ちて止めても取り下げない / 取り消さない)
  - [x] 次の周の敵対的レビュー (Opus、63b50369): P1 0 / P2 2 / P3 2 = 4 件。全部対応した:
    - P2 daemon が SIGHUP (端末・tmux のペインを閉じる) / SIGKILL で死ぬとコマンドが残り、次の daemon が同じ worktree で 2 本目を走らせうる →
      SIGHUP も受けて取り消す。実行のプロセスグループをカードに記録し (Exec.PGID。書き手は Tick だけ)、次の daemon は、グループの先頭が
      まだそのコマンドを実行している bash のときだけ止めてから「結果が無い」を渡す (pid の使い回しで別のものを撃たない)
    - P2 worktree の下のディレクトリから頼んだ正当な頼みを断る → worktree かその下なら受け、頼んだ場所で実行する
    - P3 孤児を止める処理にテストが無い → 本物のシェルで、SIGTERM を無視する子 / bash の後に残る子 / 違うコマンドは撃たない、を確かめるテスト
      (撃たれた子はゾンビで残り kill(pid, 0) が成功するので、生死は Wait の完了で見る)
    - P3 結果を渡すときに頼みを手で消していて RunCwd が残る → DropRun を通す
    変異 7 本が red (実行後にグループを止めない / 違うコマンドも撃つ / 下のディレクトリを断る / 頼んだ場所で実行しない / pgid を記録しない /
    残った実行を止めにいかない / 手で消す)。🚨 SIGHUP を受けること自体はテストで固定していない (daemon の常駐のループにテストが無い)
  - [x] 次の周 (Opus、f9bcece9): P1 1 / P2 1 / P3 1。全部対応した:
    - **P1** 残った実行の照合を `ps` の argv (`/bin/bash -c <command>`) で行っていたが、bash は `-c` の中身が単純なコマンドなら exec で置き換わるので
      (実測: `/bin/bash -c 'sleep 63'` は ps で `sleep 63`)、普通の頼みでは必ず外れて止められず、同じ worktree で 2 本が重なっていた →
      実行ごとの印を bash の $0 (`pro-con-run:<実行の id>`) に載せ、中身の後ろに `exit $?` の行を足して exec を防ぐ。次の daemon は印で探して止める。
      環境変数の印は使えなかった (この macOS の ps は他のプロセスの環境を出さない = 実測)
    - P2 プロセスグループの番号をカードに書く前 (最大 3 秒) に daemon が死ぬと止められない → 印 (Exec.RunID) は実行を始める前に記録する。番号の記録は外した
    - P3 改行・末尾の空白を含むコマンドは照合が外れる → 印の照合はコマンドの文字列を見ないので当たらなくなった
    変異 4 本が red (exec を防がない / 印の違う実行も撃つ / 印を記録しない / 止めにいかない)
  - [x] 次の周 (Opus、bfb91d27): P2 1 / P3 4 = 5 件。対応 4 / 記録のみ 1:
    - P2 印を引数の最後に置いた他のプロセス (`pgrep -f <印>` 等) のグループまで撃つ → 撃つのは `/bin/bash -c` で、かつグループの先頭 (pid = pgid) の行だけ
    - P3 `\nexit $?` の連結で、末尾のバックスラッシュ・閉じていない here-doc のコマンドの意味が変わる (実測) → コマンドは環境変数で渡して `eval` する
      (連結しない。eval は組み込みなので bash は exec せず印が残る = /bin/bash 3.2 で実測。足していた `exit $?` の行は要らなくなったので外した)
    - P3「終了コードは変わらない」はシグナルで死んだときに成り立たない (bash が 128+n で返す。前の形は -1) → 記述を直し、テストに足した
    - P3 テストの印が固定の名前で、並行するテストと撃ち合う → テスト名と pid を混ぜた
    - 記録のみ P3 旧版の daemon が走らせていた実行は RunID が無いので、新版に切り替えた直後の 1 回だけ止められない (切り替えの前に旧版を `--stop` すれば避けられる)
    変異 3 本が red (bash の形を見ない / 連結に戻す / exec させる)。偽物のテストは、偽物自身が exec されて印が消える形だと候補にすらならず緑のまま
    だった (変異で発覚) ので、exec されない形の偽物にした
  - [x] 次の周 (Opus、67c39865): **P1 / P2 なし** → 段階 4 の周回を閉じた。P3 3 件:
    - 構文エラーの終了コードが 2 → 1 (eval を通すため) → 記述とテストの表に足した
    - 記録のみ: 頼まれたコマンド自身が `exec` を含むと印が消えて、daemon が落ちた後に止められない (前の形でも同じ)。
      setsid / Setpgid で自分のグループを作った子孫も止められない。runScript の注記に書いた
    - 撃つ条件の `-c` と「グループの先頭」をテストが固定していない → 偽物を 2 つ足した (`bash -x -c` / 先頭ではない `bash -c … 印`)。変異 2 本が red
  - 🚨 訂正: その commit (subject「テストの係の撃つ条件を偽物のプロセスで固定し」) のメッセージの「make test 緑」は誤り。make test は
    tests/tmux/test_tmux_toast.sh (再入ガード) で落ちていたのを見ずに push した (`;` で繋いでいて止まらなかった)。pro-con とは無関係で、単独では 3 回とも緑
- [x] 画面の開閉と daemon (2026-09-25 にユーザーが依頼。415 の「TUI と常駐プロセスを分ける」を書き直した):
  - 画面 (本物のモード) を開くと、daemon が居なければ別のプロセスグループで起こす (出力は状態の置き場の daemon.log)。
    もとの設計にあったが実装されていなかった (画面だけ開くと、依頼が箱に溜まったまま進まなかった)
  - 閉じるのは Q → `quit` と打って enter だけ。q / ctrl+c の 1 打では閉じない (ctrl+c は終了の入力欄を開く。入力の途中なら書きかけを消さない)。
    y/N の確認のダイアログは外した (quit と打つことが確認)。入れ替え (ctrl+r) では終了の入力欄を引き継がない
  - 操作の結果の通知は 6 秒で消す (消す仕組みが無く、次の操作まで残っていた)。起動時の知らせは esc で消すまで残す。
    箱に置いたときの文は「受け付けた (daemon が適用するとカードに出る)」
  - 変異 8 本が red (quit 以外でも閉じる / Q で開かない / 入力の途中の ctrl+c で書きかけを消す / q で閉じる /
    通知を消さない / 数え直さない / 起動時の知らせを時間で消す / daemon が居ても起こす)
- [x] e2e モード (2026-09-25 にユーザーが依頼。「本物の daemon + 偽の PG」を選んだ): `pro-con --e2e <dir>` と、Claude が操作する口 `pro-con e2e`
  (start / keys / text / screen / wait / stop / scenario。画面は置き場ごとの隔離した tmux サーバ)。PG と PM は台本どおりの偽物 (daemon/e2e.go)。
  `scenario` が依頼 → 偽の PM が分解 → 偽の PG が質問 → 画面で回答 → 再開 → テストの係 (`echo e2e-ok`) → 再開 → レビュー → Q → quit を約 30 秒で通す
  (隔離サーバ・socket・プロセスは残らない)。tests/pro-con/test_e2e_scenario.sh が make test の中で回す
  - 作って見つかった本物のモードの穴: global で受けた依頼は repo が無く、PM が `card plan --issue` で分けても repo が付かないので、
    daemon が起動先を決められずに止まっていた → plan のとき repo の無いカードには最初の issue の repo を付ける
  - 変異: 偽の PM を外す / plan で repo を付けない、の 2 本で通しのテストが red。偽の PG の台本と偽の PM の単体テストもある
  - 敵対的レビュー (Opus、3df893ee): P2 4 / P3 5。P2 は全部と P3 2 件を直した:
    - P2 `--e2e X daemon --stop` のように、`--e2e` / `--mock` をサブコマンドの前に付けると、サブコマンドが本物の置き場で動く (本物の daemon と PG を止める / 本物の claude を起こす)
      → サブコマンドの前に付いていたら拒む
    - P2 e2e の画面の attach が本物の `claude attach` を起動する → e2e では偽の attach (`pro-con fake-attach`)
    - P2 scenario の起動の待ちが失敗すると後始末されない / e2eStop の出口 (画面が先に落ちていた・隔離サーバを止めた) で daemon が残る
      (daemon は画面と別のプロセスグループ) → 起動を試みた時点で後始末を張り、どの出口でも最後に `daemon --stop --e2e` と socket の片付け
    - P3 socket を消したかをテストが見ていない → 通しのテストで控えが残らないことを見る / 実行の印が e2e と本物で同じ形 → 状態の置き場のハッシュを混ぜた
    - 記録のみ P3: 画面が起動直後に落ちると socket のパスを控え損ねる / e2e で本物の通知を出していないかはテストで観測できない / e2eStop の socket のパスの確認は守りとして効いていなかった (外した)
    - 変異 3 本が red (サブコマンドの前の --e2e を拒まない / 差し替えた attach を使わない / socket を消さない)。🚨 e2eStop の最後の daemon --stop はテストで固定していない (正常の出口では画面の quit が先に止めるので観測できない)
- [x] 入力欄の日本語入力 (2026-09-25 にユーザーが依頼。`n` の入力欄で、変換中の文字が入力欄から外れた所に出ていた):
  IME は変換中の文字を端末のカーソルの位置に出すが、画面がカーソルを置いていなかった (描画の差分を書き終えた位置に出る)。
  入力欄を開いている間は、端末のカーソルをキャレットに置く (桁は表示のセル数。全角は 2)。キャレットの印 `▏` は本物のカーソル (bar) に置き換えた。
  e2e モードの隔離 tmux で `cursor_x,cursor_y` を実測: `n` → (50,47) = 入力欄の行、「日本語ab」→ (58,47)、← 2 回 → (56,47)、esc → カーソルを隠す。
  変異 4 本が red (カーソルを置かない / 幅を文字数で数える / 行を最下段にする / ボードでも出す)
- [x] 段階 5: 自動スケーリング (2026-09-25。ユーザーの「続きを実装して」)。滞留の側は dispatch がもともと見ている (分解済みのカードが無ければ起動しない / 上限まで古い順)。
  足したのは枠の残量 (dispatcher/usage.go):
  - `claude -p --no-session-persistence --setting-sources "" /usage` を 5 分ごとに状態の置き場で読む (モデルを呼ばない。約 4 秒)。
    🚨 `--no-session-persistence` が無いと読むたびに transcript が 1 本残る (実測。5 分ごとで 1 日 288 本)。`--setting-sources ""` で user の hook を走らせない (レビューが隔離した CLAUDE_CONFIG_DIR で hook の起動の有無を実測)
  - 5 時間と週 (全モデル) の大きい方が 80% 以上で同時 1 本、95% 以上で 0 本 (動いている PG は止めない)。読めなければ --limit のまま (理由を画面に出す)。
    読めない間は最後に読めた値を 30 分使う。片方の行だけ読めたら読める側で絞り、読めない側を理由に出す。モデル別の週の枠は見ない (PG のモデルを固定していない。431)
  - 回答を受けた再開の初回を新しい起動より先にする (枠で 1 本に絞ったとき、途中まで進んだ作業を待たせない)。印の残った再試行は古い順 (失敗し続ける再開が枠を占め続けないように)
  - dispatcher は Tick ごと (途中で抜けた Tick も) に `dispatcher-state.json` (store/dispatcher_state.go) を書き、画面のゲージは「PG n/今の数 (上限 m) 理由」と dispatcher の生存を
    そこから出す。🚨 以前は画面の「dispatcher N前」が画面の時刻で、dispatcher が止まっていても 0s前 と出ていた → 1 度も回っていなければ「未起動」、2 分回っていなければ赤
  - 本物の `readUsage` を 1 回呼んで 5 時間 16% / 週 9% を読めた。残ったのは空の memory の dir だけ (transcript 無し)
  - 敵対的レビュー (Opus) 3 周:
    - 1 周目 P2 4 / P3 4: transcript が残る / WaitDelay が無い / 片方の行だけ無いと絞らない / 境界と session 側のテストが無い / 再開が後回し / 初回の理由が空 / 止まった dispatcher の理由 / 読んでいる間の SIGTERM
    - 2 周目 P2 2 / P3 5: 失敗し続ける再開が枠を永久に占める / 片方の行が読めないのが黙る / 未認証の原因の取り違え / 引数が無検査 / 止まった dispatcher の「0/0」
    - 3 周目 P2 2 / P3 1: TMUX の検査が空のループ / 理由の文に長さの上限が無く、後ろの警告が画面外へ出る / 絞ったときの注記と週の欠けが無検査
    - どれも直した。3 周目の修正は判断を足しておらず (表示の切り詰めとテストの強化)、どれも変異で確かめたので 4 周目は回していない
  - 記録のみ: settings の env と plugin も落ちる (認証を settings に置くようになったら別の枠を読む。usageCmd のコメント) / 一覧 (ss) は枠を読む前に取るので、最大 31 秒古い一覧で割り当てる (前からの形。実害は未確認) /
    state が壊れると「未起動」と出る (不変条件の破れとして理由も出る) / 95% 以上では回答済みのカードも再開しない (枠を使うため。ゲージに理由が出る)
  - 変異 (bin/mutate-verify) 44 本が狙ったテストで red (当て方を誤った 3 本は当て直した) (上の各周の直した点と、境界 80 / 95・大きい方・上限 1・理由の有無・state の配線・ゲージ)
- [x] 入力したら即時に割り振る (2026-09-25 にユーザーが依頼: 「socket とかで event 駆動に。入力をしたら即時 dispatch されてほしい」)。
  それまでは、`n` で送った依頼は箱のファイルのまま dispatcher の 3 秒のポーリングを待ち、画面の読み直しも 3 秒ごとだったので、カードが出るまで最大 6 秒ほどかかっていた
  - Unix domain socket (package `wake`、状態の置き場の `dispatcher.sock`。パスが 103 バイトを超える置き場は一時ディレクトリに置き場のハッシュで置く):
    - 箱に置いた側 (store.Submit の 1 か所。画面・`pro-con card`・e2e の偽の PM) が Poke → dispatcher は待ちを切り上げてすぐ回る
    - dispatcher は箱の依頼を適用した直後 (一覧の取得より前) と、何かをした Tick の後に Broadcast
    - 画面 (live backend) は購読していて、知らされたら session の一覧を取り直さずに (前に取ったものを使う) カードを読み直し、ui へ知らせて即時に描き直す
  - socket は速くするための口で、正しさは頼らない: 届かなくても箱のファイルは残り、3 秒のポーリングが拾う。PG の状態・watchdog・利用枠はポーリングのまま (知らせてくれる相手がいない)
  - 実測 (e2e モード、Enter からそのカードが画面に出るまで、3 回ずつ): 前 3751 / 4850 / 4822 ms → 後 126 / 128 / 127 ms (測る側の画面の読み取りの 20 ms 刻みを含む)。
    隔離 tmux・socket・プロセスの残骸なし (置き場が 122 バイトで一時ディレクトリへ逃がす側も通った)
  - 変異 (bin/mutate-verify) 14 本が狙ったテストで red: Poke しない / 起こされても待つ / 適用の直後に知らせない / 何もしない Tick でも知らせる / 知らされた読み直しで一覧を取る /
    購読しない / 画面へ知らせない / 知らせの後に待ち直さない / 知らせで取り込まない / socket の権限 / 長いパスを逃がさない / socket でないファイルを消す / 繋ぎ直さない / ctx で購読を切らない。
    並行のテストは -race で 3 回続けて緑
  - 敵対的レビュー (Opus) 1 周目: 壊す経路 (起きない・起き続ける・socket の奪い合い) は見つからず。テストが本番の配線を守っていない P2 3 / P3 3 → 全部に配線のテストを足した
    (runDispatcher を e2e モードで通す / Init が知らせを待ち始める / 起動した Tick の後の知らせ / Wait が購読を待つ / 空の一覧を取り直さない / 逃がす先を TMPDIR にしない)
- [x] 複数の画面を同じ置き場で開いてよい (2026-09-25 にユーザーが依頼: 「複数の pro-con を起動してもいいように socket とか諸々を設計して」)
  - 止めるのは最後に閉じる画面だけ。画面は開いている間、置き場の `screens/<id>.lock` を flock で持つ (package presence)。閉じるときは `quit.lock` を取って自分の印を外し、
    ほかに持たれている印を数える (0 なら止める)。同時に閉じてもちょうど 1 つだけが 0 を受ける。落ちた画面の flock は OS が外すので、後始末の仕組みを持たない
  - 🚨 最初は socket (dispatcher) に画面を数えさせたが、2 周目のレビューが「dispatcher が居ないと、最初に閉じた画面が全部止める」と指摘 (socket は速くするための口で、
    正しさは頼らない、という自分で置いた原則にも反していた) → 画面の数は presence で数え、dispatcher にも socket にも頼らない形に作り直した
  - 画面の数はゲージに「画面 N」、終了の見出しは「ほかに N 画面が開いているので、この画面だけ閉じる」。開いた・閉じたときは socket の `notify` でほかの画面へすぐ知らせる
  - 3 周目のレビュー P2: 印を作ってから flock するまでの間に、ほかの画面が数えると印を消す (20000 回中 146 回再現) → `.tmp-<id>` で作って flock してから rename。
    O_EXLOCK でも open の中で割り込まれて再現した (3000 回のテストで確かめた)
  - socket のパスは symlink を解いた絶対パスで決め、長い置き場は `/tmp/pro-con-<uid>/` に逃がす (持ち主を確かめる。自分のもので権限だけ緩ければ 0700 に直す)
  - 実測 (e2e モード・隔離 tmux・2 画面): B が開くと A のゲージが 733 ms で「画面 2」、B を閉じると 34 ms で 1 に戻る。B を閉じても dispatcher は残り、
    dispatcher を kill -9 した後でも、ほかの画面が開いていれば止めない。最後の画面は止める (stop-result=ok)。開いた直後の「dispatcher 未起動」は 77 ms で消える
    (dispatcher は最初の Tick の後に必ず知らせる / 購読は繋がるたびに 1 度読み直す / 繋ぎ直しは 250 ms)
- [x] 終了のとき、pro-con が起動した claude が確実に止まることを設計で担保する (2026-09-25 にユーザーが依頼)。不変条件: 最後の画面が閉じ終えたとき
  (または画面が 1 つも無くなってから 1 分後)、pro-con が起動した session で生きているものは 0。止められなければ名指しで報告する。pro-con が起動していない session には触らない
  - 止める相手は、カードではなく pro-con の起動の記録 (registry) が正本 (カードが完了した後も生きている PG・再開で入れ替わった前の session も止める)。
    止めた後に `claude agents --json --all` で確かめ、state が stopped でないものを止め直す (約 30 秒)。残れば「カード (短い id)」で名指しして失敗を返す
  - 実測 (Claude Code 2.1.282、haiku の bg を 1 本だけ自分で起動して止め、`claude rm` で消した): `claude stop` の後、`--all` なしの一覧には出ず、`--all` では
    state: stopped・pid 無しで残り、25 秒後も自動で再開しない。→ 止まったかは `--all` の state で確かめる (`--all` なしの「一覧に無い」は止まった証拠にしない)
  - 画面が起こした dispatcher は `--exit-without-screens 1m`: 画面が 1 つも無い状態が 1 分続いたら、PG を止めて抜ける (最後の画面が quit を通らずに消えた:
    端末を閉じた・落ちた・kill -9)。手で起動した dispatcher (PM が CLI で使う形) には付けない
  - 画面は開いている間、dispatcher が 10 秒以上回っていなければ起こし直す (落ちた / 前の画面が止めている間に開いた = 3 周目の P2)。閉じる途中の画面は起こさない。
    止める頼みは、lock が外れるのを待つ前に止めた結果を読む (止め終えた直後に次の dispatcher が lock を取っても時間切れと読まない)
  - 実測 (e2e モード): 偽の PG が動いている状態で画面を kill -9 → 60 秒後に dispatcher が「開いている画面が 1m0s 無いので、PG を止めて抜ける」→ 偽の PG は stopped、stop-result=ok
  - 実測 (Claude Code 2.1.282、haiku の bg を 1 本だけ自分で起動・kill -9・stop・`claude rm`): 落ちて自動の再開を待っている session (pid 無し・working) に
    `claude stop` → rc=0 で stopped、35 秒後も再開しない → 落ちた PG は再開を待たずに止める (46 秒の待ちが消えた)
  - 敵対的レビュー (Opus) 4〜8 周 (この節の分。1 周ごとに直した差分を攻めさせた):
    - 4 周目 P1: 一覧を 1 回取れないだけで 1 本も止めずに抜ける → 止められる分は止める形 (stopCards に切り出し、どの失敗でも最後の確かめまで進む)。
      止めきれなければ dispatcher は抜けずに止め直し続ける (stopUntilDone)。止めている間に画面が開いたら、止めるのをやめて続ける。
      P2: 再開で入れ替わった前の session が記録から消える → sessions-retired.json に残して確かめる / 止める途中で dispatcher が落ちたら --stop が引き継ぐ /
      SIGTERM で ctx を切られても止めるのは続ける (呼び出しごとに上限)
    - 5 周目 P2: 止め直しのたびに履歴が増える / pid 0 のままの session に Stop を撃たない / 退いた記録が壊れると 1 本も止めない / 最初の失敗の結果の書き出しが無検査 など
    - 6 周目 P1: SIGTERM が画面と dispatcher に同時に届く (ログアウト・pkill) と、閉じる途中の画面を「開いている」と数えて止めずに抜ける →
      5 秒の間、画面が消えるのを待ってから決める。P2: 3 分の上限を 2 段で分け合って最後の確かめに届かない → 段ごとに別の上限
    - 7 周目 P1: 起動・再開の途中で取り込んだ session (記録にまだ無い) の停止が失敗しても ok になる → カードの側で止めようとした session も確かめる段に渡す
    - 記録のみ・未確認の指摘は [439](439-risk-pro-con-shutdown-and-screens-residuals.md) に集めた
  - 🚨 dogfooding (440) で本物の不具合: 作業を終えた session (state: done) は claude stop 後も state が done のまま pid が無くなる。
    state == stopped だけを見ていた判定が止まったものを止め直し続けた (手で起動した dispatcher の --stop が rc=1)。
    → 止まった = pid 無し かつ working でない、に直した (01dbb3b0)。直した版の --stop は本物の置き場で 1 秒・ok。
    偽物がこの状態を持たなかった (止めたら state: stopped にするだけ) ので、テストは緑のままだった
  - 変異 (bin/mutate-verify): この節の分は各周の直しごとに当て、狙ったテストで red を確かめた (当て方を誤った・テストが素通りした分は、テストか fixture を直して当て直した)

## 残っていること (2026-09-25 時点。上の各節から集めた)

- [x] 段階 5 (自動スケーリング) — 上の進捗の節
- [ ] **調べる係 / レビューの係は未実装** (426 の決定 6〜8)。段階の小分けに入っていないので、段階を足してから進める (切り方はユーザーと決める)
- [x] テストの係の信頼の前提: **何でも許す** (2026-09-25 にユーザーが決めた。許可リストにはしない。PG は自分の worktree の中からなら、どのコマンドでも頼める)
- [~] 今日の変更を本物の claude で通す: dogfooding (440) で、手で起動した dispatcher から本物の PG を 3 枚 (428 / 436 / 446) 回した
  (起動・テストの係・結果つきの再開・レビュー待ち・--stop で止まる、は本物で確かめた)。画面が dispatcher を起こす / Q → quit で止める、を
  本物の PG でまだ通していない (e2e モードでは通した)
- [ ] リファクタ候補: PG の session の生死の判定が trackDead・stopCrashing・prepare・Shutdown の stopTarget に分かれている (上の 10 周目の節)
- [ ] 記録のみの P3 (上の各周の「記録のみ」) の trigger が来たら見直す
- 関連: 428 は dogfooding で実装・取り込み済み (84cfc4e3)。431 は 433 (ログイン、人の作業) 待ち
- 2026-09-25 夕方に起票した子: 436 (端でぶつかる演出。dogfooding で実装・取り込み済み 88d22852) / 437 (PM を起こす) / 438 (本物のモードの
  追加オーダー・btw・片付け) / 439 (終了と複数の画面の記録のみ・未確認) / 440 (dogfooding) / 446 (レビューの差し戻し。dogfooding の 3 枚目で作業中) /
  447 (カードを閉じたら PG を止める)。外の Claude から覗く口は epic 441 (--view は入れた c6e558b2 / 442 は 7d が入れた)

## 段階 3 の小分け (2026-09-24 夜に決めた。上から順に進め、済んだら [x] と commit を書く)

- [x] **3a カードの置き場所** (`src/pro-con/store`): daemon だけが書くカードの記録 (`$XDG_STATE_HOME/pro-con/live/cards.json`) と、受付の箱
  (`…/live/inbox/` に 1 件 1 ファイルの依頼)。PM / PG / 画面は箱に置くだけで、適用は daemon の 1 か所 (426 の決定 1)。
  採番と `card.Check` の不変条件の検査も適用の中。枠を使わず単体テストで確かめられる
  - 済み (2026-09-25): `src/pro-con/store` (Submit / Load / Apply)。依頼の種類は add / plan / ask / answer / review / close
    (分解済み → 作業中は daemon の仕事なので 3c)。遷移の規則は `transition` の 1 か所、不変条件は「新しく出た違反」で判定。
    テスト 6 本、変異 4 本が red (二重適用の控え / 不変条件 / 質問待ちでない回答 / 壊れた記録を空と読む)。
    敵対的レビューは 2026-09-25 に 3a〜3d をまとめて通した (下の「敵対的レビュー (3a〜3d)」節)
- [x] **3b `pro-con card` コマンド**: PM / PG が使う口 (`add` 依頼を積む / `ask` 質問を書く / `done` 終えた / `plan` 分けた 等)。箱に置くだけ。枠を使わない
  - 済み (2026-09-25): `src/pro-con/cardcmd.go`。置いた依頼の ID を stdout、使い方の誤りは箱に置く前に rc=2。
    テスト 3 本、変異 2 本が red (--issue の書式の検査 / add の必須の検査)
- [x] **3c `pro-con daemon`**: 箱の適用 → PG の起動 (`claude --bg -w`、431 の設定、`live.Register`) → `claude agents --json` で状態を読んでカードへ →
  回答・追記は stop → resume (426 の決定 2・3) → 落ちた回数で止める (決定 4) → watchdog。起動の部分は本物の claude が要る (枠を使う)
  - [x] 3c-1 分解済みのカードに PG を起動 (上限まで)・記録 (`live.Register`) に登録・回答を受けたカードは stop → resume。起動の口は差し替えられる形にし、偽物で単体テスト (枠を使わない)
    - 済み (2026-09-25): `src/pro-con/daemon` (Tick = 箱の適用 → 一覧に出た PG の登録 → 分解済みへの割り当て)。回答は card.Resume に入り、
      再開で渡したら空にする。起動の口は Launcher で差し替え、本物は ExecLauncher (`claude --bg -w … --setting-sources project,local`)。
      🚨 **ExecLauncher は本物の claude で一度も走らせていない** (3f で確かめる)。store に daemon 用の Update を足した (不変条件の検査は Apply と共通)。
      テスト 6 本、変異 3 本が red (上限 / 再開の分岐 / 起動の失敗で作業中にしない)。敵対的レビューは下の節
    - 登録の穴を塞いだ (2026-09-25): 記録を書き直すのは daemon 自身が起動・再開した直後 (pending) だけ。それ以外で pid が変わったら
      「外から操作された疑い」を知らせて書き直さない。最初の登録も、session の起動がカードの起動より前なら取り込まない。変異 2 本が red。
      🚨 pending は daemon のメモリの中だけ。daemon が起動し直すと失うので、その間に Claude Code が自動で再開した PG は「外から操作された疑い」に倒れる (3c-2 で扱う)
  - [x] 3c-2a 落ちた回数で止める (426 の決定 4)
      pid が変わった作業中の session の transcript に、Claude Code の自動の再開の文 (425 結果 1 の実測。`live.RestartNote`) が新しく出ていれば
      自動の再開として記録を書き直し、時刻を card.Crashes に数える。文が無ければ今までどおり「外から操作された疑い」。
      最後の起動・再開の後、30 分の間に 2 回落ちたら `claude stop` して、カードを人間の回答待ち (WaitCrashed) にする。回答すると同じ session を再開する。
      止められなければ作業中のまま次の Tick でまた試す。変異 7 本が red (再開の文を読まない / 前の文を数え直す / 時間の窓を外す /
      再開の前の回数も数える / 止められないのに回答待ちにする / 上限を 1 つ上げる / transcript の文を読まない)。
      🚨 再開の文を含むレコードの origin は未実測 (発言者を問わず探している)。Claude Code の版で文が変わると数えられず、外からの操作の疑いに倒れる
  - [x] 3c-2b watchdog (停滞)。進捗 = transcript の末尾に、それまでに無かった PG の出力が出たこと (`Transcript.LastNew`。同じ出力の繰り返しは数えない)。
      新しい出力が `card.StallThreshold(c, 15 分)` の間出なければ停滞にし、出たら外す。作業中の列を離れたら停滞の印を外す (store の遷移 / daemon の起動・再開)。
      `LastProgress` の書き手は daemon だけにした (live の画面側で transcript の最終更新を足していたのを外した)。
      変異 5 本が red (閾値を無視 / 戻っても外さない / 実行中の延長を外す / 列を離れても残す / 繰り返しも進捗に数える)。
      🚨 毎回少しずつ違う文を出すループは進んでいるように見える (415 論点 10 の「同じツール + 同じ引数」は未実装。ツール呼び出しは読んでいない)
  - [x] 3c-2c 知らせ (426 の決定 10):
      件数の文 (`daemon.Status`。例 `pro-con ?2 停滞1 🚨落ちた1`、何も無ければ空) を、変わったときだけ tmux のユーザー option `@pro-con-status` に書く
      (`_tmux.conf` は #() で fork しない方針なので、option を format で読む形。daemon が止まるときに外す)。
      新しく回答待ち (質問・権限・落ちて止めた) になったカードは macOS の通知を 1 度出す (待ちを抜けたら忘れる)。
      変異 6 本が red (変わらなくても書く / 同じ待ちを何度も通知 / 書けなかった文を毎回書き直す / 待ちを抜けても覚えたまま / 落ちた件数を数えない)。
      - [x] status の置き場所は、ユーザーが 3 案から「右端のキーガイドの島の左」を選んだ (2026-09-25)。`_tmux.conf` の status-right に、件数があるときだけ出す。
        キーガイドの島は 27 セルのまま。件数が最大 (2 桁ずつ) でも status-right-length 60 に収まることを tests/tmux/test_tmux.sh が展開後の幅で pin (変異 2 本が red)
      - [x] 3f の 2 回とも daemon の `tmux set-option` と `osascript` は失敗を返さなかった (daemon の出力に「書けない」「出せない」が 0 件)。🚨 画面に出たかの目視は未確認
      - daemon を kill -9 すると option が古い件数のまま残る (画面は daemon の最終 tick で古さを出すが、status には出ない)
  - [x] 3c-3 `pro-con daemon` の常駐と排他 (2 つ起動しない) — 3c-2 より先に済ませた (daemon を動く形にするため)
    - 済み (2026-09-25): `pro-con daemon [--limit N] [--once]` (`src/pro-con/daemoncmd.go`)。3 秒ごとに Tick し、何をしたかを時刻つきで stdout へ。
      排他は `daemon.lock` の flock (プロセスが終われば OS が外す)。変異 1 本 (flock を外す) が red。🚨 本物の claude では未実行 (3f)
- [x] **3d 画面**: 本物の backend が 3a の記録を読み、書き込み (回答・依頼 等) は箱に置く。読み取り専用をやめる
  - 済み (2026-09-25): 本物の backend のカードは store の記録から出る (session 1 本 = カード 1 枚はやめた)。作業中のカードに pro-con が起動した
    session の様子 (出力の末尾・pid) を足す。新しい依頼と回答は受付の箱へ、追加オーダー・btw・片付けはまだ (backend.Accepter で、押した時点で断る。
    ReadOnlier を置き換えた)。箱に適用待ちが溜まったらヘッダーで知らせる。隔離 tmux で card add → daemon --once → 画面の依頼の列、を確認。
    変異 3 本が red (外の session の様子を足す / 追加オーダーを受ける / 適用待ちを知らせない)
- [x] **3e PM への指示書**: PM の session に渡す、`pro-con card` の使い方と規律 (AskUserQuestion を使わない 等)
    - `src/pro-con/pm-guide.md` を `pro-con card guide` で出す (embed)。指示書に書いた `pro-con card ...` は TestPMGuideCommandsParse が今のパーサに通して、ずれを止める (変異: plan の例を `--issues` に崩すと red)。
      🚨 PM の session にこれを渡す手順 (起動時のプロンプトに入れるか) は 3f で本物の PM を立てるときに決める
- [x] **3f 本物の claude で通しの確認** (枠を使う。424 から引き継いだ受け入れ条件もここ)
  - 1 回目 (2026-09-25 08:34、Claude Code 2.1.281、PG 1 本): add → plan → daemon が起動 → PG が `card ask` → kill -9 → 回答 → 再開 → PG が `card review` まで通った (約 70 秒)。
    `claude agents --json` を 3 秒ごとに stdout / stderr を分けて記録した。実測で分かったこと:
    - 一覧の cwd は PG の worktree (`~/dotfiles/.claude/worktrees/pc-c-001`)。transcript はその project の下
    - **`claude --bg --resume <session-id>` は元の session を続けず、別の session id・別の短い id の session を立てる** (cwd は同じ、名前は AI の題に変わる)。
      stop した元の session は `--all` で stopped として残る。→ daemon が自分の再開の後の新しい session を拒んでいた (「短い id が別の session を指している」)
    - kill -9 の直後は `--all` なしの一覧にも pid 無し・`working` で出る (その間の cwd は repo root)。約 18 秒後に同じ session id が新しい pid で戻り、
      **startedAt は再開した時刻になる**。カードが質問待ちだったので daemon は記録を書き直さず、pid が古いままになった
    - `claude agents --json` が 3 秒を 1 回超えた (PG の起動・再開の最中)
    - PG は規律どおり `card ask` → turn を終える → 回答で再開 → `card review` を打った。除けた依頼は 0 件
  - 直した: 再開が返した短い id の session でカードの行を置き換える (live.ReplaceCard) / 再開の取り込みは同じ作業ディレクトリでも照らす /
    記録の書き直しは完了以外のカード全部で行う / 一覧の上限を 10 秒へ。変異 4 本が red
  - [x] 2 回目 (08:43、直した版): 同じ手順で通った。kill -9 の約 11 秒後に「C-001 の PG が落ちて自動で再開した (pid 72295 → 72978)」と数えて記録を書き直し
    (1 回なので止めない)、再開で新しくなった session (4e65ef8d) でカードの行を置き換えた。隔離した tmux (`-L pc3f`) で開いた本物のモードの画面に、
    C-001 がレビューの列に出た (daemon の最終 tick「0秒前」)。後片付け: 起動した 4 本の session を `claude rm`、worktree とブランチも消えたことを確認。
    状態の置き場は scratchpad へ退避し、`~/.local/state/pro-con/live` は空に戻した

## 敵対的レビュー (3a〜3d、2026-09-25)

1 周目 (Opus、読み取りのみ、4004f313〜13c8f60e) の全数: P1 1 / P2 5 / P3 4 = 10 件。対応 7 / 3c-2 へ 1 / 記録のみ 2。

- [x] P1 起動・再開が「失敗」と返っても session が立っていることがある / Launch の後で記録を書く前に落ちる → PG が Tick ごとに増える・回答が二重に渡る。
  **起動の前に印 (card.Launching / LaunchedAt) を記録へ書き、次の Tick で一覧と照らして取り込む** (起動は名前 `pc-<id>`、再開は前の session id)。
  一覧に出なければ launchGrace (1 分) 待ってから起動し直し、待つ間は上限に数える
  - 🚨 残り: 印を書いた後に立った session が launchGrace を過ぎてから一覧に出ると、起動し直した後なので取り込まれず孤児になる (回数の上限 3c-2 で本数は抑える)。
    daemon が Launch 成功の後に落ちて取り込むまでの間に PG が `card ask/review` を置くと、カードが作業中でないので除けられる (rejected/ に理由つきで残る)
- [x] P2 除けた依頼が控え (Applied) に入り、rejected/ へ移す前に落ちると理由を残さずに消える → 除けた依頼は控えに入れない。.reason を先に書き、rename の失敗はエラー
- [x] P2 pending がメモリだけなので、再開の後に daemon が起動し直すと二度と登録されない → 判定を記録の LaunchedAt に移した (pending は廃止)
- [x] P2 再開が、今の一覧と照らさずに短い id で `claude stop` を撃つ → prepare で、短い id が前の session id を指していなければ再開しない
- [x] P2 pending の間は別の session id でも取り込む → 記録の行と session id が違えば取り込まない
- [x] P2 (推測) `--resume` で短い id が変わると追跡が切れる → Resume が返した id をカードの Session にする。🚨 変わるかは未実測 (3f)
- [x] P3 1 回の Apply で控え (1000) を超えると二重適用 → 1 回 500 件まで
- [x] P3 起動に失敗し続けると History が増え続ける → 直前と同じ文は足さない (daemon.note。変異 1 本が red)
- 記録のみ P3 `daemon.lock` を稼働中に手で消すと 2 つ目が取れる (人の手の操作だけ。対応しない)
- 記録のみ P3 箱の依頼に送り手の認証が無い (どの PG でも別カードを操作できる)。規律で守る前提 (426)。3f で本物を動かして問題になったら再評価

変異 9 本が red (取り込まない / grace を無視 / 短い id の照合を外す / 一覧なしで割り当て / 外の pid 変更を書き直す / 自分の再開後も書き直さない /
起動より前の session を取り込む / 除けた依頼を控えに入れる / 1 回の上限を外す)。

2 周目 (Opus、直した差分 147fa851) の全数: P2 4 / P3 4 = 8 件。対応 5 / 3f へ 2 / 記録のみ 1。

- [x] P2 印の残ったカードが上限の判定の後ろにあると、立っている PG を数えずに別のカードを起動する → 印付きのカードを上限より先に片付ける
- [x] P2 除けた依頼を判定し直すと、後の依頼で状態が変わった後に適用される (順序の入れ替わり) → 除けた ID と理由を記録 (State.Rejected) に控え、
  判定し直さずに rejected/ へ移すだけにする。移せなくても daemon は止めない (結果に出して次の Apply で移し直す)
- [x] P2 前の session が一覧に無いのに `claude stop` を撃ち、失敗なら再開に届かない → 一覧に無ければ止めずに再開する
- [x] P3 テストの無い分岐 (adopt の「印より前」の除外 / 再開の失敗の後の取り込み) → テストを足した
- [x] 3f で実測: 自動の再開では startedAt が再開した時刻になる。`--resume` は別の session になる (上の 3f の節)。— 元の記述: 再開した session の `startedAt` が「再開した時刻」か「元の開始時刻」か (後者なら register が再開後の登録を拒み続ける。
  今のテストは前者を前提にしている)。`-w pc-<id>` を 2 回目に渡したとき (1 回目が worktree だけ作って落ちた) 起動できるか
- [x] P3 prepare のエラー (repo が設定に無い等) が Tick ごとに History へ 1 行足す → 上と同じ (直前と同じ文は足さない)
- 記録のみ P3 この修正より前に作られた作業中カード (LaunchedAt がゼロ値) は時刻の防御が効かない。本物の daemon はまだ一度も動いていないので、該当するカードは無い

変異 5 本が red (印付きを上限の後ろへ戻す / 一覧に無くても止める / adopt の時刻の除外を外す / 再開の取り込みを外す / 判定し直す)。
3 周目 (Opus、2026-09-25 08:05、afd41f39〜066c0393 = 2 周目の修正 + 3c-2a〜c) の全数: P1 0 / P2 3 / P3 4 = 7 件。全部対応した。

- [x] P2 前の session が一覧に無いとき stop せずに再開すると、Claude Code の自動の再開 (約 25 秒、その間一覧に出ない) と重なって同じ session が 2 本立つ →
  一覧に無い session の再開は、回答から restartWait (1 分) 待つ。🚨 自動の再開の途中が `claude agents --json` (--all なし) に出るかは未実測 (3f)
- [x] P2 再開の cwd を指定していない (daemon の cwd で走る。transcript が見つからない / 別の tree を書く) → 記録に session の cwd (Owned.Cwd) を持ち、
  そこで再開する。transcript が複数の project にあれば更新の新しい方を読む
- [x] P2 本物のモードでは card.Exec を誰も書かないので、長いコマンドの実行中を停滞と誤る。ツールだけの出力も進捗に数えていない →
  進捗にツール呼び出し (名前 + 引数) を足し、結果の返っていない呼び出しがある間は閾値を longToolLimit (1 時間) まで延ばす
- [x] P3 回数の追記と記録の書き直しの順 → 記録を書き直してから数える (あいだで落ちても 1 回数え漏れるだけ。逆の順だと pid を二度と書き直せない)。テストは無い (2 回の書き込みのあいだの停止を作れない)
- [x] P3 再開の文を発言者を問わず本文のどこでも探すので、依頼の原文の引用で数える → user レコードの文の先頭にあるときだけ数える
- [x] P3 止められないまま窓を過ぎると諦める / 止める前に短い id を照らさない → StopWanted の印で止められるまで試す。
  一覧で短い id が記録の session を指していなければ止めずに回答待ちにする
- [x] P3 tmux サーバを作り直すと件数が消えたまま → 変わらなくても 1 分ごとに書き直す

変異 10 本が red (再開を待たない / cwd を渡さない / transcript の先頭を取る / ツール呼び出しを数えない / 結果の返った呼び出しを実行中のまま /
実行中を延ばさない / 引用の文も数える / 窓を過ぎたら諦める / 短い id を照らさずに止める / 書き直さない)。
4 周目 (Opus、610ae2ab = 3 周目の修正) の全数: P1 0 / P2 3 / P3 4 = 7 件。対応 6 / 記録のみ 1。

- [x] P2 止める印 (StopWanted) が質問 → 回答 → 再開の経路で残り、再開した PG をすぐ止める → 作業中へ戻る settle で外す (store の遷移では外さない。戻る経路は settle 1 つ)
- [x] P2 一覧に無い = 止めた、と扱い、直後の自動の再開と重なる → 一覧に無ければ最後に落ちてから restartWait 待つ。過ぎたら「止めなかった (一覧に無い)」と分けて書く
- [x] P2 結果の無い古いツール呼び出しで PendingSince が立ったまま → 最後の PG の出力にある呼び出しだけを実行中に数える
- [x] P3 cwd が空のとき daemon の cwd で再開する → 再開しない (理由を履歴に書く)。🚨 agents の cwd が worktree を指すかは 3f で実測
- [x] P3 announce のコメントが挙動 (1 分ごとに言い直す) と食い違う → コメントを直した
- [x] P3 FindTranscript のテストが「末尾を取る」実装を見逃す → 新しい方を辞書順の先頭に置いた
- 記録のみ P3 同じ長いコマンドを繰り返すループでは、Tick が実行中を見るか結果の後を見るかで閾値 (1 時間 / 15 分) が揺れる。いずれ検出はされる

変異 5 本が red (settle で印を外さない / 一覧に無いのを待たない / cwd 無しで再開 / 置き去りの呼び出しを実行中に数える / 末尾を取る)。
5 周目 (Opus、0a18a2d1) の全数: P1 0 / P2 1 / P3 1 = 2 件。両方対応した。

- [x] P2 並列のツール呼び出しは 1 件ずつ時刻の違う別の行に書かれる (実 transcript で確認) ので、「最後の出力より前の呼び出しは置き去り」では長い方を落とす →
  置き去りの判定を「呼び出しの後に、ツールの結果ではない user 行 (再開の文・人間の発言) が来たか」に変えた
- [x] P3 記録の行に cwd が無いと二度と再開できない (前の版が書いた行 / 一覧の cwd が空だった) → 一覧に cwd が出ていれば行を書き直す

変異 3 本が red (結果で他の呼び出しも消す / 置き去りを消さない / cwd を埋め直さない)。
🚨 **周回はここで区切る** (adversarial-review-own-safeguards §7: 観測 API が絡むなら、周回を増やす前に実機を 1 回挟む)。残る不確かさは本物の claude の挙動
(一覧の cwd / 自動の再開の途中の見え方 / 再開後の startedAt / 短い id が変わるか) なので、3f で測ってから、その結果に合わせてもう 1 周攻める。

6 周目 (Opus、3f の実測を渡して 51084138 / 8f3f17e6 = 3f の修正を攻めた) の全数: P1 1 / P2 2 / P3 2 = 5 件。全部対応した。

- [x] **P1 再開の結果が分からない間に、人間が PG の worktree で開いた対話の session を取り込み、カードの行まで乗っ取る**
  (以後 `claude stop` や `--resume` の対象になる = pro-con が起動していない session に触る) → 取り込みと登録は `kind: background` だけ。
  cwd で照らすのは pro-con の worktree (`/.claude/worktrees/pc-`) の下だけ。自動の再開で記録を書き直すときは記録の cwd を優先する
  (落ちている間の一覧の cwd は repo root で、それを記録すると照合が repo 全体に当たる)
- [x] P2 cwd の照合の否定側のテストが無い → 対話 / 別の worktree / 印より前、の 3 ケース (一覧は候補だけ。元の session が先に当たると退行が隠れた)
- [x] P2 行を置き換える条件の守りが固定されていない → 短い id が使い回された形のテスト (`o.ID != c.Session`)。
  もう片方の `o.StartedAt.Before(c.LaunchedAt)` は、到達できる状態では前者と同時にしか外れないので、テストで区別できない (守りとして残す)
- [x] P3 履歴の重複除去が「claude を走らせて失敗と返った」まで潰す → 重複除去は「起動・再開できない」(何も走らせていない) の履歴だけ (noteOnce)
- [x] P3 コメントの「最大 3 秒」→ 10 秒

変異 7 本が red (対話も取り込む / worktree を照らさない / 印より前も取り込む / 対話も登録する / 使い回しで置き換える / 失敗の履歴も潰す / 一覧の cwd で上書き)。

7 周目 (Opus、6d7863ce。daemon 全体で「自分が起動していない session に届く経路」を攻めた) の全数: P1 0 / P2 1 / P3 2 = 3 件。全部対応した。

- [x] P2 起動の取り込みが名前 (`pc-c-001`) しか照らさず、別の状態の置き場で動く daemon が同じカード ID で立てた PG に当たる →
  cwd がこのカードの repo の worktree そのもの (`<repo>/.claude/worktrees/pc-<card>`) のときだけ取り込む。
  🚨 残り: 同じ repo を 2 つの状態の置き場で同時に使うと worktree そのものを共有するので、これでも区別できない (1 つの repo に daemon は 1 つ、が前提)
- [x] P3 stop / 再開の相手選びが pid を照らさず、登録の側 (pid が違えば外から操作された疑い) より甘い → 記録の pid と違う session は止めも再開もしない
- [x] P3 kind が background 以外の値 (版の変更・欠落) になると黙って取りこぼす → 短い id が一致するのに background でない session を見たら知らせる

変異 4 本が red (worktree を照らさない / 再開で pid を照らさない / 止めるときに pid を照らさない / kind の違いを知らせない)。

8 周目 (Opus、93b89fa8) の全数: P1 0 / P2 2 / P3 3 = 5 件。対応 3 / 記録のみ 2。

- [x] P2 止める段で、落ちている最中 (一覧に pid 0 で出る) の自分の PG を「記録と一致しない」と読み、止めずに回答待ちへ送る
  (直後の自動の再開で戻った PG が回答待ちの下で走り続ける) → pid 0 は一覧に無いのと同じく、最後に落ちてから restartWait 待つ。再開の側も同じく待つ
- [x] P2 起動の取り込みの cwd を文字列で比べていて、symlink を含む repo のパスで自分の PG を取りこぼす (→ 二重起動) → 両側の symlink を解決して比べる (samePath)
- [x] P3 repo の場所が分からないと worktreePath が空になり、空の cwd と一致して名前だけで取り込む → 空は何とも一致しない
- 記録のみ P3 落ちている間の一覧の cwd は repo root なので、最初の起動の直後に落ちて、かつ起動が失敗と返っていると、launchGrace と重なった時だけ取り込めず二重に起動しうる。
  取り込まない側に倒れるので外の session には触らない。回数の上限 (3c-2a) で本数は抑える
- 記録のみ P3 kind の警告は Tick ごとに daemon のログへ出続ける (pid の警告と同じ扱い。履歴・通知には入らない)

変異 5 本が red (pid 0 の間も止めに行く / pid 0 の間も再開する / symlink を解決しない / 空も一致と読む / repo の場所なしで取り込む)。

9 周目 (Opus、b18ec9db) の全数: P1 0 / P2 1 / P3 2 = 3 件。全部対応した (根は 1 つ: 落ちている間の待ちを、落ちた時刻ではない時刻から数えていた)。

- [x] P2 止める段の待ちを「直前の自動の再開の時刻」から数えていて、再開から長く走ってから落ちた PG を待たずに回答待ちへ送る →
  daemon が「一覧に無い / pid 無し」を最初に見た時刻をカードに持つ (card.DeadSince。生きているのを見たら外す)。止める段も再開の段もそこから restartWait 待つ
- [x] P3 再開の段の待ちを回答の時刻から数えていて、上限で待たされたカードで自動の再開と重なる → 同上
- [x] P3 TestSamePath が、実際に symlink を含む側 (第 2 引数 = 設定の repo のパス) を検査していない → symlink を含む repo のパスで起動の取り込みを通すテストにした

変異 4 本が red (止める段を直前の再開から数える / 再開の段を回答から数える / 第 2 引数を解決しない / 生きていても DeadSince を外さない)。

10 周目 (Opus、24a65860): **採用すべき P1 / P2 なし** → 周回を閉じた。P3 2 件:
- 記録のみ P3 止める印が立った後、PG がまた落ちて同じ session id が新しい pid で戻り、transcript の再開の文がまだ書かれていない瞬間に止める段が当たると、
  「記録と一致しない」として待たずに回答待ちへ送る (外からの操作と区別するには別の時刻が要る)。**trigger**: 次に本物で動かすとき、一覧に戻るのと
  再開の文が書かれるのとの前後を測る。戻るのが先なら、pid が違う形も restartWait 待つ
- [x] P3 コメントの乖離 (stopCrashing の待ちの起点 / restartsSince の doc の位置) → 直した
- 🚨 リファクタ候補 (未着手): 「PG の session が生きているか」の判定が trackDead (session id で照らす)・stopCrashing・prepare (短い id で照らす) の 3 か所に分かれていて、
  周回の指摘の多くがこの食い違いから出た。**trigger**: 次にこの 3 か所のどれかを触るとき、1 つの関数 (カード + 記録 + 一覧 → 生きている / 落ちている / 別の session / 無い) へ寄せる

## 前提

- **設計の決定は 426 の「決定」節が正本** (カードの書き手は daemon / 回答は stop → resume / 追記は turn の区切り / 落ちたら自動の再開 + 回数で止める /
  役割 4 つ (PG・テストの係・調べる係・レビューの係) / PM 1 つ / 知らせは tmux の status + macOS の通知)。ここには写さない

- **PG / 係を起動したら、`live.Register` で pro-con の記録 (`$XDG_STATE_HOME/pro-con/live/sessions.json`) に足す**。本物のモードは記録にある session
  だけを扱う ([424](done/424-feat-pro-con-readonly-real-backend.md) の範囲の変更)。記録の書き手は daemon だけ (426 の決定 1)
- **記録には session id と pid が必須** (`live.Register` が拒む)。`claude --bg` が返す短い id で `claude agents --json` を引き、session id と pid を得てから記録する。
  再開のときも CardID を渡す (書き直しは行ごと置き換える)
- **pid は落ちて自動で再開されると変わる** (425)。変わると pro-con の session は一覧から外れる (失敗側)。自動の再開 (transcript に
  「automatically restarted」の注記が入る) なら新しい pid で記録を書き直し、外での再開なら書き直さない、を区別する
- **外からの操作を検出する**: pro-con の session は、他の shell からも `claude attach` / `claude stop` できてしまう (Claude Code の側で止められない)。
  pro-con が指示していない変化 (止まった・入力が増えた・transcript に pro-con の知らない人間の発言) を見つけたら、カードに「外から操作された」と出して止める
- PG / 係は [431](431-feat-pro-con-pg-session-settings.md) の役割ごとの設定で起動する (431 を先にやる)
- 模擬 (ハリボテ) は残して起動のときに選ぶ。起動の口・状態ファイルの分け方・画面の区別は [424](done/424-feat-pro-con-readonly-real-backend.md) の「模擬と本物の併用」節

### 415 の決定事項

- PG は `claude --bg -w` で起動し、自分のブランチまで push する (master へは PM がレビューしてから)
- 同時実行数の上限は 2 から始める
- dispatcher と watchdog は決定論的に作る (LLM に見張らせない)

## 実測で分かった前提 (425)

- PG の状態は `claude agents --json --all` の `status` / `state` / `pid` / `waitingFor` で読める (完了・API エラー・停止・プロセスの死・権限待ち・質問待ちを区別できる)
- PG への送信は SendMessage (bg session も `ListAgents` に名前で出る)。枠は `claude -p "/usage"`
- PG にもユーザーの hook と規約が効く (起動時 約 13 万 token。Stop hook が別の作業の issue を更新させにいく)。PG 用の `--settings` で hook を絞るかを決める

## 受け入れ条件 (424 から引き継ぎ)

- [x] pro-con が起動し、記録 (`sessions.json`) に書いた本物の bg session が、本物のモードのカードに出る (3f の 2 回目) (424 では記録を書く側が無く、実物で確かめていない)

## 関連ファイル

- `src/pro-con/backend/backend.go` (UI との口。模擬と同じ interface を満たす)
- `src/pro-con/fake/` (振る舞いの手本)

## 進捗

- [x] 着手できる (425 の実測・426 の決定が済んだ。2026-09-24)
- [x] カーソルの枠がレーンを跨いで滑る途中にカードの中身へ縦線を描き、中身が一瞬消えて見える不具合を直した (2026-09-25 の報告。commit「fix(pro-con): 滑走中のカーソルの縦線でカードの中身を消さない」。回帰テスト `TestCursorGlideKeepsCardContent` は修正を外すと red)
