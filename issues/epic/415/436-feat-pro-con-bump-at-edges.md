# 436 (feat): pro-con の選択が端でぶつかったときの演出 (上下左右)

起票日: 2026-09-25

親: [415](415-design-claude-pm-worker-orchestration.md)

## 概要

ボードで一番上のカードを選んだまま ↑ を押したら「上にぶつかった」と分かる、ゴツッと出てすぐ戻る演出が欲しい。
最下部・左端・右端でも同じ (2026-09-25 にユーザーが依頼)。今は何も起きない (選択が動かない = 押したかどうかも分からない)。

## 対応方針

- 見た目は本体に入れる前に見本で決める (`~/.claude/rules/decide-layout-in-sample-renderer-first.md`)。見本を用意した (使い捨て、commit しない):
  `python3 ~/dotfiles/tmp/pro-con-bump-sample.py [A|B|C] [ms]`
  - A: 選択の枠が壁の方へ少し出て (1 行 / 2 桁)、減衰しながら跳ね返って戻る
  - B: A + ぶつかった壁 (列の枠の辺) が一瞬赤く光る
  - C: 列ごと小さく揺れる
  - 既定 500 ms。約 75 ms で出きって、跳ね返りながら戻る
- **ユーザーの選択待ち** (A / B / C と長さ)。決まったら ui/cursor.go の枠の滑り (cursorGlide) に「ぶつかる」を足す。
  端の判定は moveRow / moveCol / jumpCol が動かなかったとき (空のレーン・カード 1 枚の列も含む)
- 好み (記録): 操作のメタファー + 600 ms〜1 s + 主役を高コントラスト (memory の tui-animation-taste)。ぶつかる演出は短い方が合うかもしれないので、長さも見本で決める

## 進捗

- [x] 見本 A / B / C (2026-09-25)
- [x] ユーザーの選択: **見本 C (列ごと揺れる) を 1000 ms** (2026-09-25)
- [x] 本体 + テスト (2026-09-25、C-003)。`ui/bump.go`
  - 揺らすのは 1 枚 / 1 列だけ動くキー (↑k ctrl+p / ↓j ctrl+n / ←h ctrl+b / →l ctrl+f) で動けなかったとき。
    半ページ・g / G・数字キーは「端へ寄せる」移動なので揺らさない
  - 描き方: ボードに選択の枠と移動中のカードを重ねた後、そのレーンの帯 (桁の範囲) を丸ごとずらす。枠も帯に入っているので
    一緒に揺れ、`ui/cursor.go` の描き方 (f82819a1) には手を入れていない。はみ出しは切る (左右の端の外・ボードの上端の外 =
    上へぶつかるとレーンの上枠が見出しの罫線にめり込んで見える)。下へずれるときだけボードを 1 行伸ばして下枠を残す
  - ずれは見本と同じく偶数丸め (`math.RoundToEven`)。振れが小さいので、跳ね返り (負の側) は左右でも丸めで 0 になり、
    見えるのは壁の方へ出る 1 回だけ (上下は約 75〜230 ms、左右は約 40〜265 ms。負の側は最小でも約 -0.19 で、2 倍しても丸めると 0)
  - テスト `ui/bump_test.go`: 上下 (枠が 1 行ずれる・下枠が消えない) / 左右 (空のレーンで見出しが 2 桁ずれる) /
    端でなければ揺れない / 1000 ms で止まって画面が戻る / 揺れの間も次のキーを受ける。5 本とも `bin/mutate-verify` で
    想定の検査だけが red になるのを確かめた
- [x] e2e モードで揺れの途中を撮った (空のボード、200×50)。左端で h を押して 100 ms 後 — レーン 1 が壁の外へ 2 桁出て、
  レーン 2 との間が 2 桁空く:

  ```
  ─ ▶ 1 依頼 (0) ───────────────╮  ╭─   2 分解済み (0) ───────────╮ ╭─   3 作業中 (
                                │  │                              │ │
  ```

  上 (k) は 120〜160 ms にレーン 1 の下の角が 43 → 42 行目へ、下 (j) は 120〜200 ms に 43 → 44 行目へ動き、
  最下段の案内の行は動かなかった。右端 (l) はレーン 6 が 2 桁右へ出て、右の壁で切れた
