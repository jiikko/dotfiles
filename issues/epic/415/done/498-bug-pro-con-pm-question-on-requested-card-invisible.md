# 498 (bug): PM が人に確かめている依頼のカードが、依頼の列に「PM が依頼を分ける」のまま置きっぱなしになる (人の番に見えない)

起票日: 2026-09-26

親: [415](../415-design-claude-pm-worker-orchestration.md)

## 概要

ユーザーの指摘 (2026-09-26): 「質問を待っているなら、依頼レーンに置きっぱなしになっているのはダメじゃね？ バグか？」。

C-056 (画面の issue の一覧から 469 を積んだ依頼。原文は空) で、PM は「469 は実装済み。閉じる / 残りのリスクを直す / 別の意図があるか」を
**PM の session の会話の中で**人に聞いて turn を終えた (2026-09-26 15:57 と 16:05 の 2 回)。人からはその会話が見えないので、
カードは 36 分以上、依頼の列に「今の待ち: PM が依頼を分ける」のまま残った。人の番の目印 (452) も通知も出ない。

## 原因 (2026-09-26 に読んだ)

- `src/pro-con/pm-guide.md` の規律: 「人間への確認は、会話の本文で聞いて turn を終える」(AskUserQuestion の答えが届かないため)。
  PM の問いは、カードにも画面にも載らない
- `pro-con card ask` は作業中のカードしか受けない (`src/pro-con/store/store.go` の `case "ask"`: 「作業中の列に無い」で断る)。
  依頼の列のカードでは、PM が質問をカードに載せる手段が無い
- PG の質問は `card ask` で質問待ちの列に移り、人に回せば (`card handoff`) 人の番になる。PM の質問だけがこの経路の外にある

## 期待する動作

- PM が人に確かめたい依頼のカードは、人の番として見えること (列・目印・通知)。PM が問いを会話にだけ書いて止まる形を無くす
- 案: 依頼の列のカードでも PM が問いをカードに載せられる口を作る (`card ask` を依頼の列でも受け、質問待ちの列へ移し、人の番にする。
  回答は依頼の列に戻して PM に知らせる)。または、依頼の列のまま「人の返事待ち」の印を付ける。どちらにするかは決める (実装の選択は PG が決めてよい)
- pm-guide の規律を、その口を使って聞くように直す (会話の本文でだけ聞かない)
- 回答の入力 (493 の回答フォーム) は、PM の問いでも同じものが使えること

## 決めたこと (2026-09-26。C-058)

PG が決めた (実装の選択は任された):

- **口は `card ask` を依頼の列でも受ける形**。依頼の列のカードへの ask は PM の問い (`card.Wait.AskedBy = PM`。`Wait.FromPM`) として質問待ちの列へ移す。
  「依頼の列のまま印を付ける」は採らない: 列・人の番 (`card.Turn`)・通知 (`dispatcher/notify.go`)・回答フォーム (493)・`r` が、どれも質問待ちの列を前提にしていて、そのまま効く
- 形は PG の ask と同じ (自由文 / `--json` の選択肢つき)。答えも同じフォームと `card answer` の自由文 (493 とそろう)
- PM の問いは**いつも人の番** (PM が PM に聞かない)。PM には知らせない (`pmKey`)。`card answer --from PM` は断る
- 人が答えると**依頼の列へ戻す** (PG の session は無いので Resume にしない)。回答は `card.PMAnswer` と履歴に原文で残し、
  PM への知らせは「PM の質問に人が回答した依頼 C-xxx「題」: <回答>」。PMAnswer は依頼の列を離れたら (分けた・閉じた・また聞いた) 消す
- 見た目は変えない (既存の質問待ちの列・`?質問`・人の番の目印・回答フォーム)。「今の待ち」だけ「人の番: PM の質問に答える (r で回答すると依頼の列へ戻り…)」になる。
  新しい見た目が無いので見本は出していない
- pm-guide: 役目 1 に「聞くときは `card ask` でカードに載せる」を足し、規律の「会話の本文で聞く」を直した

## 関連

- 452 (人の番) / 437 (PM が PG の質問を受けて人に回す) / 493 (回答フォーム) / C-056 (起きた実例)

## 完了 (2026-09-26)

- C-058 で master に入った (ea155913 / 4ebed83b。取り込みは取り込みの係)。17:49 に dispatcher を起動し直して効かせた (505 が入るまでは、取り込みの後に手で起動し直す)
- 🚨 動いている PM の session は、再開のたびに知らせだけを受け、指示書を読み直さない。pm-guide の「聞くときは card ask でカードに載せる」が
  今の PM に効くのは、PM が `pro-con card guide` を読み直したときか、新しい PM が起動したとき (未確認。次に PM が人に聞く場面で見る)
