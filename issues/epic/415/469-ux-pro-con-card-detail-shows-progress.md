# 469 (ux): カードを開いたら、作業の進捗 (何が済んで何が残っているか) を確かめられる

起票日: 2026-09-25

親: [415](415-design-claude-pm-worker-orchestration.md)

## 概要

ユーザーの依頼 (2026-09-25): 「カードを開いたら、作業進捗を確認したい」。
今の詳細 (enter で開く引き出し。`src/pro-con/ui/drawer.go` の `drawerBody`) に出るのは、状態・担当・issue・依頼の原文・指示・質問・追加オーダー・履歴・出力の末尾 3 行だけで、
「どこまで進んだか・あと何が残っているか」が分からない。2026-09-25 の dogfooding では、PM (人間の代わりの Claude) が PG のブランチの log・`runs/` のテストの出力・
issue の本文の進捗を手で読んで確かめていた (440)。

## 期待する動作 (候補。載せるものと並びは見本で決める)

- **PG の作業**: ブランチの commit (master より先の本数と subject)・未 commit の変更の数・最後に commit / 出力した時刻
- **テスト**: どのディレクトリで走らせたか (2026-09-25 の C-011 は `src/pro-con` の `make test` = pro-con だけの `go test -race` を頼み、17 秒で rc=0。repo 全体の make test と見分けが付かなかった) と、テストの係に頼んだコマンドの状態 (順番待ち何番目 / 実行中の経過 / 終わった rc と所要時間)。出力の末尾の数行
- **issue の進捗**: カードの issue の本文の「進捗」節のチェックボックス (済み / 残り) と、残りの行
- **取り込みの見込み**: master と、ほかの作業中のカードとの取り込みの衝突 (`git merge-tree` で commit 済みの分だけ。468 の順番の指定とも合わせる)
- **今の待ち**: 何を待っているか (PG の turn の途中 / テスト / 利用枠 / 順番 (468) / 人の番 (452))
- 🚨 読むだけ: `--view` でも出す。集めるのは画面ではなく dispatcher か読む口の側にし、画面が 3 秒ごとに git やファイルを重く読まない (441 の「人間の画面を遅くしない」)
- CLI の `pro-con card show` にも同じ進捗を出す (PM と外の Claude が読む)

## 関連

- 415 の要件 8 (「カードを選ぶと、履歴とそのタスクのログが見られる」) を満たす部品 (467 = ログ / 469 = 進捗 / 473 = 今走っているもの)

- 467 (PG の生の活動ログ。こちらは要約した進捗) / 455 (作業中と待ちを分ける) / 452 (人の番) / 468 (順番) / 440 (dogfooding で手で確かめていたもの)
- ついでに直す: 詳細の「PM に渡した指示」の見出しは、中身が PM から PG への指示 (`card add --prompt`) なので「PG への指示」が正しい

## 順番の見積もり (PM, 2026-09-26。C-035 を C-032 の後に積んだ)

- 触る場所: `ui/drawer.go` の `drawerBody()` に節を足す。集め方 (git log・テストの係の列) は dispatcher 側の新しい関数
- 変える判断: 既にある状態の遷移は変えない。452 が定義する「人の番」をそのまま読んで出す
- 順番の理由: 452 (C-032) の判定が無いと、この issue が同じ判定を別に作ることになる。467 (C-034) と 473 (C-037) も同じ `drawerBody()` に別の節を足すが、判断は共有しないので順番は付けていない (取り込むときの rebase で解ける)

## 進捗

- 2026-09-26 (C-035): 実装した。詳細 (enter の引き出し) と `pro-con card show` に「今の待ち」と「進捗」の節を足し、指示の見出しを「PG への指示」に直した
  - 集め方: dispatcher が 30 秒ごとに裏で `progress.json` (worktree の git の commit・未 commit・最後の commit と、issue の「進捗」節)、
    見張りが `conflicts.json` (今見えている取り込みの衝突。見張りの merge-tree の結果をそのまま写す = 同じ判定を 2 か所に作らない)、
    テストの係が結果を返すときにカードの `LastRun` (コマンド・worktree からの相対の場所・rc・所要・末尾 3 行)。画面と card show は読むだけ
  - 今の待ち: `card.WaitingOn` が 452 の `Turn` を読んで決める (人の番は理由つき・黄で出す)
  - 載せなかったもの: 「最後に出力した時刻」(活動は 467 のログ、今走っているものは 473 の節が出す)。issue の本文は repo が同じ issue だけ読む
