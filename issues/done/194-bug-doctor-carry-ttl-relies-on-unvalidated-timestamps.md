# 194 bug: `carryFresh` が依拠する時刻そのものが検証されておらず、無期限延命が別経路から再発する

起票日: 2026-09-03
重要度: **P3**
関連: [issues/done/172](done/172-bug-doctor-reused-size-feeds-startup-toast.md) (敵対レビュー 6 周目) /
[issues/done/173](done/173-bug-doctor-failed-entry-overwrites-cache-and-silences-toast.md)

## 対象

`src/glogx/doctor_cache.go` の `carryFresh` と、`doctorCacheFromReport` が新規エントリに書く `MeasuredAt`

## 何が起きるか

issue 172 で `doctorCarryTTL = 24h` を入れて「今回実測していないエントリの無期限延命」を塞いだが、
**TTL が依拠している時刻の正しさは誰も守っていない**。敵対レビュー 6 周目が同型の再発条件を 2 つ実測した。

### (a) 極端な未来ズレでは失効しない (実証済み)

`carryFresh` は「未来の時刻は引き継ぐ」側に倒してある (記録を残す側が安全、という判断)。
コメントは「時計のズレが過ぎれば age は正になり TTL で切れる」と書いているが、これは
**ズレが小さいこと**を暗黙に前提にしている。

実測: `MeasuredAt` が 100 年後を指す状態 (RTC 故障・不正な NTP 応答) を作ると、
以後 real time で 300 日 (60 日おき × 5 回) 相当の Esc を繰り返しても `heavy` (44GB) は
**一度も減衰しなかった**。

- 小さいズレ (3 時間) では設計どおり: `+26h` で生存 / `+27h` (= 24h + 3h) で失効

### (b) `MeasuredAt` の書き込み側を検証しているテストが無い (実証済み)

`doctorCacheFromReport` が新規エントリに書く `MeasuredAt: r.MeasuredAt` を
`MeasuredAt: rep.ScannedAt` に変異させても、**`go test ./...` が全部 green のまま**。
既存の「TTL 直前 / TTL 超過」のテーブルは `prev.Entries` にあらかじめ `MeasuredAt` を仕込んで
**seed-carry の側だけ**を検証しており、「saveCache が今回測ったエントリの `MeasuredAt` を
正しく永続化し、それが次回以降の判定に使われるか」という**書き込み側は一度も検証されていない**。

さらに `MeasuredAt` を**書かない**変異 (フォールバックの `prev.ScannedAt` に頼らせる) は、
**1 回の呼び出しでは検出できないが複数ラウンドで牙を剥く**:

- TTL より長い間隔 (25h) を空けた 2 ラウンド → 正しく失効する (フォールバックがたまたま機能する)
- **TTL より短い間隔 (10h おき) で 30 ラウンド (真の経過 300h)** → `hasHeavy=true` / `total=124GB`。
  「直前キャッシュの `ScannedAt`」へのフォールバックが毎回リセットされ、真の経過時間の累積を無視する

## 共通の根

`carryFresh` が信頼する時刻の出どころ (未来の `MeasuredAt` / `MeasuredAt` が無いときの
`ScannedAt` フォールバック) が、**想定した狭いケース (小さなクロック補正 / 単発の呼び出し) でしか
正しく機能せず、その前提を外れると同じ無期限延命に戻る**。

## 対応案

- **未来の `MeasuredAt` を書き込ませない** (保存時に `now` で頭打ちにする)。読み出し側で救うより、
  書き込み側で不変条件を作る方が経路が少ない
- **新規エントリが必ず `MeasuredAt` を持つことを保証する** (ゼロなら `rep.ScannedAt` を書き込み時点で入れる)。
  読み出し時のフォールバックをやめれば (b) の累積無視が構造的に起きない
- `carryFresh` のコメントに「未来を引き継ぐ判断はズレが小さいことを前提にしている」と明記する
  (この前提が書かれていなかったのが (a) の想定漏れの入口)

## 受け入れ条件

- [x] 100 年後の `MeasuredAt` を持つエントリが TTL 相当で失効する
- [x] 変異検証: `MeasuredAt: r.MeasuredAt` を `rep.ScannedAt` に変えると red になる
- [x] 変異検証: `MeasuredAt` を書かない変異が red になる
      — ⚠️ **「TTL より短い間隔の複数ラウンドで」の部分は成立しなくなった**。実装を
      「読み出し側のフォールバックを残したまま新規エントリだけ埋める」ではなく
      「**carry するときにも埋める**」形にしたので、ゼロ値は 1 回目の carry で埋まり、
      フォールバックが継続しない = **(b) の再発条件そのものが構造的に消えた**。
      この変異を捕まえるのは前段の assert (実測したエントリが `MeasuredAt` を持つ) と
      下記 (c)。複数ラウンドの assert は単独ではこの変異を捕まえない (実測して確認した)

## 対応 (2026-09-03)

**読み出し側で未来を救うのではなく、書き込み側で不変条件を作った。** 救う経路を 1 つに寄せるため
(`carryFresh` / トースト / 再利用判定がそれぞれ未来を疑う形にすると、どれか 1 つ漏れたときに
同じ無期限延命へ戻る)。

- `clampMeasuredAt(at, fallback, now)` を新設。**未来なら `now` で頭打ち / ゼロなら `fallback` で埋める**
- `doctorCacheFromReport` の 3 経路すべてに通した: 新規エントリ / partial の carry / `Reused` の引き継ぎ。
  **carry 側にも通したのが効いた** (旧版が書いた未来の時刻がキャッシュに居座っていても、
  次の保存で正規化される。上の受け入れ条件 3 の挙動もこれによる)
- `carryFresh` のコメントに「未来を引き継ぐ判断は **`clampMeasuredAt` が未来を書かせないこと**に
  依拠している」と明記した。この前提が書かれていなかったのが (a) の想定漏れの入口だった

### 着手前に issue の主張を再現した

- **(a) は再現した**: 100 年後の `MeasuredAt` で 60 日おき 5 回 (真の経過 300 日) 回しても
  44GB のエントリが一度も減衰しなかった
- **(b) は現行コードでは再現しなかった** (10h おき 30 ラウンドで正しく失効した)。
  issue が書いているとおり **`MeasuredAt` を書かない変異を当てて初めて再現する** (124GB が生存)。
  つまり (b) は「今壊れている」のではなく「**壊れたことに誰も気づけない**」形の指摘だった

### 検証

`TestDoctorCarryTTLClampsMeasuredAt` を追加。(a) 未来の頭打ちと失効 / (b) TTL より短い間隔の
30 ラウンドで失効・2 ラウンドでは生存 / (c) 走査時刻ではなく実測時刻が保存される、を固定する。

⚠️ **fixture で `MeasuredAt` と `ScannedAt` を別の値にした**。同じ値だと
「`MeasuredAt` を書く」を「`ScannedAt` を書く」に変える変異を素通りさせる
(最初はこれで書いてしまい、変異 M1 が green だった)。実際に両者がずれるのは
「重いエントリの計測値を再利用した走査」(計測は前、走査は今)。

変異検証 (使い捨て worktree、3 本とも red):

| 変異 | 結果 | 最初に落ちた assert |
|---|---|---|
| `MeasuredAt: r.MeasuredAt` → `rep.ScannedAt` | red | (c) 実測時刻ではなく走査時刻が保存されている |
| `MeasuredAt` を書かない | red | 前段の「実測したエントリに `MeasuredAt` が書かれていない」 |
| `clampMeasuredAt` の頭打ちを外す | red | (a) 未来の `MeasuredAt` が頭打ちにされていない |

`make test` rc=0。

## 未対応として受容した所見 (同レビュー)

- **TTL が切れた瞬間、トーストは何の説明もなく沈黙する**。`Failed` は立たない (そのエントリは
  「診断できなかった」のではなく「24 時間以上検証していない」だけ)。ユーザーからは
  「昨日まで 45GB と言っていたのに今日は何も言わない」に見え、issue 173 の
  「診断できないことは診断できないと言う」の精神とはやや不整合。文言を足すかは UI の判断なので、
  ここでは記録に留める
