# 207 test: 163 が起票した issue のうち、反証レビューを通していないものを着手時に通す

起票日: 2026-09-03
出典: [issues/163](163-audit-doctor-implementation-red-team.md) の受け入れ条件「起票した issue はそれぞれ反証レビューを通している」
重要度: **P3** (規律の記録。単独で着手する作業ではない)

## 何が未達か

163 は「起票した issue はそれぞれ反証レビューを通す (P1 は必須)」を受け入れ条件に置いたが、
**実際に通したのは P1 の 2 件だけ** (167 / 178。2026-09-02)。トークン消費が重いため、
残りは「着手するときにその issue だけ反証する」運用へ切り替えた。

## 現状 (2026-09-03 時点)

163 が起票した 17 件のうち **16 件は既に決着済み**で、内容は実コードで検証されている。

| 状態 | 件数 | 番号 |
|---|---|---|
| done | 15 | 167 / 168 / 170 / 171 / 172 / 173 / 174 / 175 / 176 / 177 / 178 / 179 / 180 / 181 / 182 |
| pending | 1 | 169 (glob の実名が未実測。`xcodebuild test` を回した後が trigger) |
| open (未着手) | 1 | **183** (`Y` のコピー文に裏取りコマンドを載せる) |

### done の 15 件は 2 種類に分かれる (2026-09-03 の敵対的レビューで訂正)

当初この issue は「done の 15 件は全部、着手時に実コードで再現を確かめて変異検証まで通している」と
書いていたが、**2 件で成立しない**。実ファイルで確認した:

| 区分 | 件数 | 何が確かめられているか |
|---|---|---|
| **再現まで確認** | 13 | 着手前に実コードで再現し、修正後に変異検証で red を確認した (各 issue の対応節に記録) |
| **被害の成立が未確認だったもの** | 2 → **1** | **167** は 2026-09-03 に実機で決着 (UUID 名コンテナは実在し、修正が正しく除外していた)。**168** は並列テストの実行中でないと観測できないので据え置き |

167 / 168 は**修正そのものは入っていて変異検証も通っている** (走査漏れを塞ぐ側は実コードで確認済み)。
未確認なのは「**その穴が実機で本当に踏まれるか**」で、163 自身も
「167 の実機 probe 由来の主張はコードからの静的検証止まり」と書いていた。

## 残る作業

- [x] **183 に着手するとき、その issue だけ反証レビューを通す** → **実施済み (2026-09-03)**。
      opus の反証レビューが P1 を 2 件・P2 を 2 件出し、**起票時の対応表をそのまま実装すると
      引用漏れでコマンド注入を再導入する**ことが分かったため表ごと書き直した。記録は
      `issues/done/183-*.md` の「この issue の反証レビュー」節
- [ ] 169 を pending から戻すとき (= `xcodebuild test` を回して glob の実名を採るとき) も同じ。
      🚨 2026-09-03 に **169 の静的探索は尽きた** ことを確認済み (`outputPath` は呼び出し側が渡すので
      Xcode のバイナリに名前のリテラルが無い)。同時に **`xctest-spindump` も同じ未確認の接頭辞を
      共有していた**ことが分かったので、実名は 2 エントリまとめて採る。詳細は 169 の追加調査節
- [x] **167 の UUID 名コンテナ** → **決着 (2026-09-03)**。実機に **1 件実在**し
      (`0A7CEF49-…`。全 646 件中)、持ち主は `/Applications/やくそく帳.app` の
      `Wrapper/Promise.app` (`com.jiikko.promise`) で**インストール済み**だった。
      修正後の `diskdoctor -json` はこの UUID を候補に出さず、残る 7 件は起票時の
      レビューが「正しい孤児」と判定したものと一致する。**修正が守っている対象が実在した**。
      詳細は 167 の「実機での裏取り」節
- [ ] **168 の clone デバイス** → **今回も未確認 (2026-09-03 に再確認済み)**。照合先 3 セットのうち
      `CoreSimulator/Devices` (1 件) と `XCTestDevices` (0 件だがディレクトリは実在) を確認したが、
      **`/private/var/tmp` の SimDevice 作業ディレクトリは 0 件**で被害の成立は観測できない。
      観測には並列テストの実行中である必要があり、`no-ios-simulator-verification.md` により
      シミュレータを使う確認は自発的に行わない。🚨 **現時点で誤判定は起こりえない**
      (候補が生成されない)。trigger は 168 に据え置き

## なぜ「13 件に遡って反証する」をやらないか

**反証の対象が残っていない**。反証レビューは「まだ実装していない起票の内容が正しいか」を
確かめる工程で、13 件は実装のときに実コードで裏を取り、変異検証まで通している。
遡って read-only のレビューを回しても、実測より弱い確認を後から足すことになる。

🚨 この判断は**未確認が残っている 4 件には適用しない**。183 / 169 (未着手) と
167 / 168 (被害の成立が未確認) は上の「残る作業」で個別に回収する。

## 受け入れ条件

- [x] 183 の着手時に反証レビューを通した記録が、183 の本文に残る (2026-09-03)
- [ ] 169 を再開するときも同じ
- [x] **167** の「被害の成立」に決着 (2026-09-03。実機に実在し、修正が正しく除外していた)
- [ ] **168** の「被害の成立」に決着 (並列テストを回す機会待ち。現時点で誤判定は起こりえない)

## 2026-09-03 の決着 — 183 は済み、残りは 169 / 168 の実測待ちだけ

この issue は「163 が起票した 17 件の反証レビューが済んだか」の**台帳**で、単独の作業は無い。
現状を実ファイルで確かめ直した:

| 対象 | 状態 |
|---|---|
| 183 | **済**。着手時に opus の反証レビューを通し、P1 2 件・P2 2 件が出て**起票時の対応表を書き直した** (そのまま実装すると引用漏れでコマンド注入を再導入するところだった)。記録は `issues/done/183-*.md` の「この issue の反証レビュー」節 |
| 167 | **済** (2026-09-03。実機に UUID 名コンテナが 1 件実在し、修正が正しく除外していた) |
| 169 | **待ち**。glob の実名は `xcodebuild test` を回さないと採れない。⚠️ **静的探索は尽きたことを確認済み**なので、次に読む人が Xcode を grep し直す必要は無い。同時に **`xctest-spindump` も同じ未確認の接頭辞を共有していた**ことが分かり、実名は 2 エントリまとめて採る |
| 168 | **待ち**。並列テストの実行中でないと観測できない。現時点では候補が生成されないので**誤判定は起こりえない** |

### 副産物: 169 の対応案 3 は実測を待たずに入れた

「検出 0 件を『候補なし』と表示しない」は実名が無くても実装できるので、CLI (`7bb5137d`) と
UI (`ecd26414`) の両方へ入れた。**未実測の 2 項目が候補 0 件でも画面から消えなくなった**
(それまでは畳まれていたので、「探せていない」が「きれい」と同じ見え方だった)。詳細は 169。

## この台帳を閉じるかどうか

残る受け入れ条件は 169 と 168 の 2 つで、**どちらも当該 issue 自身に trigger が書いてある**。
台帳を別に持つと本体と二重管理になり、実際この issue は 183 が済んだ後もチェックが古いままだった。
**閉じて 169 / 168 に一本化するのが妥当**だが、判断はユーザーに委ねる (この節を読んで決める)。

## 閉じた (2026-09-03。ユーザー判断)

**台帳を 169 / 168 へ一本化して閉じる。** 163 起票の 17 件に open は 1 件も無く
(183 も done)、残るのは 169 (pending) と 168 (done) の**実測待ちの trigger** だけ。
trigger は両方の本文に実在することを確認した:

- `issues/pending/169-*.md` の「再開の trigger」節 — `xcodebuild test` を回す作業が
  入ったとき、直後に `ls -la /private/var/tmp/*.logarchive` で実名を採る
  (静的探索は尽きたことを確認済みなので Xcode を grep し直す必要は無い。
  `xctest-spindump` も同じ接頭辞を共有しているので実名は 2 エントリまとめて採る)
- `issues/done/168-*.md` — 並列テスト実行中でないと観測できない。現時点では候補が
  生成されないので**誤判定は起こりえない**

台帳を別に持つ形は**実際に更新漏れを起こした** (183 が済んだ後もチェックが古いまま残った)。
本体が trigger を持っているので、台帳は二重管理にしかならない。
