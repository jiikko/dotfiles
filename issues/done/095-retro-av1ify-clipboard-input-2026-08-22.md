# 095 retro: av1ify のクリップボード入力を実装したセッション (2026-08-22)

起票日: 2026-08-22
種別: retro
関連: [094](094-human-verify-av1ify-clipboard-input.md)

やったこと: `av1ify` / `av1c` の引数なし呼び出しでクリップボードを入力に取り、一覧表示 +
[y/N] 確認を挟む経路を追加した（6973d99）。以下は踏んだ点と改善案。

## 1. 自作の検証ハーネス（`script -q`）が「機能側のバグ」に見える偽陰性を作った

実 TTY での動作確認に `printf 'y\n' | script -q /dev/null zsh -c ...` を使ったところ、
**確認プロンプトが「中止しました」で終わり exit 130** になった。パイプが閉じた時点で
`script` が pty へ EOF を送り、`read` が EOF を受けて空回答扱いになったため。
出力だけを見ると「y を入れたのに中止される = 実装のバグ」に見える形だった。

`python3` の `pty.fork()` でプロンプト文字列を待ってから `y` を書く driver に替えたら
一発で通った（一覧 → 確認 → dry-run 実行、exit 0）。

**切り出し先の提案**: `_claude/rules/` に新規ルール
「**対話プロンプトの検証は `cmd | script` でなく pty driver（プロンプトを待ってから書く）で行う**」。
既存の [`adversarial-review-own-safeguards.md`](../_claude/rules/adversarial-review-own-safeguards.md)
節 2 は「検査できなかったのに緑」を扱っており、今回は逆向き（**検証できなかったのに赤**、
しかも赤の原因がハーネス側）なので、同じ節に 1 段落足す形でも足りる。判断はユーザーに委ねる。

## 2. `zshlib/_av1ify.zsh` は shellcheck 側なので zsh 固有構文が通らない

`${${x##...}%%...}` のネスト展開（SC2299）と `${(Q)x}`（SC2296）で `make test` が落ちた。
Makefile 冒頭に「同じ .zsh でも `zshlib/_av1ify.zsh` は sh 互換で shellcheck 側」と
書いてあるのに、書く前に読んでいなかった。`(Q)` は既存箇所と同じ `disable=SC2296` で通し、
ネストは一時変数に分解した（分解した方が読みやすくもなった）。

**切り出し先の提案**: 却下（Makefile のコメントが既に正本で、読めば分かる。ルールを増やすより
「触るファイルが lint のどちら側かを最初に見る」で足りる）。

## 3. 敵対的レビュー 3 観点の全数勘定（2026-08-22）

`codex` は使わない方針のため、観点を分けたエージェント 3 体（①壊す ②素通り ③回帰）で実施。
重複を除いた指摘 14 件。**対応 11 / 記録のみ 3**。修正は 2c7fb59。

### 採用して直したも（11）

| # | 指摘 | 対応 |
|---|---|---|
| 1 | 発火ゲートが `-t 0` だけ → 端末から起動したスクリプトで発火（`av1c **/*.mkv(N)` が削除の確認に化ける） | `[[ -o interactive ]]` を追加。①と③が独立に到達した最重要指摘。**後日 (同日) 追記**: ユーザーの明示要求で `_zshrc` に `av1c()` ラッパーを足したため、対話シェルの `av1c` はこのゲートを通る = `av1c **/*.mkv(N)` の化けは再び起こりうる。防波堤は一覧・赤字警告・既定 No・drain の 4 点のみ（実地確認は issue 094 の項目 7） |
| 2 | ディレクトリ 1 行が `✓` 1 件に見えて再帰展開される | `[ディレクトリ]` 印 + 配下件数 + 集計を実ファイル数に |
| 3 | symlink は表示と削除対象が違う | `:A` で正規化して表示・処理を揃え、`(貼付: ...)` を併記 |
| 4 | 先行入力（typeahead）の `y` で承認が成立 | 確認前に drain。pty で「drain なしだと承認される」ことを実測 |
| 5 | 一覧とプロンプトが stdout（`av1c > log` で無言の入力待ち） | stderr へ（バナーと統一） |
| 6 | `AV1_*` の fail-fast がプロンプトの後に後退 / バナー順序が反転 | 検証とバナーをクリップボード読取りより前に戻した |
| 7 | 先頭が `-` のファイルは `✓` に出るが処理されない | 絶対化で解消 |
| 8 | 相対パスが相対表示のままで、どこのファイルか分からない | 絶対化 |
| 9 | `~/` が無言で `✗` 除外 | 先頭 `~` を展開 |
| 10 | 発火条件の 2 行がどのテストからも実行されない（壊しても全 green） | 非対話文脈での実ゲート検証 + `zsh -i` + 静的 pin の 3 本 |
| 11 | 確認プロンプト文字列・`${ans:l}` に肯定 assert が無い | 肯定 assert と大文字 `Y`/`YES` のテストを追加 |

### 記録のみ（3）

- **issue 089 の連鎖**: 承認後は処理本体の `print -P` がファイル名の `$(...)` を実行する。
  執行者は `_av1ify_encode.zsh:68`（単独 attribution 済み）。**ユーザー判断で今回は記録に留めた**。
  089 に追記し、Test 7 の assert 名を「一覧表示では実行しない」に狭めた
- **`av1ify &`（引数なし）が tty input で suspend する**: 従来はヘルプが出て終了していた。
  受容する（`fg` で復帰でき、データ被害はない）。対話シェルでバックグラウンドに投げる
  使い方は想定していない
- **`(( ! __av1ify_internal ))` が現状は冗長**（内部呼び出しは必ず引数を渡すため）: 却下。
  防御として妥当で、消すと将来の内部呼び出し追加時に無言で壊れる

### 反証できた（= 今後の audit が再生成しても即棄却してよい）

- `${(Q)line}` からのコマンド実行は無い（`$(...)` / バッククォート / `'...'` すべて literal）
- `[[ -e "$line" ]]` は quoted なので glob 展開しない（`*` `?` `[]` は literal 名）
- 確認の判定は堅い: `y\r`（CRLF 貼り付け）は中止、空回答・EOF・`read` 失敗は 130、
  `pbpaste` 失敗は 1。`yarn ...` のような「y で始まる別の文字列」も承認にならない
- 配列ずれは作れない（全展開が quoted、空行は事前除去）
- `-f` / `--help` / 不明オプション / 内部再帰 / `_reload_then_call` / 重複パス /
  `pbpaste` 不在（Linux・CI）は回帰なし

**切り出し先の提案**: なし（対応は 2c7fb59 で完了、記録はこの節と issue 089 で足りる）。

## 4. パイプで書いた「先行入力 drain」のテストは原理的に vacuous だった

drain を `read -t 0` で実装し、パイプで「溜めた入力が捨てられる」テストを書いたら red。
調べると **zsh の `read -t 0` は端末に対してのみ「入力が溜まっているか」を答え、パイプでは
常に偽**だった。つまりパイプで書いたテストは「drain がある実装」と「無い実装」を区別できない
（= 書いても vacuous）。pty では期待どおり動くことを実測し、テストは静的 pin に置き換えた。

**切り出し先の提案**: `_claude/rules/mutation-verify-new-tests.md` の「よくある守っていない
テストの形」に 1 行足す（「本番と違う入力チャネル（端末 vs パイプ）で測ると、機構の有無を
区別できないことがある」）。判断はユーザーに委ねる。

## 残課題

- ~~issue 089（`print -P` の 37 行）~~ → 2026-08-22 に全件対応して done へ移した
  （共通パレット `zshlib/_ansi_colors.zsh` + `print -r`。承認後の実行トリガも閉じた）
- ~~項目 1（対話プロンプトの検証を pty driver で行う）の切り出し~~ → 2026-08-24 に実施。
  **新規** [`_claude/rules/verify-interactive-prompt-with-pty-driver.md`](../_claude/rules/verify-interactive-prompt-with-pty-driver.md)（commit `e2448f5`）。
  retro 本文は「`adversarial-review-own-safeguards.md` 節 2 への 1 段落追記でも足りる」と
  していたが、あちらは「検査できなかったのに**緑**」、こちらは「検証できなかったのに**赤**
  （しかも赤の原因がハーネス側）」と向きが逆なので、トリガー別に新規ルールへ分けた
- ~~項目 4（本番と違う入力チャネルで測ると機構の有無を区別できない）の切り出し~~ →
  **却下 (2026-08-29): 反映済み**。項目 1 で新設した
  [`verify-interactive-prompt-with-pty-driver.md`](../_claude/rules/verify-interactive-prompt-with-pty-driver.md)
  が「パイプで足りるか / pty が要るか」の表で `read -t 0` を名指しし、
  「**pty が要る挙動を pty 無しでテストに書かない**。書いても『実装が無い版と区別できない』
  テストになる」と本文で言っている。`mutation-verify-new-tests.md` 側への重複追記は不要
- ~~項目 2 / 3~~ → 本文で却下・対応済みを明記済み
- ~~094 の人手確認（実運用での貼り付け確認）~~ → **2026-09-02 に確認済み**
  （ユーザー報告「クリップボード経由で貼り付けれたよ」）。094 は done へ移した。
  🚨 094 の細目 3〜9 は個別の可否が報告されていないので未確認のまま閉じた（理由は 094 本文）

残課題なし。
