# `--on-lost=kill` の「最初の判定不能で即殺す」と、381 の「判定不能でも更新を続ける」が正面から矛盾する

> 🚨 **担当中: Claude セッション (opus, dotfiles)**（2026-09-20〜）

起票日: 2026-09-16
カテゴリ: design / priority: medium
対象: `src/lockman/with.go` の `reportRenewErr` (`escalate.Do`) と select ループ
出典: [381](381-bug-lockman-with-renew-latch-stops-renewal-forever.md) の敵対的レビュー (観点③ 並行・中断)
反証レビュー: 未実施

## 問題

`reportRenewErr` は**最初の**判定不能で `escalate.Do` を撃つ。理由はコードに書いてある:

> fail-closed で子を止める。**次の tick を待たない** — TTL を超えれば他者が引き継ぐので、
> 待つほど二重実行に近づく。

一方 381 の修正でループ側は逆を言うようになった:

> 判定不能は一過性かもしれないので**更新をやめない**。

**381 より前は整合していた** (諦める = 更新も止める、が一体だった)。381 の後は
**ループが粘り、昇格は即殺す**。昇格は不可逆、更新の粘りは可逆なので、既定
(`--on-lost=kill`) では 381 の便益が**ほとんど回収されない**。

## 実測 (レビュワー。本セッションでは追試していない)

`TestLeaseSurvivesTransientRenewBlock` と**同じ** 900ms の一過性の詰まりを既定
(`onLostKill=true`) で作ると **exit=125 / 子は完走しない**。lease は誰にも奪われていない
(同じ手順の warn 版テストが lease の生存を証明している)。つまり
**生き残った lease のために、殺さなくてよかった子を殺している**。

便益がゼロではない点は正確に: TERM を trap / 無視する子は猶予 (既定 5s) のあいだ走り続け、
その間は更新の粘りが lease を保つ。SIGTERM で素直に死ぬ子では観測できない。

## 🚨 「上限に張り付くと二重実行が残る」は新しい穴ではない

381 / 本 issue に「見捨てた更新が上限 (既定 8) まで溜まると更新を起こさなくなり、
`--on-lost=warn` では二重実行が残る」と書いてあるが、**修正前は詰まった更新 1 本で同じ状態に
なっていた** (`renewCh` を握ったまま二度と更新しない)。つまり 381 が新しい失敗モードを
作ったのではなく、**発火の閾値が 1 本 → 8 本に上がった**だけ。判断の前提を取り違えないこと。

## 判断すべきこと

「判定不能」で即殺すのは**安全側だが高価**。lease を保てる見込みがあるなら、
`renewLost` (確実な喪失) と `renewIndeterminate` (判定不能) で昇格の扱いを分ける余地がある。

- (a) 現状維持。fail-closed を最優先し、381 の便益は `--on-lost=warn` 専用と割り切る
- (b) 判定不能では**猶予を置く** (次の更新が成功したら昇格しない / N 回連続で判定不能なら昇格)。
  🚨 これは fail-closed → fail-open の変更なので、**二重実行の窓を自分で開けることになる**。
  変えるなら A-B の実測と変異検証が要る
- (c) 判定不能のときだけ `--on-lost` の既定を変える (新しいモード)

## 進捗 (2026-09-20): (b') を採用して実装

### 追試 (残タスク 3)

レビュワーの実測を**本セッションで再現した** (ttl=900ms / tick=300ms / io-timeout=200ms、
`TestLeaseSurvivesTransientRenewBlock` と同じ 900ms の詰まり):

| 腕 | rc | 子の完走 | 所要 | lease |
|---|---|---|---|---|
| `--on-lost=warn` | 125 | **true** | 3.04s | 保持 (他者は取得できず) |
| `--on-lost=kill` (既定) | 125 | **false** | 710ms | 殺した時点では生きていた |

### 採用した案: (b') — (b) を精緻化したもの

(b) は issue 本文が「fail-closed → fail-open になる」と警告しているが、**保留の終点を
推測ではなく計算にすれば fail-open にならない**:

- `renewLost` (errNotOwner) = **他者が既に引き継いでいる** → **即昇格** (従来どおり)
- `renewIndeterminate` = lease の生死は不明。ただし**他者が正当に引き継げる時刻は計算できる**
  (最後に成功した更新の開始時刻 + TTL)。その期限までは誰も引き継げないので昇格を保留し、
  **期限を過ぎたら tick で必ず昇格する**

🚨 見積もりは**保守側**: 打刻はサーバが行うのでこちらからは「呼ぶ前」と「返った後」しか
見えない。**呼ぶ前**を起点にすれば「最も早い期限」になる (遅く見積もると、他者が引き継げる
時刻を過ぎてから昇格することになる)。残る窓 (TERM → SIGKILL の猶予) は、確実な喪失の経路が
既に受容しているものと同じ。

### A-B 実測 (2026-09-20)

| 詰まり | 旧 (即昇格) | 新 (期限まで保留) |
|---|---|---|
| 300ms | 子が死ぬ (510ms) | **子が完走** (3.02s) / lease 保持 |
| 500ms | 子が死ぬ (710ms) | **子が完走** (3.02s) / lease 保持 |
| 700ms | 子が死ぬ (710ms) | 子が死ぬ (910ms) ← **期限超過なので従来どおり昇格** |

### テスト (残タスク 2 = 「既定の経路を通る lease 生存テスト」)

- `TestDefaultKillWaitsForLeaseDeadline` — **両腕**を固定 (期限前の復旧で完走 / 期限超過で昇格)
- `TestLeaseDeadlineAdvancesOnSuccessfulRenew` — 詰まり **2 回**。
  🚨 これが無いと「更新の成功で期限を進める」が無検査だった (1 回詰まりでは期限を進めるか
  どうかが結果を変えないので**構造的に観測できない**。実測: M3 が 1 回詰まりの 2 本で緑)

### 変異検証 (3 本とも red)

| 変異 | 結果 |
|---|---|
| M1 期限の判定を外す (判定不能で永久に昇格しない = fail-open へ倒す) | `TestDefaultKillWaitsForLeaseDeadline/期限を過ぎる` **FAIL** |
| M2 判定不能でも即昇格 (旧挙動) | 同 `/一過性` **FAIL** |
| M3 更新が成功しても期限を進めない | `TestLeaseDeadlineAdvancesOnSuccessfulRenew` **FAIL** |

`go test -race ./...` 緑 / golangci-lint 0 issues。

### 据え置いたもの

- **終了コードは sticky のまま 125**。判定不能だった窓があった事実は消えないので、
  復旧して完走しても rc=125 (091:398-399 / issue 381 の判断を据え置く)
- **`--on-lost=warn` の挙動は不変**

## 敵対的レビュー 1 周目 (2026-09-20。全数勘定)

指摘 9 件、**採用 6 / 記録 3 / 却下 0**。核 ((b') の判断) は壊せなかったが、
🚨 **(b') を「fail-open ではない」と言い切る根拠の一文が偽**で、**要の 1 行が無検査**だった。

| # | 指摘 | 判定 |
|---|---|---|
| P1-1 | 「**期限までは誰も引き継げない**」は **`lockman break` に対して偽**。`Break` は期限も token も見ない無条件の rename。実測 (ttl=1800ms / 恒久的な詰まり / t=1000ms で break → 別ホストが acquire、各 3 回): **旧挙動は重なり −7〜−16ms (二重実行なし) / 新挙動は +185〜+193ms**。窓の上限は「break 後に**完了する**更新が errNotOwner を返すまで」= 最大 1 tick + io-timeout (既定 ttl 30m なら約 10 分)。🚨 判定不能の原因が続いて errNotOwner を観測できない場合は期限まで (既定 30 分) 伸びうる (**未確認**) | **採用 (訂正)**。前提の記述をコードと本 issue で訂正し、**それでも (b') を採る理由**を書いた: break は人が force で撃つ操作で、`lock.go` の `tryTakeover` の注記が既に「最も現実的に二重取得を作る操作」として受容している |
| P1-2 | `case err := <-renewCh` の **`break` が完全に無検査**。外すと**失敗した更新でも期限が ttl ぶん前進し保留も解ける** = 昇格が永久に来ない (lease は死ぬので無制限の二重実行) のに、**全スイート緑**。既存テストは FIFO しか使わず **`renewCh` のエラー枝を 1 本も通っていなかった** | **採用**。実在する速い失敗 (`errUnreadableLock`) で列を作り `TestFailedRenewDoesNotAdvanceLeaseDeadline` を新設 (A-B: 現行 918ms で昇格 / break 削除は 3.03s で子が完走) |
| P2-1 | 「保守側の見積もり」と境界が**1 つもテストで固定されていない**。M4 (起点を「更新が返った時刻」へ) / M5 (同、acquire 側) / **M6 (境界に 400ms = ttl の 44% の猶予を足す = 明示的な fail-open)** の 3 変異がすべて全スイート緑 | **採用 (部分)**。境界を純関数 `leaseHasExpired` へ切り出して**単体で固定**した (M6 で red)。**M4 / M5 は記録**: 差は「その呼び出しにかかった時間」で、ローカルでは数 ms なので統合テストでは原理的に判別できない (SMB では秒オーダーになりうる)。再開の trigger は「実 SMB で A-B を採るとき」 |
| P2-2 | 上限の枝のコメント 2 つが (b') で**偽になっていた** (「ここへ来た時点で昇格も済んでいる」/「kill なら最初の期限切れで止めにいっている」) | **採用**。両方訂正した (「もう守られている」の嘘は、次の人が冗長と判断する根拠になる) |
| P2-3 | 期限は**ローカル単調時計**だが、引き継ぎを決めるのは**サーバ時計** (`serverNow`)。サーバ側が前方へステップ / 速くドリフトすると保守側の保証は崩れる (**未確認**) | **採用 (明記)**。「同一レート・ステップ無しを仮定している」をコードに書いた |
| P3-1 | 期限は**構造的に tick 境界と重なる** (`renewStartedAt` が tick の瞬間、周期は ttl/3) ので、tick の枝でしか見ないと配送ジッタで**次の tick まで = ttl/3 遅れる** (実測 92 回中 1 回。既定なら 10 分の窓) | **採用**。`reportRenewErr` の判定不能側でも期限を見るようにした。🚨 この 1 行は**変異 M8 で緑のまま** — 窓を縮めるだけで決定論的に固定する手段が無い (seam を足せば可能だが機構が増える)。検出可能性は「**検出手段はあるが未実証**」 |
| P3-2 | 昇格の後に「lease は生きていた」と**証明できても取り消せない** (猶予中に復旧しても SIGKILL する)。実測 3/3 | **記録**。(b') の目的が最後に破れる場所。猶予の select に「復旧した」チャネルを足せば閉じるが、機構が増えるので見送る。再開の trigger: 猶予中の復旧が実運用で観測されたとき |
| P3-3 | 新テスト 2 本は **lease を観測していない** (マーカーと rc だけ) / **SIGKILL まで進む列が無い** | **記録**。「一過性」腕は他者に奪われていても緑になる。既存の `probeLease` を足せば閉じる |
| P3-4 | `TestLeaseDeadlineAdvancesOnSuccessfulRenew` の余裕は「更新 1 本が io-timeout 以内に返ること」だけ (復旧の窓に tick が 1 本しか入らない) | **記録**。`-race` + CPU 負荷 8 本で 4 回連続 green = **再現せず** (余裕 ≈ 70 倍) |

### 壊せなかった経路 (レビューが実験で確認)

- **上限 (`maxInFlightRenews`) に張り付いたまま昇格が消える列**: 作れなかった。上限の枝へ
  `pendingIndeterminate=false` で到達するには「直前の更新が成功」が要るが、その時点で
  `inFlight` は増えていない (新しい arm には (b)(c) の経路が要る)
- **ローカル時計のステップ / 巻き戻し**: Go の単調読みなので起きない
- **`renewStartedAt` が古いまま / 別の更新の起点が混ざる**: `renewCh != nil` の間は tick が
  `continue` するので新しい arm が起きない。ゼロ値で使われる経路も**安全側** (過去 = 期限切れ)
- **select 内 `break` が `for` を抜ける罠**: innermost が select なので起きない (A-B でも裏付け)
- **`--on-lost=warn` の挙動**: `if !onLostKill { return }` が手前にあるので不変
- **既存テストの意味**: `TestOnLostKillEscalatesToSigkill` は即昇格の枝なので不変。
  🚨 ただし `TestWithSeparatesLostLeaseFromIndeterminate` は**子の生死を assert していない**ので
  「即殺す → 期限まで待つ」の変化を素通りする (退行ではないが、この列は今回の変更を 1 mm も守らない)

### 2 周目が要る

§7 の打ち切り条件 (a)(b) を**満たさない**: 1 周目の対応が **production に新しい判定を足した**
(報告時の期限チェック / 境界の純関数化)。攻め口: ①報告時の期限チェックと tick 側の二重化で
昇格が二度撃たれないか ②`leaseHasExpired` の切り出しで呼び出し側の意味が変わっていないか
③`errUnreadableLock` を使った新 fixture が本当にその列を作れているか (別の理由で赤くないか)

## 敵対的レビュー 2 周目 (2026-09-20。全数勘定)

**P1 なし**。指摘 4 件、**採用 2 / 記録 2 / 却下 0**。核 ((b') の判断) も 1 周目の修正も壊せなかった。

| # | 指摘 | 判定 |
|---|---|---|
| P2-A | `leaseHasExpired` の doc が「この関数が**根拠そのもの**」と書いていたが、根拠は比較される**値**。期限を決める箇所は 3 つ (①起点 ②呼び出し側 ③述語) で、単体で固定できているのは③だけ。**同じ幅 (ttl の 44%) の猶予を①②へ移した変異 M9 / M10 は全緑**。🚨 記録済みの M4 / M5 (どの時刻を起点にするか = ローカルでは数 ms で判別不能) とは別で、**ローカルでも観測可能な幅なのに 1 本も落ちない** | **採用 (訂正)**。doc を実態へ直し、**受容して記録**した。閉じるには静的 pin (「`leaseDeadline` への代入は 1 関数の戻り値だけ」) か壁時計の上限が要り、どちらも新しい機構になる。再開の trigger: 期限の計算を触る変更が入るとき |
| P3-C | `TestFailedRenewDoesNotAdvanceLeaseDeadline` の合否が「marker が無い」+「rc == 125」だけの**純粋な否定**。rc=125 は昇格と無関係な 3 経路 (Acquire の I/O 失敗 / `cmd.Start()` の失敗 / 解放失敗) からも出るので、**fixture が壊れて列が成立しなくなっても緑**。実測: argv を存在しないバイナリに替えると PASS (0.02s。正常時は 0.91s) | **採用**。子が走り始めたことを固定する肯定側の観測を 1 つ足した (M11 で red) |
| P3-B | 1 周目に足した「報告時の期限チェック」は**毎 run 走っているが、効いた列は 1 本も無い** (probe で実測: 常に tick が先に `escalate.Do` を消費した直後に走る)。1 周目の「未実証」を「**効果が出る順序で走った列が 0 本**」まで具体化した | **記録**。消さないのは、1 周目に実測した取りこぼし (92 回中 1 回 = 既定なら 10 分の窓) がまさにこの枝の対象だから。決定論的に作るには seam が要る。🚨 根拠コメント自体は正しいことをレビューが ticker の意味論で確認した |
| P3-D | 1 周目の対応で `escalateNow` が **1 run に 2 回**呼ばれる形になった (tick → 報告時)。実害は `escalate.Do` の Once が吸収しているが、**その Once は無検査のまま** (直接呼び出しへ置換する変異が全緑)。以前は稀にしか踏まなかった経路が**毎 run 踏まれる** = 守りの重心を無検査の機構へ寄せた | **記録**。Once が退行したときの害は doc が書いている (「猶予が振り出しに戻り SIGKILL へ永久に到達しない」)。再開の trigger: `escalate.Do` を触る変更が入るとき |

### 壊せなかった経路 (レビューが実験・推論を明記して確認)

- **報告時チェックが tick より早く昇格させる**: 原理的に無い (`now >= leaseDeadline` でしか真にならない)
- **昇格が二度撃たれる実害**: `escalate.Do` が吸収 (実験)
- **`leaseHasExpired` の抽出で意味が変わった**: 抽出前後で**逐語一致** (実験)
- **新 fixture が別の列を通っている**: probe で `arm=renewCh err=errUnreadableLock` が **9/9**、
  `arm=renewExpired` は **0**。FIFO 系は逆に `renewExpired` 5 / `renewCh` 0 = **2 つの fixture は相補的**
- **P1-2 の修正が効いていない**: `break` を外すと**その 1 本だけ**が FAIL = 唯一の番人として効いている
- **コメントの事実確認**: break 後の `errNotOwner` / 上限の枝の 2 つ / tick 境界ジッタの根拠 — **すべて正しい**
  (1 周目に書いた break の A-B の実測値だけは 2 周目では未追試)

### 3 周目は不要 (§7 の打ち切り条件)

打ち切り条件の例外 (a)(b) を満たす: **(a) 2 周目の対応は production の判定ロジックを 1 つも
新設していない** (テストへ肯定側の assert を 1 つ + doc の訂正。採用した P2-A も「受容して記録」で、
静的 pin や壁時計の上限は**足さない**と決めた)。**(b) 各修正を直接の実測で確認した**
(M11 / M7 / M6 の 3 本で red、`go test -race ./...` 緑、golangci-lint 0 issues)。
🚨 環境条件を新たに持ち込む修正も無い。

## テストの穴 (どの案を採るにも先に埋まっていた方がよい)

lease 生存系のテストは**全部 `onLostKill=false`**。既定の経路を通るのは
`TestOnLostKillEscalatesToSigkill` だけで、そこは「本当に喪失した」列。
381 のレビュー観点② で `escalate.Do` のゲートを外す変異が全緑だったのも同じ穴の別の顔。

## 残タスク

- [x] (a)/(b)/(c) を決めた → **(b')** (ユーザー判断。A-B と変異検証つき。上節)
- [x] 既定 (`--on-lost=kill`) の経路を通る lease 生存テストを足した (2 本)
- [x] レビュワーの実測を追試した (上表)
- [x] 反証レビュー 1 周目。P1 2 件 + P2 3 件 + P3 1 件を採用、P3 3 件を記録 (変異 2 本で red)
- [x] 2 周目 (§7)。**P1 なし**。P2-A / P3-C を採用、P3-B / P3-D を記録 (変異 3 本で red)。
      **3 周目は不要** (打ち切り条件 (a)(b) を満たす。production の判定は 1 つも増やしていない)

## 関連

- [381](381-bug-lockman-with-renew-latch-stops-renewal-forever.md) — 出典。ループ側を「粘る」へ変えた
- [384](384-bug-lockman-escalation-burns-out-and-sigkill-skips-recheck.md) — 昇格**実装**側の穴
- [356](356-bug-lockman-with-releases-lock-while-grandchildren-run.md) — 昇格そのものの出典

## 後日対応 (2026-09-20): `leaseTracker` を切り出して P2-A を閉じた

2 周目の P2-A (「期限の**値**は無検査。猶予を①起点 ②呼び出し側へ移した変異が全緑」) を
**受容して記録**する判断だったが、ユーザーの指示で**その場で対応した**。

切り出しの目的は分割ではなく **期限の値を単体で assert できるようにすること**。
`runWith` に散っていた 3 箇所 (起点に ttl を足す 2 箇所 / 呼び出し側の述語 / 境界の述語) を
`leaseTracker` (`src/lockman/lease_tracker.go`) へ寄せ、**時刻は引数で受け取る**契約にした
(内部で `time.Now()` を読むと「いつを起点にしたか」がテストから観測できず、M4 型の変異が
また無検査へ戻る)。

### 変異検証 (P2-A で「無検査」とされた 3 箇所すべて)

| 変異 | 対応前 | 対応後 |
|---|---|---|
| M6' 境界 (述語) に猶予 | red (単体のみ) | `TestLeaseTracker` の 4 subtest + 統合 2 本が red |
| M10' 起点 (`renewed`) に猶予 | **全緑** | `TestLeaseTracker` 2 subtest が red |
| M4' `renewed` が渡された時刻を無視して `time.Now()` を使う | **全緑** | `TestLeaseTracker` 2 subtest が red |
| M9' 呼び出し側 (tick) だけに猶予 | — | **緑のまま**。ただし tick と報告の 2 箇所は**多重防御**で、
  **両方に当てると red** (`TestDefaultKillWaitsForLeaseDeadline/期限を過ぎる` +
  `TestFailedRenewDoesNotAdvanceLeaseDeadline`)。片方だけ潰しても、もう片方が同じ判定を持つ |

`go test -race ./...` 緑 / golangci-lint 0 issues。**production の挙動は変えていない**
(期限の計算・境界・保留の意味はすべて逐語で移した)。
