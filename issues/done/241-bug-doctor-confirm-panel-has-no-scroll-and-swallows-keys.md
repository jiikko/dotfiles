# 241 bug: 削除の確認パネルは入り切らない対象を見る手段が無く、見ようとした打鍵が確認ごと無言で消える

起票日: 2026-09-04
出典: audit (ux) 2026-09-04 / doctor スコープ
重要度: **P2**（破壊的操作の確認画面で「対象を確かめる」ができない。倒れる向きは安全側だが無言）
対象: `src/glogx/doctor_delete.go` の `handleDeleteKey`（`case d.confirm` の `default`）/
      `assembleDeletePanel` / `maxConfirmPaths`

## 症状と発火条件

選択が多くて `assembleDeletePanel` が `… 他 N 件は画面に入りません (端末を広げてください)` を出す状態、
または 1 エントリの対象が 11 件以上で `… 他 N 件` に畳まれた状態で、
`j` / `k` / `ctrl+d` / `Space` / `g` / `G` / `Enter` のいずれかを押したとき。

- `assembleDeletePanel` は塊単位で本文を落とし、案内は「端末を広げてください」だけ（**スクロールの語彙が無い**）
- `handleDeleteKey` の confirm 分岐は `case "y", "Y"` 以外を**すべて `d.reset()` + `doctorSwallow`** にするので、
  スクロールしようとした打鍵が**確認そのものを閉じる**
- `doctorSwallow` はトーストを出さないので**無言**。`v.selected` は残るため一覧に戻ると `*` 印だけが
  元のままで、「何が起きたか」が読み取れない

## 語彙の非対称（同じ経路）

glogx の破壊操作の確認は `status_view.go:discardBox` が `y/Enter: 実行   n/Esc: キャンセル` で、
`discardKey` の doc は「push/pull 確認と同じ語彙」と明記し issue 071 / 123 で 2 度再確認されている。
**doctor の削除確認だけ `Enter` がキャンセル**（hint も `y: 削除する   n/Esc: やめる`）。
倒れる向きは安全側だが、`Enter=実行` の手癖で押すと無言で閉じる。

## 直し方

確認パネルに窓を持たせる（同 package の `rowCursor.window` がそのまま使える）。
最低限 `j/k/ctrl+d/ctrl+u/g/G` は「飲むだけ」にして中止から外し、`maxConfirmPaths` の
打ち切りを画面高に応じて可変にする。`Enter` を受けるかは共通語彙の議論なので別途。

## 既存 issue との関係

issue 233 は「触らない対象まで数えて並べる」= 件数と対象集合の話で、
**表示しきれない対象を見る手段が無い / 見ようとするとキャンセルされる**は別。
issue 236 の P3-6（削除中の `ctrl+g`）は実行中フェーズの話で、こちらは確認フェーズ。

## 決着 (2026-09-04)

確認パネルに**行単位の窓**を持たせ、送るキーを中止から外した。

- `doctorDelete.confirmScroll` (`deleteScroll`) が縦位置を持つ。`assembleDeletePanel` は
  `sc != nil` の呼び出し (= 確認パネル) だけ本文を行単位で切り出す。**塊単位で落とす形だと、
  1 つの塊が窓より大きいときに本文が丸ごと消える**ので、行単位にした
  (結果パネルは送る手段が無いので従来どおり塊単位。`sc == nil`)
- 入り切らないぶんは `… 上に N 行 / 下に M 行 (j/k で送る)` で伝える
  (従来の「端末を広げてください」は、送れる今は誤案内なので置き換えた)
- `j` / `down` / `k` / `up` / `ctrl+d` / `pgdown` / `ctrl+u` / `pgup` / `g` / `home` / `G` / `end` は
  **送るだけ**。それ以外は**従来どおり中止**（既定は安全側のまま緩めていない）
- 送り方の出典は**パネルの注記 1 つ**にした。hint に足すと、hint は前フレームの高さから
  判断するので 1 フレーム遅れ、「パネルと hint が違うことを言う」形になる (242 の P3-2 と同型)

### やらなかったこと（理由つき）

- **`maxConfirmPaths` を画面高で可変にする**: 却下。この打ち切りは「1 エントリで画面を埋めない
  (他のエントリが丸ごと省略されるのを防ぐ)」ための不変条件で、`doctor_delete_test.go` の
  `他 3 件` がそれを pin している。窓が付いた今も、1 エントリを全部展開すると他のエントリが
  窓の外へ押し出される点は変わらないので、10 件のままにした
- **`Enter` を実行側へ揃える**: 本 issue が「別途」としていたとおり手を付けていない。
  破壊的操作の確認で既定を実行側へ倒す変更なので、単独で判断する (issue 243 に切り出した)

### 検証

- 変異 3 本（いずれも `go build` 通過を確認してから判定）:
  | 変異 | 結果 |
  |---|---|
  | 送るキーの分岐を消す (本 issue 以前の姿) | `TestDeleteConfirmScrollKeysDoNotCancel` が red |
  | `window` が offset を無視して常に先頭を返す | 同テストが red |
  | 中止の既定を「飲むだけ」に緩める | `TestDeleteConfirmOtherKeysStillCancel` が red |
- 🚨 2 本目は**最初 green だった**。テストが注記 (`上に N 行`) の変化しか見ておらず、
  本文が動いていなくても通っていた (offset は更新されるため)。**本文の中身**
  (先頭のエントリが消えて末尾のエントリが見える) で判定する形へ直した
- `make -C src/glogx lint` 0 issues / `make -C src/glogx test` (-race) 全緑
- 見た目は本体へ入れる前にサンプル描画で確認 (offset 0 / 6 / 99=clamp の 3 位置)
