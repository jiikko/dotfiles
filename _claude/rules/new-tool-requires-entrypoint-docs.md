# 新しいツール・スクリプトを導入したら、同じ変更で「入口のドキュメント」を更新する

## ルール

- **再利用される道具（スクリプト / CLI / Makefile target / lint ルール / ヘルパー）を新設したら、
  それを見つけられる「入口」を同じ変更の中で更新する。** 道具だけ足して終わりにしない
- 更新すべき「入口」は、**その作業をこれからやる人が実際に読む場所**:
  - その作業手順を持つ **skill (`SKILL.md`)** ← 最優先。手順書に載っていない道具は使われない
  - その領域の **CLAUDE.md / README**（規約や運用の正本）
  - 既存の類似ツールの **一覧表 / 関連リンク**（`cutout-sheet` があるなら `cutout-single` も並べる）
- **ツール自身のヘッダコメントは「入口」に数えない**。そのファイルを開いた人にしか届かず、
  開く動機は「存在を知っている人」にしかない。循環しているので単独では機能しない
- 既存ツールと**紛らわしい/使い分けが要る**ものを足したときは、**判断基準を 1 行で書く**。
  「どちらを使うか」が読んで即決できない記述は、書いていないのとあまり変わらない
- 道具を**消した / 置き換えた**ときも同じ。入口に古い道具が残っていると、存在しないものへ誘導する

## なぜ（起源: ThumbnailThumb `cutout-single`, 2026-08-16）

素材生成パイプラインで、既存の `cutout-sheet`（連結成分 = 別素材として分割）が
「1 つの記号が離れた島に分かれている素材」を扱えないと分かり、`cutout-single` を新設した。
ツールは commit し、**設計理由もファイル冒頭に丁寧に書いた**。

しかし **`.claude/skills/asset-gen/SKILL.md` の切り抜き手順を更新しなかった**。
手順書は `cutout-sheet` だけを指示したままで、`assets/README.md`（規約の正本）にも記載がない。

結果として「ツールは存在するが、次にこの作業をする人は手順書どおり `cutout-sheet` を使い、
同じ 4 分割を踏む」状態が出来上がった。翌日ユーザーに
「使い方は skill かな？明文化できているの？」と聞かれて初めて気づいた。

**道具を作った時点の満足感が、明文化の動機を消す。** 作った本人は使い方を覚えているので、
書かれていないことに気づけない。気づけるのは「知らない人」であり、その人は困るまで現れない。

## タイミング

**道具を commit する変更に含める。** 「あとでまとめて」にしない。

理由: ツール単体の commit は「動くから完了」と感じられて、そこで作業が閉じる。
一度閉じたものを開き直す動機は生まれない（実際に上記は翌日まで放置された）。

## チェック（道具を足したら自問する）

- [ ] この道具を**使うべき場面に遭遇する人**は、どの手順書を読むか。そこに書いたか
- [ ] 似た道具がすでにあるか。あるなら**どちらを使うかの判断基準**を書いたか
- [ ] 一覧表・関連リンクに並べたか（片方だけ載っている状態にしていないか）
- [ ] 自分が半年後にこの作業をするとき、**ツールの存在を思い出せなくても**たどり着けるか

## やること / やらないこと

- ✓ 道具の追加と入口ドキュメントの更新を**同じ変更**で行う
- ✓ 使い分けが要るなら判断基準を 1 行で書く
- ✓ skill / CLAUDE.md / README のうち「実際に読まれる入口」を狙って書く
- ✗ ツールのヘッダコメントだけ書いて明文化した気になる
- ✗ 「動いたから完了」でツール単体を commit して閉じる
- ✗ 既存ツールの一覧に新ツールを並べず、片方だけ載った状態にする

## 適用しなくてよいもの

- そのタスク限りの使い捨てスクリプト（`./tmp` に置いて消すもの）。
  ただし**2 回目に使いたくなった時点で**このルールの対象になる
- 既存の道具の内部実装だけを直した場合（使い方が変わらないなら入口の更新は不要）

## 関連

- [`claude-md-maintenance.md`](claude-md-maintenance.md) — 「触ったディレクトリの CLAUDE.md は
  同じ PR で直す」。本ルールはその「新規の道具」版で、方向が逆（乖離の修正ではなく新規の登録）
- [`pending-issue-rationale-in-code.md`](pending-issue-rationale-in-code.md) — 「なぜそうしないか」を
  コード直近に残す。本ルールの「ヘッダコメントは入口に数えない」と併せて読む:
  **rationale はコードに、使い方は手順書に**置く（置き場所が違う）
- [`comment-no-restate-enforced.md`](comment-no-restate-enforced.md) — コメントに書くべきものの線引き
