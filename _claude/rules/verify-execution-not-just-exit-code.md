# 検証は「exit code」ではなく「実行された証拠」で判定する

## ルール

- **検査・テスト・gate を「通った」と判断する前に、それが実際に実行された証拠を出力で確認する**。
  exit code 0 は「合格」ではなく「**失敗しなかった**」であり、
  「**そもそも走らなかった**」も含む
- **新設した検査を最初に配線したときは、必ず「その検査の出力が現れること」を確認する**。
  `make lint && echo ok` / `pytest -k new_test` の exit 0 だけで満足しない
- **パイプの終端の status を見ない**。`cmd | tail` の `$?` は `tail` の status。
  必要なら `set -o pipefail` を使うか、リダイレクトしてから exit code を取る

## なぜ (2026-08-23 obaket、同一セッションで 3 回踏んだ)

`mutation-verify-new-tests.md` は「green は正しいではない」を扱うが、その **手前** に
「そもそも走ったのか」という段がある。ここが抜けると変異検証すら空振りする
(壊す変更を当てても、走っていない検査は元から red にならない)。

実際に踏んだ 3 形態:

| 症状の出方 | 誤読 | 実態 |
|---|---|---|
| **緑** | 「gate 通過」 | 新設 gate が集約 target に未配線で **1 度も走っていない** |
| 赤 (generic message) | 「どれかの gate が落ちた」 | gate は 1 つも走らず、`make` が target 不在で即死 |
| 赤 (別 exit code) | 「パス解決を直せば通る」 | その実行環境に検査対象のソースが存在しない |

**最も危険なのは 1 つ目 (緑)**。赤なら調査が始まるが、緑は指摘されるまで気づけない。
しかも「検査を足す」= 安全側の作業ほどこの穴を踏む (足した本人は守ったと思い、実際は守られない)。

同型の先例として obaket issue 556 がある: 検査を `make lint` の gate へ移したが、
CI は `make lint` を呼んでいなかったため **移した先で 1 度も走らなくなっていた**。
症状 (テストが落ちる) だけが消えて、回帰検出は失われていた。

## 確認の仕方

| 対象 | 「走った証拠」の取り方 |
|---|---|
| lint gate / 検査スクリプト | その gate 固有の出力行を `grep` する (件数を出す設計にしておくと確実) |
| 新規テスト | テスト名が実行一覧に出ることを確認する (`-only-testing` の vacuous pass 対策と同根) |
| CI 上の検査 | ログに出力行があることを確認する。**緑という結果だけで判断しない** |
| 集約 target への配線 | 集約 target 経由で実行して出力を確認する。個別に直接叩いた結果で代用しない |

**「件数を出す」設計が効く**: `検査対象 N 件を確認` のように数を出しておくと、
0 件や想定より少ない数を見た瞬間に空振りが分かる。収集 0 件を失敗扱いにするのも同じ発想。

## 構造で守れるなら守る

同じ配線漏れが繰り返す箇所は、運用ではなく機械で止める
(実例: obaket `macOS/bin/check-lint-gate-wiring` = gate target が集約 target に
配線されているか / `.PHONY` 漏れ / allowlist の stale を検出する meta-gate)。

## やること / やらないこと

- ✓ 検査の合格判定は「その検査の出力が出たか」で行う
- ✓ 新設検査は集約経路から実行して出力を確認する
- ✓ 収集 0 件 / 対象 0 件は成功にせず失敗扱いにする
- ✓ パイプを挟むなら `set -o pipefail`
- ✗ exit code 0 だけを見て「通った」と報告する
- ✗ `cmd | tail` の `$?` を cmd の status として扱う
- ✗ 個別に直接叩いた結果を「集約経路でも走る」証拠として使う

## 関連

- [`mutation-verify-new-tests.md`](mutation-verify-new-tests.md) — 本ルールの **次段**
  (走ったことを確認した後、その検査が本当に主張を守れているかを変異で確かめる)
- [`instrument-before-second-fix.md`](instrument-before-second-fix.md) — 修正が外れたときに
  観測を増やす。本ルールは「観測結果の読み方」側
- obaket `issues/566-test-lint-gate-wiring-is-unverified.md` /
  `issues/568-retro-not-executed-read-as-passed-2026-08-23.md` — 起源
